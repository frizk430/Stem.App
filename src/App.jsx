import React, { useState, useEffect, useMemo } from "react";
import {
  Warehouse, ArrowDownCircle, ArrowUpCircle, History as HistoryIcon,
  AlertTriangle, Search, Plus, Loader2, Leaf, Truck, Star, Trash2,
  ShieldCheck, FileText, Printer, Mail, X, ClipboardList, LogOut,
  UserPlus, KeyRound, Settings, Tag, Scissors, Repeat, Download, Users,
  DollarSign, Image as ImageIcon, Gauge, CheckCircle2, Circle, LayoutGrid,
  RotateCcw, ChevronDown, Edit3, Cog, HelpCircle, Clock, ClipboardCheck,
  Camera, ScanLine, Keyboard, Sprout, Check
} from "lucide-react";
import { supabase } from "./supabaseClient";

const G_PER_LB = 454; // business rounding: 454g = 1lb
const APP_VERSION = "26.8.3"; // format: <year>.<minor>.<patch> — patch counts 1-9 each minor, then rolls the minor up and patch resets to 1 (e.g. 26.3.9 -> 26.4.1); bump minor first digit only each new year
export const LOGO_DATA_URI = "data:image/png;base64,";
const LOW_STOCK_G = 454;

const uid = () => Math.random().toString(36).slice(2, 10) + Date.now().toString(36);
const todayISO = () => new Date().toISOString().slice(0, 10);

// Used to power "quick-fill from a recent batch" — sorts by whichever delivery/addition was most
// recently logged (not when the batch/lot was first created), so a batch that just got a new
// delivery this week bubbles back to the top even if it was originally created weeks ago.
function lastActivityTime(item, subListKey) {
  const subList = item[subListKey];
  if (subList && subList.length > 0) return Math.max(...subList.map((s) => s.createdAt || 0));
  return item.createdAt || 0;
}
function recentDistinct(list, keyFn, subListKey, limit = 8) {
  const seen = new Set();
  const result = [];
  [...(list || [])].sort((a, b) => lastActivityTime(b, subListKey) - lastActivityTime(a, subListKey)).forEach((item) => {
    const key = keyFn(item);
    if (seen.has(key)) return;
    seen.add(key);
    result.push(item);
  });
  return result.slice(0, limit);
}
// A Metrc package tag should never be logged twice — it identifies one physical package. Checks
// both what's already been submitted (existingList, e.g. batches or bucked lots) and whatever's
// currently staged in this manifest but not yet submitted (lineItems), so a double-tap on "Add
// Item" gets caught before it ever reaches the database.
function findPackageTagOwner(tag, existingList, subListKey, lineItems) {
  const tagLower = (tag || "").trim().toLowerCase();
  if (!tagLower) return null;
  for (const item of (existingList || [])) {
    const subList = item[subListKey];
    const entries = (subList && subList.length > 0) ? subList : [{ packageTag: item.packageTag }];
    for (const entry of entries) {
      if (entry.packageTag && entry.packageTag.toLowerCase() === tagLower) return `${item.strain} (Batch ${item.lot})`;
    }
  }
  for (const l of (lineItems || [])) {
    if (l.packageTag && l.packageTag.toLowerCase() === tagLower) return `${l.strain} (Batch ${l.lot}) — already added to this manifest`;
  }
  return null;
}
// Trim entries record a trimmer's total weight only (grams), not a grade breakdown. Older entries
// from before this split still carry a gradeGrams object instead — this keeps both readable.
function trimEntryTotal(e) {
  if (e.grams != null) return e.grams;
  return Object.values(e.gradeGrams || {}).reduce((s, g) => s + g, 0);
}
function gradeEntryTotal(e) {
  return Object.values(e.gradeGrams || {}).reduce((s, g) => s + g, 0);
}
function QuickFillStrip({ items, renderLabel, onPick }) {
  if (!items || items.length === 0) return null;
  return (
    <div style={{ marginBottom: 10 }}>
      <div style={{ fontSize: 11, color: "#7C8571", marginBottom: 6 }}>Quick-fill from a recent batch:</div>
      <div style={{ display: "flex", flexWrap: "wrap", gap: 6 }}>
        {items.map((item) => (
          <button type="button" key={item.id} onClick={() => onPick(item)} style={styles.quickFillChip}>{renderLabel(item)}</button>
        ))}
      </div>
    </div>
  );
}

// Natural-order string compare so "F2" sorts before "F10" (alphabetical would reverse them). Splits
// each string into text/number chunks and compares chunk by chunk, numbers numerically.
function naturalCompare(a, b) {
  const ax = String(a == null ? "" : a).match(/(\d+|\D+)/g) || [];
  const bx = String(b == null ? "" : b).match(/(\d+|\D+)/g) || [];
  for (let i = 0; i < Math.max(ax.length, bx.length); i++) {
    const an = ax[i], bn = bx[i];
    if (an === undefined) return -1;
    if (bn === undefined) return 1;
    const aNum = /^\d+$/.test(an), bNum = /^\d+$/.test(bn);
    if (aNum && bNum) { const d = parseInt(an, 10) - parseInt(bn, 10); if (d !== 0) return d; }
    else { const c = an.localeCompare(bn); if (c !== 0) return c; }
  }
  return 0;
}
function fmtWeight(grams, unit) {
  if (unit === "lb") return (grams / G_PER_LB).toFixed(2) + " lb";
  return Math.round(grams).toLocaleString() + " g";
}
function fmtBoth(grams) {
  return `${(grams / G_PER_LB).toFixed(2)} lb (${Math.round(grams).toLocaleString()} g)`;
}
function toGrams(amount, unit) {
  const n = parseFloat(amount);
  if (isNaN(n)) return 0;
  return unit === "lb" ? n * G_PER_LB : n;
}
function fmtMoney(n) {
  return "$" + (n || 0).toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 });
}

// --- Zebra label printing (3.5in x 1in, 203dpi = 711 x 203 dots) ---
// ZPL text fields can't safely contain ^ or ~ (those are ZPL command characters),
// and the default printer codepage doesn't reliably support special typographic
// characters, so this keeps everything to plain ASCII.
function zplSafe(s) {
  return String(s == null ? "" : s).replace(/[\^~]/g, "").replace(/[^\x20-\x7E]/g, "");
}
function buildBatchLabelZPL(batch, shipper, unit) {
  const strain = zplSafe(batch.strain);
  const grade = zplSafe(batch.grade);
  const lot = zplSafe(batch.lot || "-");
  const sourceLabel = zplSafe(SOURCE_META[batch.source] ? SOURCE_META[batch.source].label : "");

  // A batch can carry more than one Metrc package tag across its deliveries — pull from every
  // addition (not just the batch's top-level field, which only ever mirrors the first one) so a
  // reprint days later never shows a stale or incomplete tag.
  const tags = Array.from(new Set(
    (batch.additions && batch.additions.length > 0 ? batch.additions.map((a) => a.packageTag) : [batch.packageTag]).filter(Boolean)
  ));
  const tagText = tags.length === 0 ? "" : tags.length === 1 ? `Tag ...${tags[0]}` : `Tags ...${tags.slice(0, 2).join(", ...")}${tags.length > 2 ? ` +${tags.length - 2}` : ""}`;

  // Label: 3.5" x 1" at 203 dpi = 711 x 203 dots. Text occupies the left ~535 dots; a QR code
  // sits on the right. QR encodes the full tag(s) + batch identity so a scan pulls real data,
  // even though the printed tag line is abbreviated to the last 4 for the human reader.
  const qrData = zplSafe(`${batch.strain} | ${sourceLabel} | Batch ${lot} | ${tags.length ? "Tag " + tags.join(", ") : "no tag"}`);

  return [
    "^XA",
    "^PW711",
    "^LL203",
    "^CI28",              // UTF-8, so strain names with accents print correctly
    // --- text block (left side) ---
    "^CF0,40",
    `^FO16,14^FB520,1,0,L^FD${strain} - ${grade}^FS`,
    "^CF0,30",
    `^FO16,66^FB520,1,0,L^FD${sourceLabel} / Batch ${lot}^FS`,
    "^CF0,28",
    tagText ? `^FO16,112^FB520,1,0,L^FD${zplSafe(tagText)}^FS` : "",
    shipperName(shipper) ? "^CF0,20" : "",
    shipperName(shipper) ? `^FO16,158^FB520,1,0,L^FD${zplSafe(shipperName(shipper))}^FS` : "",
    // --- QR code (right side) ---
    `^FO556,40^BQN,2,5^FDLA,${qrData}^FS`,
    "^XZ",
  ].filter(Boolean).join("\n");
}
function shipperName(shipper) { return shipper && shipper.name ? shipper.name : ""; }
function printZebraLabel(zpl) {
  return new Promise((resolve, reject) => {
    if (!window.BrowserPrint) {
      reject(new Error("Zebra Browser Print isn't detected on this computer. Make sure it's installed and running, then reload this page."));
      return;
    }
    window.BrowserPrint.getDefaultDevice(
      "printer",
      (device) => {
        if (!device) { reject(new Error("No Zebra printer found. Check that it's connected and powered on.")); return; }
        device.send(zpl, () => resolve(), (err) => reject(new Error(String(err) || "The printer didn't accept the label.")));
      },
      (err) => reject(new Error(String(err) || "Couldn't reach Zebra Browser Print.")));
  });
}

export const IS_DEMO = typeof window !== "undefined" && window.location.hostname.startsWith("demo.");
const SOURCE_META = IS_DEMO
  ? {
      daddyspipes: { label: "Green Valley Cultivation", color: "#7C9473", bg: "rgba(124,148,115,0.14)" },
      merc: { label: "Green Valley Distribution", color: "#C9A24B", bg: "rgba(201,162,75,0.14)" },
    }
  : {
      daddyspipes: { label: "Daddy's Pipes", color: "#7C9473", bg: "rgba(124,148,115,0.14)" },
      merc: { label: "MERC", color: "#C9A24B", bg: "rgba(201,162,75,0.14)" },
    };
const DEFAULT_ROOMS = {
  daddyspipes: ["Room 1", "Room 2", "Room 3", "Room 4", "Show Room"],
  merc: Array.from({ length: 15 }, (_, i) => `F${i + 1}`),
};

// Vaults — named custody checkpoints a batch moves THROUGH, from the MERC/Lemonnade SOP. Distinct
// from rooms (physical grow/trim spaces): a vault is "where in the process" custody currently sits.
// Order matters — it's the normal forward flow. Phase 1 tracks which vault a batch/lot is in and
// logs every move; the money layer (invoices, settlement) attaches to these later.
const VAULTS = ["processing", "trim_room", "sales", "lemonnade_sales"];
const VAULT_META = {
  processing:      { label: "Processing Vault", short: "Processing", color: "#7C9473" },
  trim_room:       { label: "Trim Room",         short: "Trim Room",  color: "#C08FD8" },
  sales:           { label: "Sales Vault",       short: "Sales",      color: "#C9A24B" },
  lemonnade_sales: { label: "Lemonnade Sales Vault", short: "Lemonnade", color: "#4A90D9" },
};
function vaultLabel(v) { return VAULT_META[v] ? VAULT_META[v].label : (v || "—"); }

// Origin dot — a quick visual marker of where product was grown, since Daddy's facility physically
// holds MERC-grown product too. Blue = grown at Daddy's, Yellow = grown at MERC (processed/kept here).
const ORIGIN_DOT_COLOR = { daddyspipes: "#4A90D9", merc: "#E8C24B" };
function OriginDot({ source, origin, size = 9, style }) {
  const key = origin || source; // prefer explicit origin (who grew it); fall back to legacy source
  const color = ORIGIN_DOT_COLOR[key];
  if (!color) return null;
  const label = SOURCE_META[key] ? `Grown at ${SOURCE_META[key].label}` : "";
  return <span title={label} style={{ display: "inline-block", width: size, height: size, borderRadius: "50%", background: color, flexShrink: 0, verticalAlign: "middle", ...style }} />;
}

// Loads the html5-qrcode scanning library from a CDN, once, only when first needed. We switched to
// this from ZXing because ZXing's JS build struggles with dense Code 128 barcodes like Metrc tags —
// html5-qrcode uses a more robust decoding core that reads them reliably on phones. Tries multiple
// CDN sources so one hiccup doesn't kill scanning.
let _scanLibPromise = null;
function loadScript(src) {
  return new Promise((resolve, reject) => {
    const s = document.createElement("script");
    s.src = src; s.async = true;
    s.onload = () => resolve();
    s.onerror = () => reject(new Error("load failed: " + src));
    document.head.appendChild(s);
  });
}
function loadScanLib() {
  if (_scanLibPromise) return _scanLibPromise;
  const sources = [
    "https://cdn.jsdelivr.net/npm/html5-qrcode@2.3.8/html5-qrcode.min.js",
    "https://unpkg.com/html5-qrcode@2.3.8/html5-qrcode.min.js",
    "https://cdn.jsdelivr.net/npm/html5-qrcode/html5-qrcode.min.js",
  ];
  _scanLibPromise = (async () => {
    if (window.Html5Qrcode) return window.Html5Qrcode;
    let lastErr = null;
    for (const src of sources) {
      try {
        await loadScript(src);
        if (window.Html5Qrcode) return window.Html5Qrcode;
      } catch (e) { lastErr = e; }
    }
    throw lastErr || new Error("Couldn't load the barcode scanner from any source.");
  })();
  return _scanLibPromise;
}

function shortTag(tag) {
  const t = normalizeTag(tag);
  return t.length > 8 ? `…${t.slice(-6)}` : t;
}

// Camera-based Metrc tag scanner. Opens a focused overlay, reads Code 128 barcodes with a narrow
// reticle (so it grabs one tag, not the neighbor on a stacked roll), auto-fires on a valid read with
// beep + vibrate, and always offers manual entry as a first-class fallback. Calls onScan(tag).
function TagScanner({ open, onScan, onClose, title = "Scan Tag" }) {
  const [status, setStatus] = useState("starting"); // starting | scanning | error | manual
  const [errorMsg, setErrorMsg] = useState("");
  const [manualValue, setManualValue] = useState("");
  const lastScanRef = React.useRef({ tag: "", at: 0 });

  function feedback() {
    try { if (navigator.vibrate) navigator.vibrate(60); } catch {}
    try {
      const Ctx = window.AudioContext || window.webkitAudioContext;
      if (Ctx) {
        const ctx = new Ctx();
        const osc = ctx.createOscillator();
        const gain = ctx.createGain();
        osc.frequency.value = 880; gain.gain.value = 0.08;
        osc.connect(gain); gain.connect(ctx.destination);
        osc.start(); osc.stop(ctx.currentTime + 0.09);
      }
    } catch {}
  }

  function handleHit(rawText) {
    const tag = normalizeTag(rawText);
    const now = Date.now();
    // Debounce: ignore the same tag re-read within 2.5s so one scan doesn't fire repeatedly.
    if (tag === lastScanRef.current.tag && now - lastScanRef.current.at < 2500) return;
    if (!looksLikeMetrcTag(tag)) return; // ignore stray reads that aren't tag-shaped
    lastScanRef.current = { tag, at: now };
    feedback();
    onScan(tag);
  }

  const scannerRef = React.useRef(null);
  const scanRegionId = "stem-scan-region";
  const [capturing, setCapturing] = useState(false);
  const [captureMsg, setCaptureMsg] = useState("");

  // Capture-and-decode: grab one frame from the live video, run heavy image processing on it
  // (grayscale + hard contrast threshold to erase the blue label background, plus upscale to fatten
  // thin bars), then decode from that still image with no time pressure. This is a genuinely
  // different attack than live scanning — it can succeed on dense/low-contrast Metrc tags that the
  // real-time decoder (and even iOS Camera's live reader) gives up on.
  async function captureAndDecode() {
    const region = document.getElementById(scanRegionId);
    const video = region && region.querySelector("video");
    if (!video || !video.videoWidth) { setCaptureMsg("Camera not ready yet — give it a second."); return; }
    setCapturing(true); setCaptureMsg("Processing…");
    try {
      const Html5Qrcode = await loadScanLib();
      // Draw the current frame, upscaled 2x, to a canvas.
      const scale = 2;
      const cw = video.videoWidth * scale, ch = video.videoHeight * scale;
      const canvas = document.createElement("canvas");
      canvas.width = cw; canvas.height = ch;
      const ctx = canvas.getContext("2d");
      ctx.imageSmoothingEnabled = true;
      ctx.drawImage(video, 0, 0, cw, ch);
      // Grayscale + adaptive-ish contrast: push everything toward pure black or white so the barcode
      // bars stand out from the blue label. Threshold chosen to survive the blue tint + glare.
      const img = ctx.getImageData(0, 0, cw, ch);
      const d = img.data;
      for (let i = 0; i < d.length; i += 4) {
        const g = 0.299 * d[i] + 0.587 * d[i + 1] + 0.114 * d[i + 2];
        // stretch contrast around mid then hard-clip
        const v = g < 128 ? Math.max(0, (g - 40) * 2.2) : Math.min(255, 255 - (215 - g) * 2.2);
        const out = v < 110 ? 0 : 255;
        d[i] = d[i + 1] = d[i + 2] = out;
      }
      ctx.putImageData(img, 0, 0);
      // html5-qrcode decodes from a File; convert the canvas to a blob → File.
      const blob = await new Promise((res) => canvas.toBlob(res, "image/png"));
      const file = new File([blob], "capture.png", { type: "image/png" });
      const tmpId = "stem-capture-decode";
      let holder = document.getElementById(tmpId);
      if (!holder) { holder = document.createElement("div"); holder.id = tmpId; holder.style.display = "none"; document.body.appendChild(holder); }
      const still = new Html5Qrcode(tmpId, { verbose: false });
      try {
        const text = await still.scanFile(file, false);
        setCaptureMsg("");
        handleHit(text);
      } catch (e) {
        setCaptureMsg("Couldn't read it from the photo either — try again with the barcode filling the box, or type it in.");
      } finally {
        try { still.clear(); } catch {}
      }
    } catch (e) {
      setCaptureMsg("Something went wrong processing the image — you can type the tag instead.");
    } finally {
      setCapturing(false);
    }
  }

  useEffect(() => {
    if (!open) return;
    let cancelled = false;
    setStatus("starting"); setErrorMsg("");
    (async () => {
      try {
        const Html5Qrcode = await loadScanLib();
        if (cancelled) return;

        // Support the barcode formats Metrc uses. html5-qrcode's formatsToSupport takes its own enum.
        const F = window.Html5QrcodeSupportedFormats || {};
        const formats = [F.CODE_128, F.CODE_39, F.CODE_93, F.DATA_MATRIX, F.QR_CODE, F.PDF_417].filter((x) => x !== undefined);

        const scanner = new Html5Qrcode(scanRegionId, {
          formatsToSupport: formats.length ? formats : undefined,
          verbose: false,
        });
        scannerRef.current = scanner;

        const config = {
          fps: 12,
          // A wide, short scan box matches a linear barcode and helps the decoder focus on one tag.
          qrbox: (vw, vh) => {
            const w = Math.min(vw * 0.86, 460);
            return { width: w, height: Math.max(120, Math.min(vh * 0.32, 200)) };
          },
          aspectRatio: 1.777,
          // Use the browser's native BarcodeDetector when available (very strong on iOS 17+/Android) —
          // falls back to html5-qrcode's own decoder otherwise.
          experimentalFeatures: { useBarCodeDetectorIfSupported: true },
          videoConstraints: {
            facingMode: { ideal: "environment" },
            width: { ideal: 1920 },
            height: { ideal: 1080 },
            focusMode: "continuous",
            advanced: [{ focusMode: "continuous" }],
          },
        };

        await scanner.start(
          { facingMode: "environment" },
          config,
          (decodedText) => handleHit(decodedText),
          () => { /* per-frame no-read; ignore */ }
        );
        if (cancelled) { try { await scanner.stop(); } catch {} return; }
        setStatus("scanning");
      } catch (e) {
        if (cancelled) return;
        setStatus("error");
        const name = e && (e.name || "");
        const msg = /NotAllowed|Permission/i.test(name + (e && e.message || ""))
          ? "Camera permission was blocked. Allow camera access in your browser settings, then reopen."
          : (e && e.message && /load/i.test(e.message))
            ? "Couldn't load the barcode scanner. You can still type the tag below."
            : "Couldn't start the camera. You can still type the tag below.";
        setErrorMsg(msg);
      }
    })();
    return () => {
      cancelled = true;
      const s = scannerRef.current;
      scannerRef.current = null;
      if (s) { try { s.stop().then(() => { try { s.clear(); } catch {} }).catch(() => {}); } catch {} }
    };
  }, [open]);

  function submitManual() {
    const tag = normalizeTag(manualValue);
    if (!looksLikeMetrcTag(tag)) { setErrorMsg("That doesn't look like a full Metrc tag — check the number under the barcode."); return; }
    setErrorMsg("");
    setManualValue("");
    onScan(tag);
  }

  if (!open) return null;

  return (
    <div style={styles.scanOverlay}>
      <div style={styles.scanHeader}>
        <span style={{ display: "flex", alignItems: "center", gap: 8 }}><ScanLine size={18} color="#C9A24B" /> {title}</span>
        <button style={styles.scanClose} onClick={onClose}><X size={20} /></button>
      </div>

      {status !== "manual" && (
        <div style={styles.scanStage}>
          <div id={scanRegionId} style={styles.scanVideo} />
          <div style={styles.scanReticle}>
            <div style={styles.scanReticleLine} />
          </div>
          <div style={styles.scanHint}>
            {status === "starting" && "Starting camera…"}
            {status === "scanning" && "Fill the box with the barcode, then tap Capture & Read"}
            {status === "error" && "Camera unavailable"}
          </div>
        </div>
      )}

      {status === "error" && (
        <div style={{ ...styles.noticeBox, margin: "12px 16px" }}>{errorMsg} You can still type the tag below.</div>
      )}

      <div style={styles.scanControls}>
        {status !== "manual" ? (
          <>
            {captureMsg && <div style={{ fontSize: 12.5, color: "#C9A24B", textAlign: "center", padding: "0 16px 10px" }}>{captureMsg}</div>}
            <button style={{ ...styles.submitBtn, margin: "0 16px 10px", width: "calc(100% - 32px)", fontSize: 16, padding: 14, opacity: capturing ? 0.6 : 1 }} onClick={captureAndDecode} disabled={capturing}>
              {capturing ? <Loader2 size={18} className="animate-spin" /> : <Camera size={18} />} {capturing ? "Reading…" : "Capture & Read"}
            </button>
            <button style={styles.scanManualToggle} onClick={() => setStatus("manual")}><Keyboard size={16} /> Type it instead</button>
          </>
        ) : (
          <div style={{ padding: "0 16px" }}>
            <FieldLabel>Enter the tag (the number under the barcode)</FieldLabel>
            <input
              style={{ ...styles.input, fontFamily: "monospace", letterSpacing: "0.03em" }}
              value={manualValue}
              onChange={(e) => setManualValue(e.target.value.toUpperCase())}
              placeholder="1A40602…"
              autoFocus
            />
            {errorMsg && <div style={styles.errorText}>{errorMsg}</div>}
            <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
              <button style={styles.submitBtn} onClick={submitManual}><Check size={16} /> Use Tag</button>
              <button style={styles.backBtn} onClick={() => { setStatus("scanning"); setErrorMsg(""); }}>Back to camera</button>
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
const GRADE_META = {
  A: { label: "A - Big", short: "A - Big", color: "#C9A24B" },
  B: { label: "B - Medium", short: "B - Medium", color: "#8FAF8B" },
  C: { label: "C - Small", short: "C - Small", color: "#B98F5E" },
  D: { label: "D", short: "D", color: "#6B7264" },
  T: { label: "TRIM", short: "TRIM", color: "#C08FD8" },
};
const GRADE_ORDER = ["A", "B", "C", "D", "T"];
const ENTRY_TYPE_META = {
  in: { label: "Bulk In", icon: ArrowDownCircle, color: "#7C9473", bg: "rgba(124,148,115,0.16)" },
  out: { label: "Shipped Out", icon: ArrowUpCircle, color: "#C9A24B", bg: "rgba(201,162,75,0.16)" },
  bucked_in: { label: "Bucked Flower", icon: Leaf, color: "#7C9473", bg: "rgba(124,148,115,0.16)" },
  harvest_in: { label: "Harvest", icon: Leaf, color: "#8FAF8B", bg: "rgba(143,175,139,0.16)" },
  trim: { label: "Trim", icon: Scissors, color: "#C08FD8", bg: "rgba(192,143,216,0.16)" },
  grade: { label: "Grade Entry", icon: ClipboardCheck, color: "#C08FD8", bg: "rgba(192,143,216,0.16)" },
  waste: { label: "Waste", icon: AlertTriangle, color: "#C9A24B", bg: "rgba(201,162,75,0.16)" },
};
const ENTRY_TYPE_ORDER = ["in", "out", "bucked_in", "harvest_in", "trim", "grade", "waste"];
const ENTRY_TYPE_POSITIVE = { in: true, out: false, bucked_in: true, harvest_in: true, trim: false, grade: true, waste: false };
const DEFAULT_GRADE_DESCRIPTIONS = {
  A: "Bigs — premium, top-shelf flower",
  B: "Mediums — solid quality, mid-size buds",
  C: "Smalls — smaller buds, still sellable flower",
  D: "Micro — very small buds",
  T: "Trim / shake — leaf and small material from trimming",
};
function allGradeCodes(customGrades) { return [...GRADE_ORDER, ...(customGrades || []).map((g) => g.code)]; }
function gradeMetaFor(code, customGrades) {
  if (GRADE_META[code]) return GRADE_META[code];
  const cg = (customGrades || []).find((g) => g.code === code);
  return cg ? { label: cg.code, short: cg.code, color: cg.color || "#8FAF8B" } : { label: code, short: code, color: "#8FAF8B" };
}
function gradeDescriptionFor(code, gradeDescriptions, customGrades) {
  if (gradeDescriptions && gradeDescriptions[code]) return gradeDescriptions[code];
  const cg = (customGrades || []).find((g) => g.code === code);
  if (cg) return cg.description || "";
  return DEFAULT_GRADE_DESCRIPTIONS[code] || "";
}
const PERMISSION_DEFS = [
  { key: "receive", label: "Log incoming inventory & movements" },
  { key: "delete", label: "Delete inventory & order entries" },
  { key: "reports", label: "View & print reports" },
  { key: "catalog", label: "Manage rooms & strain list" },
];
function defaultPermissions() { return { receive: false, delete: false, reports: false, catalog: false }; }
function getEffectivePrice(batch, priceList, priceOverrides) {
  const byBatch = priceOverrides?.byBatch?.[batch.id];
  if (byBatch) return parseFloat(byBatch) || 0;
  const byStrainGrade = priceList?.byStrain?.[batch.strain]?.[batch.grade];
  if (byStrainGrade) return parseFloat(byStrainGrade) || 0;
  return (priceList?.standard?.[batch.source]?.[batch.grade]) || 0;
}
function getEffectivePhoto(batch, photos) {
  if (!photos) return null;
  return photos.byBatch?.[batch.id] || photos.byStrain?.[batch.strain] || photos.bySource?.[batch.source] || null;
}

// Adds origin + location to every batch/lot that predates the two-field split. Idempotent: once a
// record has both fields it's skipped, so this is safe to run on every load. Location for existing
// records is always daddyspipes, because all product entered before this change physically lives at
// Daddy's (MERC-grown product had already been sent down and received here).
function migrateOriginLocation(data) {
  const fix = (rec) => {
    if (!rec) return;
    if (rec.origin && rec.location) return; // already migrated
    const src = rec.source || "daddyspipes";
    if (!rec.origin) rec.origin = src;          // who grew it = whatever source said
    if (!rec.location) rec.location = "daddyspipes"; // everything so far is physically at Daddy's
  };
  (data.batches || []).forEach(fix);
  (data.buckedLots || []).forEach(fix);
  return data;
}

// --- Cultivation record shapes (Phase 1 foundation) ---
// Stages a plant/batch moves through, in order. "landed" is the shop's word for immature-in-veg.
const CULTIVATION_STAGES = ["propagation", "veg", "flower", "harvested"];
const STAGE_META = {
  propagation: { label: "Propagation", color: "#8FAF8B" },
  veg:         { label: "Veg (Landed)", color: "#7C9473" },
  flower:      { label: "Flowering", color: "#C08FD8" },
  harvested:   { label: "Harvested", color: "#C9A24B" },
};

// A mother plant — individually tagged, durable, lives in veg. Clones are cut from it over time.
function makeMother({ tag, strain, room, facility, staff }) {
  return {
    id: uid(), tag: (tag || "").trim(), strain, room, facility: facility || "daddyspipes",
    status: "active",           // "active" | "retired"
    createdAt: Date.now(), staff,
    cloneCuts: [],              // [{ id, date, count, cutFor, staff, createdAt }]
  };
}

// A plant batch — a tagged GROUP of immature plants (clones). Multiple plants share one tag until
// they reach flowering and get individual tags. `count` is the live number still in the batch.
function makePlantBatch({ tag, strain, room, facility, stage, count, motherId, cutFor, staff }) {
  return {
    id: uid(), tag: (tag || "").trim(), strain, room, facility: facility || "daddyspipes",
    stage: stage || "propagation",
    count: count || 0,          // current live count
    startCount: count || 0,     // count when created (for survival tracking)
    motherId: motherId || null, // which mother these were cut from
    cutFor: cutFor || "",       // what they were cut for
    createdAt: Date.now(), staff,
    archived: false,            // set true once fully moved to flower / harvested / destroyed
  };
}

// An individual plant — gets its own tag at flowering. May trace back to the batch it came from.
function makePlant({ tag, strain, room, facility, batchId, staff }) {
  return {
    id: uid(), tag: (tag || "").trim(), strain, room, facility: facility || "daddyspipes",
    stage: "flower",
    batchId: batchId || null,   // the plantBatch it was promoted from
    createdAt: Date.now(), staff,
    status: "growing",          // "growing" | "harvested" | "destroyed"
    harvestId: null,            // set when harvested
  };
}

// A cultivation harvest — created when flowering plants are cut. Gets a batch number, per-plant wet
// weights, and a dry room. Once dried, it hands off to the existing Harvest intake (buckedLots).
function makeHarvest({ batchNumber, strain, dryRoom, facility, staff }) {
  return {
    id: uid(), batchNumber: (batchNumber || "").trim(), strain, dryRoom, facility: facility || "daddyspipes",
    createdAt: Date.now(), staff,
    status: "drying",           // "drying" | "handed_off"
    plantWeights: [],           // [{ plantTag, grams, staff, createdAt }]
    totalWetGrams: 0,
    handedOffLotId: null,       // links to the buckedLot once brought into the existing flow
  };
}

// One entry in the append-only cultivation audit log. Every scan-driven action writes one of these.
function makeCultivationEvent({ type, facility, staff, refTag, detail }) {
  return {
    id: uid(), type,            // "clone_cut" | "stage_move" | "destroy" | "harvest" | "mother_new" | ...
    facility: facility || "daddyspipes",
    refTag: refTag || "",       // the tag that was scanned
    detail: detail || {},       // type-specific payload (counts, weights, from/to rooms, etc.)
    date: todayISO(), staff, createdAt: Date.now(),
  };
}

// --- Phase 1: Vaults & BIN# (MERC/Lemonnade SOP) ---
// A BIN is a tared intake container tied to a METRC UID, created on the fly when bucked flower is
// received. Net product weight = gross weighed - tare. The BIN carries the batch through the vaults
// and is what trim yield later reconciles back against (SOP A3, T5).
function makeBin({ binNumber, metrcUid, tareGrams, grossGrams, strain, facility, staff, vault, manifestNumber }) {
  const tare = Number(tareGrams) || 0;
  const gross = Number(grossGrams) || 0;
  return {
    id: uid(),
    binNumber: (binNumber || "").trim(),      // internal BIN # (created on the fly)
    metrcUid: (metrcUid || "").trim(),         // the METRC UID/tag from the manifest
    manifestNumber: (manifestNumber || "").trim(),
    strain: strain || "",
    facility: facility || "daddyspipes",
    tareGrams: tare,                           // empty bin weight
    grossGrams: gross,                         // bin + product as weighed at intake
    netGrams: Math.max(0, gross - tare),       // product weight logged against tare
    vault: vault || "processing",              // current custody checkpoint
    createdAt: Date.now(), staff,
    status: "open",                            // "open" | "closed" (once fully processed out)
    transfers: [],                             // [{ from, to, grams, date, staff, createdAt }]
  };
}

// One vault-to-vault custody move for a BIN (SOP A5, T7, T8). Appended to the BIN's transfers[].
function makeVaultTransfer({ from, to, grams, staff, notes }) {
  return {
    id: uid(), from, to, grams: Number(grams) || 0,
    notes: notes || "", date: todayISO(), staff, createdAt: Date.now(),
  };
}

// Normalizes a scanned or typed Metrc tag: strips whitespace, uppercases. Lenient on length/format
// on purpose — a torn label or slightly-off scan should still match, and Metrc validates for real
// once the API is wired. We only require it look tag-ish (alphanumeric, reasonably long).
function normalizeTag(raw) {
  return (raw || "").replace(/\s+/g, "").toUpperCase();
}
function looksLikeMetrcTag(raw) {
  const t = normalizeTag(raw);
  return /^[0-9A-Z]{10,}$/.test(t); // at least 10 alphanumerics; CA tags are 24 but we don't hard-require it
}

// The core of the scan engine: given a scanned tag, find what it points to across cultivation data.
// Returns { kind, record } where kind is "mother" | "plantBatch" | "plant" | null.
function resolveTag(rawTag, data) {
  const tag = normalizeTag(rawTag);
  if (!tag) return { kind: null, record: null };
  const mother = (data.mothers || []).find((m) => normalizeTag(m.tag) === tag);
  if (mother) return { kind: "mother", record: mother };
  const batch = (data.plantBatches || []).find((b) => normalizeTag(b.tag) === tag);
  if (batch) return { kind: "plantBatch", record: batch };
  const plant = (data.plants || []).find((p) => normalizeTag(p.tag) === tag);
  if (plant) return { kind: "plant", record: plant };
  return { kind: null, record: null };
}

function emptyAppData() {
  return {
    batches: [], shipments: [], orders: [], trimLogs: [], customers: [],
    buckedLots: [], trimmers: [], trimEntries: [], gradeEntries: [], wasteEntries: [],
    rooms: { daddyspipes: [...DEFAULT_ROOMS.daddyspipes], merc: [...DEFAULT_ROOMS.merc] },
    strains: [], distributionEmail: "",
    shipper: { name: "Daddy's Pipes, Inc.", address: "7040 Hayvenhurst Ave. Van Nuys CA 91406", license: "C12-0000092-LIC", contact: "" },
    paymentInfo: { instructions: "", terms: "" },
    licenses: { daddyspipes: "", merc: "" },
    cashOnHand: { daddyspipes: 0, merc: 0 },
    priceList: {
      standard: {
        daddyspipes: { A: 0, B: 0, C: 0, D: 0, T: 0 },
        merc: { A: 0, B: 0, C: 0, D: 0, T: 0 },
      },
      byStrain: {},
    },
    photos: { bySource: { daddyspipes: "", merc: "" }, byStrain: {}, byBatch: {} },
    priceOverrides: { byBatch: {} },
    menuPassword: "",
    nextOrderNumber: null,
    reconciliations: [],
    gradeDescriptions: {},
    customGrades: [],
    // --- Cultivation (Phase 1: data model foundation) ---
    // Mirrors Metrc's structure so a future API sync is a connection, not a rebuild.
    // mothers: individually-tagged source plants clones are cut from (repeatedly, over time).
    // plantBatches: tagged GROUPS of immature plants (clones in propagation/veg), carry a live count.
    // plants: individually-tagged plants once they reach flowering.
    // harvests: a cultivation harvest — batch #, per-plant wet weights, assigned dry room. Hands off
    //   to the existing Harvest intake once dried.
    // cultivationEvents: append-only audit log of every scan-driven action (cut, move, destroy, harvest).
    mothers: [],
    plantBatches: [],
    plants: [],
    harvests: [],
    cultivationEvents: [],
    bins: [],
    backups: {},
  };
}
const BACKUP_FIELDS = ["batches", "shipments", "orders", "trimLogs", "customers", "buckedLots", "trimmers", "trimEntries", "gradeEntries", "wasteEntries", "rooms", "strains", "priceList", "priceOverrides", "shipper", "paymentInfo", "licenses", "cashOnHand", "gradeDescriptions", "customGrades", "reconciliations", "mothers", "plantBatches", "plants", "harvests", "cultivationEvents", "bins"];
function snapshotForBackup(data) {
  const snap = {};
  BACKUP_FIELDS.forEach((k) => { snap[k] = data[k]; });
  return JSON.parse(JSON.stringify(snap));
}

// Resizes + compresses an uploaded image client-side before storing it as a data URI,
// so photo menus stay small enough to save without hitting storage limits.
function compressImage(file, maxWidth = 480, quality = 0.72) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = (e) => {
      const img = new window.Image();
      img.onload = () => {
        const scale = Math.min(1, maxWidth / img.width);
        const canvas = document.createElement("canvas");
        canvas.width = Math.round(img.width * scale);
        canvas.height = Math.round(img.height * scale);
        const ctx = canvas.getContext("2d");
        ctx.drawImage(img, 0, 0, canvas.width, canvas.height);
        resolve(canvas.toDataURL("image/jpeg", quality));
      };
      img.onerror = reject;
      img.src = e.target.result;
    };
    reader.onerror = reject;
    reader.readAsDataURL(file);
  });
}

// --- print helper: opens a real new window, formatted for 8.5x11 printing ---
function buildPrintDoc(title, bodyHTML) {
  return `<!DOCTYPE html><html><head><meta charset="utf-8"><title>${title}</title><style>
    @page { size: 8.5in 11in; margin: 0.5in; }
    body { font-family: Georgia, 'Times New Roman', serif; color: #1A1A14; background: #fff; margin: 0; padding: 0.4in; width: 7.7in; }
    h1 { font-size: 22px; letter-spacing: 0.03em; margin-bottom: 2px; }
    .sub { color: #555; font-size: 12px; margin-bottom: 18px; }
    table { width: 100%; border-collapse: collapse; margin-top: 10px; }
    th { text-align: left; font-size: 11px; text-transform: uppercase; letter-spacing: 0.04em; color: #666; border-bottom: 1px solid #999; padding: 6px 8px; }
    td { font-size: 13px; padding: 8px; border-bottom: 1px solid #ddd; }
    .meta { text-align: right; font-size: 12.5px; line-height: 1.6; }
    .total { display:flex; justify-content: flex-end; gap: 20px; margin-top: 16px; font-size: 15px; padding-top: 12px; border-top: 1px solid #999; }
    .headrow { display:flex; justify-content: space-between; align-items: flex-start; }
    .shipper-top { margin-bottom: 4px; }
    .shipper-top .biz { font-weight: 700; font-size: 15px; }
    .shipper-top div { font-size: 12px; color: #333; line-height: 1.5; }
    .billto { display:flex; justify-content: space-between; margin: 14px 0 6px; padding-top: 10px; border-top: 1px solid #ccc; }
    .billto .label { font-size: 10px; letter-spacing: 0.06em; color: #888; text-transform: uppercase; }
    .payment-block { font-size: 12px; color: #333; line-height: 1.6; border-top: 1px solid #ccc; margin-top: 16px; padding-top: 10px; }
    .paid-badge { display:inline-block; font-size: 11px; font-weight: 700; padding: 3px 10px; border-radius: 4px; letter-spacing: 0.04em; }
    .menu-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; margin-top: 14px; }
    .menu-card { border: 1px solid #ccc; border-radius: 8px; overflow: hidden; break-inside: avoid; }
    .menu-card .photo { width: 100%; height: 130px; object-fit: cover; background: #eee; display: block; }
    .menu-card .info { padding: 8px 10px; }
    .menu-card .name { font-weight: 700; font-size: 13px; }
    .menu-card .price { font-size: 12px; color: #444; margin-top: 3px; }
  </style></head><body>${bodyHTML}</body></html>`;
}

// Print via a hidden same-page iframe rather than a popup window — this avoids
// popup blockers, which is what stops window.open()-based printing in most browsers.
function printHTML(title, bodyHTML) {
  const html = buildPrintDoc(title, bodyHTML);
  const iframe = document.createElement("iframe");
  iframe.style.position = "fixed";
  iframe.style.right = "0";
  iframe.style.bottom = "0";
  iframe.style.width = "0";
  iframe.style.height = "0";
  iframe.style.border = "0";
  iframe.setAttribute("aria-hidden", "true");
  document.body.appendChild(iframe);
  const cleanup = () => { if (iframe.parentNode) iframe.parentNode.removeChild(iframe); };
  iframe.onload = () => {
    try { iframe.contentWindow.focus(); iframe.contentWindow.print(); } catch (e) {}
    setTimeout(cleanup, 1500);
  };
  const doc = iframe.contentDocument || iframe.contentWindow.document;
  doc.open();
  doc.write(html);
  doc.close();
  // Fallback in case onload never fires
  setTimeout(() => { try { iframe.contentWindow.focus(); iframe.contentWindow.print(); } catch (e) {} }, 500);
  setTimeout(cleanup, 3000);
}

// Guaranteed-to-work fallback: downloads a standalone HTML file. Open it in any
// browser and print/save-as-PDF from there — no popups or iframes involved.
function downloadHTML(filename, title, bodyHTML) {
  const html = buildPrintDoc(title, bodyHTML);
  const blob = new Blob([html], { type: "text/html" });
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = filename.endsWith(".html") ? filename : `${filename}.html`;
  document.body.appendChild(a);
  a.click();
  document.body.removeChild(a);
  setTimeout(() => URL.revokeObjectURL(url), 2000);
}


export default function App() {
  const [session, setSession] = useState(undefined); // undefined = checking, null = signed out
  const [profile, setProfile] = useState(null);
  const [data, setData] = useState(null);
  const [loadingData, setLoadingData] = useState(false);
  const [loadError, setLoadError] = useState(false);
  const [saving, setSaving] = useState(false);
  const [saveError, setSaveError] = useState(null);
  const [lastSyncedAt, setLastSyncedAt] = useState(null);
  const [tab, setTab] = useState("dashboard");
  const [intakeMode, setIntakeMode] = useState("graded");
  const [unit, setUnit] = useState("lb");
  const [invoiceOrderId, setInvoiceOrderId] = useState(null);

  useEffect(() => {
    supabase.auth.getSession().then(({ data: { session } }) => setSession(session));
    const { data: sub } = supabase.auth.onAuthStateChange((_event, newSession) => {
      // Supabase silently refreshes the token whenever the tab regains focus — that used to
      // be treated as a fresh sign-in, which reloaded all data and wiped out anything you
      // were typing. Only actually update state when the signed-in user really changed.
      setSession((prev) => (prev?.user?.id === newSession?.user?.id ? prev : newSession));
    });
    return () => sub.subscription.unsubscribe();
  }, []);

  useEffect(() => {
    if (!session) { setProfile(null); return; }
    (async () => {
      const { data: row } = await supabase.from("profiles").select("*").eq("id", session.user.id).single();
      setProfile(row);
    })();
  }, [session]);

  useEffect(() => {
    if (!session) return;
    setLoadingData(true);
    setLoadError(false);
    (async () => {
      const { data: row, error } = await supabase.from("app_state").select("data, updated_at").eq("id", 1).single();
      if (error) {
        // A failed fetch must NEVER be treated as "no data yet" — doing that previously caused
        // real data to be silently overwritten with a blank state. Stop here instead and let the
        // person retry, rather than touching the database with anything.
        console.error("Failed to load app data:", error);
        setLoadError(true);
        setLoadingData(false);
        return;
      }
      const loaded = row ? { ...emptyAppData(), ...row.data } : emptyAppData();
      if (!loaded.shipper || !loaded.shipper.name) loaded.shipper = emptyAppData().shipper;
      // One-time origin/location split. Historically each batch/lot carried a single `source` that
      // meant both "who grew it" and "where it is". We now track those separately:
      //   origin   = who grew it (drives the blue/yellow dot) — copied straight from source
      //   location = where it physically is (drives who can see it)
      // Everything MERC-grown entered so far was physically received at Daddy's, so its location
      // becomes daddyspipes while origin stays merc. Daddy's-grown stays daddyspipes for both.
      // `source` is intentionally left in place as-is so any not-yet-migrated read still works.
      migrateOriginLocation(loaded);
      // Orders redesign migration: existing orders all predate the pending/shipped lifecycle. They're
      // already finalized (product deducted via shipments, already counted as revenue), so mark them
      // "shipped" and stamp shippedAt from their date. New orders created after this use the full
      // pending → shipped flow. Idempotent: only touches orders lacking the new status.
      (loaded.orders || []).forEach((o) => {
        if (!o.status || o.status === "finalized") o.status = "shipped";
        if (o.status === "shipped" && !o.shippedAt) o.shippedAt = o.createdAt || Date.parse(o.date) || Date.now();
      });
      if (row && row.data && row.data.strainPhotos && Object.keys(row.data.strainPhotos).length && Object.keys(loaded.photos.byStrain).length === 0) {
        loaded.photos = { ...loaded.photos, byStrain: { ...loaded.photos.byStrain, ...row.data.strainPhotos } };
      }
      if (row && row.data && row.data.priceList && row.data.priceList.daddyspipes && !row.data.priceList.standard) {
        loaded.priceList = { standard: row.data.priceList, byStrain: loaded.priceList.byStrain || {} };
      }
      if (row && row.data && row.data.priceOverrides && row.data.priceOverrides.byStrain && Object.keys(row.data.priceOverrides.byStrain).length) {
        const migratedByStrain = { ...loaded.priceList.byStrain };
        Object.entries(row.data.priceOverrides.byStrain).forEach(([strain, price]) => {
          if (!migratedByStrain[strain]) migratedByStrain[strain] = { A: price, B: price, C: price, D: price, T: price };
        });
        loaded.priceList = { ...loaded.priceList, byStrain: migratedByStrain };
      }
      const todayKey = todayISO();
      let syncedAt = row?.updated_at || null;
      if (row && !loaded.backups[todayKey]) {
        // SAFETY GUARD: never let the daily auto-backup write shrink the stored data. If the freshly
        // loaded/migrated object somehow has fewer core records than what's already in the database
        // (a failed migration, a bad merge, a partial load), we skip the write entirely rather than
        // risk persisting a degraded state over good data. The daily snapshot is a convenience, not
        // worth the risk of overwriting real inventory.
        const coreCount = (o) => (o?.batches?.length || 0) + (o?.buckedLots?.length || 0) + (o?.orders?.length || 0) + (o?.shipments?.length || 0);
        const loadedCount = coreCount(loaded);
        const storedCount = coreCount(row.data);
        if (loadedCount >= storedCount) {
          const entries = Object.entries({ ...loaded.backups, [todayKey]: snapshotForBackup(loaded) })
            .sort((a, b) => (a[0] < b[0] ? 1 : -1)).slice(0, 14);
          loaded.backups = Object.fromEntries(entries);
          const stamp = new Date().toISOString();
          await supabase.from("app_state").update({ data: loaded, updated_at: stamp }).eq("id", 1);
          syncedAt = stamp;
        } else {
          console.warn(`Skipped daily backup write: loaded ${loadedCount} core records but database has ${storedCount}. Not overwriting.`);
        }
      }
      setLastSyncedAt(syncedAt);
      setData(loaded);
      setLoadingData(false);
    })();
  }, [session]);

  // Merges "my" changes onto the latest DB data after a collision. Both `mine` (what I tried to save)
  // and `base` (what I started from) are full app-data objects; `latest` is what's now in the DB
  // (including someone else's just-saved change). For each array collection keyed by record id, we
  // replay only the records I actually added/changed/removed on top of `latest`, so my change and
  // theirs both survive. Non-array fields I changed are applied last-write-wins (rare + low-stakes:
  // shipper info, pricing, cash on hand). Returns the merged object.
  function mergeOntoLatest(base, mine, latest) {
    const merged = { ...latest };
    const keys = new Set([...Object.keys(mine), ...Object.keys(latest)]);
    keys.forEach((key) => {
      const mineVal = mine[key], baseVal = base ? base[key] : undefined, latestVal = latest[key];
      const allArrays = Array.isArray(mineVal) && Array.isArray(latestVal) && Array.isArray(baseVal);
      if (allArrays && mineVal.every((r) => r && r.id) && latestVal.every((r) => r && r.id)) {
        // Record-keyed collection: replay my adds/edits/removes onto the latest list.
        const baseById = new Map(baseVal.map((r) => [r.id, r]));
        const mineById = new Map(mineVal.map((r) => [r.id, r]));
        const latestById = new Map(latestVal.map((r) => [r.id, r]));
        // Start from latest, then apply my diffs.
        const out = new Map(latestById);
        // Adds/edits I made (present in mine, and either new or changed vs base)
        mineById.forEach((rec, id) => {
          const baseRec = baseById.get(id);
          if (!baseRec || JSON.stringify(baseRec) !== JSON.stringify(rec)) out.set(id, rec);
        });
        // Removes I made (in base but not in mine) — drop them, unless someone else re-added/changed
        baseById.forEach((_, id) => {
          if (!mineById.has(id) && latestById.has(id)) {
            const latestRec = latestById.get(id), baseRec = baseById.get(id);
            // Only honor my delete if the record is unchanged from base in latest (nobody else touched it)
            if (JSON.stringify(latestRec) === JSON.stringify(baseRec)) out.delete(id);
          }
        });
        // Preserve the original ordering of `latest`, then append any of my new records not in latest.
        const ordered = latestVal.map((r) => out.get(r.id)).filter(Boolean);
        const seen = new Set(latestVal.map((r) => r.id));
        mineVal.forEach((r) => { if (!seen.has(r.id) && out.has(r.id)) ordered.push(out.get(r.id)); });
        merged[key] = ordered;
      } else if (JSON.stringify(mineVal) !== JSON.stringify(baseVal)) {
        // A non-array field I actually changed → apply mine (last-write-wins for these).
        merged[key] = mineVal;
      } // else: I didn't change it → keep latest's value.
    });
    return merged;
  }

  async function persist(next, _baseOverride) {
    const base = _baseOverride || data; // what this change was computed from
    setData(next);
    setSaving(true);
    const stamp = new Date().toISOString();
    let query = supabase.from("app_state").update({ data: next, updated_at: stamp }).eq("id", 1);
    if (lastSyncedAt) query = query.eq("updated_at", lastSyncedAt);
    const { data: updatedRows, error } = await query.select("updated_at");
    if (error) {
      setSaving(false);
      console.error("Save failed:", error);
      setSaveError(error.message || "Save failed — this change did not reach the database.");
      return;
    }
    if (lastSyncedAt && (!updatedRows || updatedRows.length === 0)) {
      // Collision: someone else saved between our last sync and now. Instead of giving up, re-fetch
      // the latest, replay our change onto it, and save again — so both changes survive.
      const { data: freshRow, error: fetchErr } = await supabase.from("app_state").select("data, updated_at").eq("id", 1).single();
      if (fetchErr || !freshRow) {
        setSaving(false);
        setSaveError("Someone else saved at the same moment and we couldn't auto-merge. Reload to see the latest, then redo your last action.");
        return;
      }
      const remerged = mergeOntoLatest(base, next, freshRow.data);
      const stamp2 = new Date().toISOString();
      const retry = await supabase.from("app_state")
        .update({ data: remerged, updated_at: stamp2 })
        .eq("id", 1).eq("updated_at", freshRow.updated_at).select("updated_at");
      setSaving(false);
      if (retry.error || !retry.data || retry.data.length === 0) {
        // Two collisions back-to-back (very busy moment). Keep the merged view locally and ask for reload.
        setData(remerged);
        setSaveError("It's busy right now and your change couldn't be saved after a retry. Reload to get the latest, then redo your last action.");
        return;
      }
      setData(remerged);
      setLastSyncedAt(retry.data[0].updated_at);
      setSaveError(null);
      return;
    }
    setSaving(false);
    setSaveError(null);
    setLastSyncedAt(updatedRows && updatedRows[0] ? updatedRows[0].updated_at : stamp);
  }

  // Shared by Bucked Flower and Wet Harvest intake — both produce raw material lots in the same
  // shape (just with different fields populated on the delivery), so one handler covers both.
  function handleRawLotSubmit(results, newStrainList) {
    let buckedLots = data.buckedLots;
    results.forEach((item) => {
      if (item.type === "new") {
        buckedLots = [...buckedLots, item.lot];
      } else {
        buckedLots = buckedLots.map((l) => (l.id === item.lotId
          ? { ...l, receivedGrams: l.receivedGrams + item.delivery.grams, deliveries: [...(l.deliveries || []), item.delivery] }
          : l));
      }
    });
    let strains = data.strains;
    newStrainList.forEach((newStrain) => {
      if (!strains.some((s) => s.toLowerCase() === newStrain.toLowerCase())) strains = [...strains, newStrain];
    });
    persist({ ...data, buckedLots, strains: strains.sort() });
  }
  function handleEditBuckedLot(lotId, patch) {
    persist({ ...data, buckedLots: data.buckedLots.map((l) => (l.id === lotId ? { ...l, ...patch } : l)) });
  }
  function handleEditBuckedDelivery(lotId, deliveryIndex, patch) {
    const buckedLots = data.buckedLots.map((l) => {
      if (l.id !== lotId) return l;
      const deliveries = l.deliveries || [];
      const oldGrams = deliveries[deliveryIndex].grams;
      const updatedDeliveries = deliveries.map((d, i) => (i === deliveryIndex ? { ...d, ...patch } : d));
      const receivedGrams = l.receivedGrams + (patch.grams != null ? patch.grams - oldGrams : 0);
      return { ...l, deliveries: updatedDeliveries, receivedGrams };
    });
    persist({ ...data, buckedLots });
  }
  function handleDeleteBuckedLot(lotId) {
    persist({
      ...data,
      buckedLots: data.buckedLots.filter((l) => l.id !== lotId),
      trimEntries: data.trimEntries.filter((t) => t.buckedLotId !== lotId),
      wasteEntries: (data.wasteEntries || []).filter((w) => w.buckedLotId !== lotId),
    });
  }
  function handleEditWaste(wasteId, patch) {
    persist({ ...data, wasteEntries: (data.wasteEntries || []).map((w) => (w.id === wasteId ? { ...w, ...patch } : w)) });
  }
  function handleDeleteWaste(wasteId) {
    persist({ ...data, wasteEntries: (data.wasteEntries || []).filter((w) => w.id !== wasteId) });
  }
  function handleEditGrade(gradeId, patch) {
    persist({ ...data, gradeEntries: (data.gradeEntries || []).map((g) => (g.id === gradeId ? { ...g, ...patch } : g)) });
  }
  function handleDeleteGrade(gradeId) {
    persist({ ...data, gradeEntries: (data.gradeEntries || []).filter((g) => g.id !== gradeId) });
  }

  const batchesWithRemaining = useMemo(() => {
    if (!data) return [];
    return data.batches.map((b) => {
      const shipped = data.shipments.filter((s) => s.batchId === b.id).reduce((sum, s) => sum + s.grams, 0);
      return { ...b, remainingGrams: Math.max(0, b.receivedGrams - shipped) };
    });
  }, [data]);
  const buckedLotsWithRemaining = useMemo(() => {
    if (!data) return [];
    return data.buckedLots.map((l) => {
      const trimmedGrams = data.trimEntries.filter((t) => t.buckedLotId === l.id)
        .reduce((sum, t) => sum + trimEntryTotal(t), 0);
      const wasteGrams = (data.wasteEntries || []).filter((w) => w.buckedLotId === l.id).reduce((sum, w) => sum + w.grams, 0);
      return { ...l, remainingGrams: Math.max(0, l.receivedGrams - trimmedGrams - wasteGrams), trimmedGrams, wasteGrams };
    });
  }, [data]);
  const inStock = useMemo(() => batchesWithRemaining.filter((b) => b.remainingGrams > 0), [batchesWithRemaining]);

  // Grams HELD (not deducted) by pending orders and out-samples, per batch. Held product still exists
  // in inventory (remainingGrams unchanged) but is unavailable for new orders — it's spoken for. When
  // a pending order ships, its hold turns into a real shipment (deduction) and drops out of here.
  const heldByBatch = useMemo(() => {
    const m = {};
    if (!data) return m;
    (data.orders || []).forEach((o) => {
      if (o.status === "pending" || o.status === "sample") {
        (o.lineItems || []).forEach((li) => { m[li.batchId] = (m[li.batchId] || 0) + li.grams; });
      }
    });
    return m;
  }, [data]);
  const editableBatches = useMemo(() => {
    if (!data || !invoiceOrderId) return [];
    return data.batches.map((b) => {
      const shipped = data.shipments.filter((s) => s.batchId === b.id && s.orderId !== invoiceOrderId).reduce((sum, s) => sum + s.grams, 0);
      return { ...b, remainingGrams: Math.max(0, b.receivedGrams - shipped) };
    }).filter((b) => b.remainingGrams > 0);
  }, [data, invoiceOrderId]);
  const totalOnHand = inStock.reduce((s, b) => s + b.remainingGrams, 0);
  const totalDaddysPipes = inStock.filter((b) => b.source === "daddyspipes").reduce((s, b) => s + b.remainingGrams, 0);
  const totalMerc = inStock.filter((b) => b.source === "merc").reduce((s, b) => s + b.remainingGrams, 0);

  // Role flags — computed with optional chaining since `profile` may not be loaded yet on early renders.
  // These (and the hooks below) must stay above every early return so hook order never changes between renders.
  const isAdmin = profile?.role === "admin";
  const isManager = profile?.role === "manager";
  const isTrimmer = profile?.role === "trimmer";
  const fullAccess = isAdmin || isManager;
  const can = (key) => fullAccess || (profile?.permissions && profile.permissions[key]);
  const roleLabel = isAdmin ? "Admin" : isManager ? "Manager" : isTrimmer ? "Trimming Manager" : "Sales Rep";
  // Adapter so every existing component (built against the Claude-hosted version) works unchanged
  const currentUser = { id: profile?.id, displayName: profile?.display_name, role: profile?.role, permissions: profile?.permissions };

  // Admins land on the Overview dashboard by default. Only redirect once, right after the profile
  // first loads, so it doesn't fight the user if they navigate away.
  const landedRef = React.useRef(false);
  useEffect(() => {
    if (profile && !landedRef.current) {
      landedRef.current = true;
      if (profile.role === "admin") setTab("overview");
    }
  }, [profile]);

  // Facility scoping: admins always see both; everyone else is limited to whichever facility
  // (or both) an admin has granted them in Team management.
  // Visibility is by LOCATION (where product physically sits), not origin (who grew it). So a
  // MERC-grown batch received at Daddy's is visible to Daddy's (location daddyspipes) and carries a
  // yellow origin dot. Records predating the split fall back to `source`.
  const userFacility = isAdmin ? "both" : (profile?.permissions?.facilityAccess || "both");
  const visibleSources = userFacility === "both" ? ["daddyspipes", "merc"] : [userFacility];
  const scopeOk = (source) => userFacility === "both" || source === userFacility;
  const locOk = (rec) => userFacility === "both" || (rec.location || rec.source) === userFacility;

  const scopedInStock = useMemo(() => inStock.filter(locOk), [inStock, userFacility]);
  const scopedAllBatches = useMemo(() => (data ? data.batches.filter(locOk) : []), [data, userFacility]);
  const scopedBatchesWithRemaining = useMemo(() => batchesWithRemaining.filter(locOk), [batchesWithRemaining, userFacility]);
  const scopedOrders = useMemo(() => (data ? data.orders.filter((o) => o.lineItems.every((li) => scopeOk(li.source))) : []), [data, userFacility]);

  // Alert when orders are waiting to be manifested/shipped: a chime the moment the pending count goes
  // UP (a new order was created), so nothing sits un-manifested. The red badge on the Ship tab shows
  // the count; this is the audible nudge Frank asked for.
  const pendingShipCount = useMemo(() => scopedOrders.filter((o) => o.status === "pending").length, [scopedOrders]);
  const prevPendingRef = React.useRef(null);
  useEffect(() => {
    if (prevPendingRef.current === null) { prevPendingRef.current = pendingShipCount; return; }
    if (pendingShipCount > prevPendingRef.current) {
      try {
        const Ctx = window.AudioContext || window.webkitAudioContext;
        if (Ctx) {
          const ctx = new Ctx();
          [880, 1180].forEach((freq, i) => {
            const osc = ctx.createOscillator(), gain = ctx.createGain();
            osc.frequency.value = freq; gain.gain.value = 0.05;
            osc.connect(gain); gain.connect(ctx.destination);
            osc.start(ctx.currentTime + i * 0.16); osc.stop(ctx.currentTime + i * 0.16 + 0.15);
          });
        }
      } catch {}
    }
    prevPendingRef.current = pendingShipCount;
  }, [pendingShipCount]);
  const scopedTrimLogs = useMemo(() => (data ? data.trimLogs.filter((l) => scopeOk(l.source)) : []), [data, userFacility]);
  const scopedBuckedLots = useMemo(() => buckedLotsWithRemaining.filter(locOk), [buckedLotsWithRemaining, userFacility]);
  const scopedTrimEntries = useMemo(() => {
    if (!data) return [];
    if (userFacility === "both") return data.trimEntries;
    const visibleLotIds = new Set(scopedBuckedLots.map((l) => l.id));
    return data.trimEntries.filter((t) => visibleLotIds.has(t.buckedLotId));
  }, [data, scopedBuckedLots, userFacility]);
  const scopedWasteEntries = useMemo(() => {
    if (!data) return [];
    if (userFacility === "both") return data.wasteEntries || [];
    const visibleLotIds = new Set(scopedBuckedLots.map((l) => l.id));
    return (data.wasteEntries || []).filter((w) => visibleLotIds.has(w.buckedLotId));
  }, [data, scopedBuckedLots, userFacility]);
  const scopedGradeEntries = useMemo(() => {
    if (!data) return [];
    if (userFacility === "both") return data.gradeEntries || [];
    const visibleLotIds = new Set(scopedBuckedLots.map((l) => l.id));
    return (data.gradeEntries || []).filter((g) => visibleLotIds.has(g.buckedLotId));
  }, [data, scopedBuckedLots, userFacility]);
  const scopedShipments = useMemo(() => {
    if (!data) return [];
    if (userFacility === "both") return data.shipments;
    const visibleIds = new Set(scopedAllBatches.map((b) => b.id));
    return data.shipments.filter((s) => visibleIds.has(s.batchId));
  }, [data, scopedAllBatches, userFacility]);
  const scopedRooms = useMemo(() => {
    if (!data) return { daddyspipes: [], merc: [] };
    return userFacility === "both" ? data.rooms : { daddyspipes: userFacility === "daddyspipes" ? data.rooms.daddyspipes : [], merc: userFacility === "merc" ? data.rooms.merc : [] };
  }, [data, userFacility]);
  const scopedEditableBatches = useMemo(() => editableBatches.filter((b) => scopeOk(b.source)), [editableBatches, userFacility]);
  const scopedTotalOnHand = scopedInStock.reduce((s, b) => s + b.remainingGrams, 0);
  const scopedTotalDaddysPipes = scopedInStock.filter((b) => b.source === "daddyspipes").reduce((s, b) => s + b.remainingGrams, 0);
  const scopedTotalMerc = scopedInStock.filter((b) => b.source === "merc").reduce((s, b) => s + b.remainingGrams, 0);

  const GlobalStyle = () => (
    <style>{`
      @import url('https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;600;700&family=IBM+Plex+Mono:wght@400;500;600&family=Inter:wght@400;500;600&display=swap');
      * { box-sizing: border-box; } body { margin: 0; }
      input, select, textarea, button { font-family: 'Inter', sans-serif; }
      input::placeholder { color: #6B7264; }
      input:focus, select:focus, textarea:focus { outline: 2px solid #C9A24B; outline-offset: 1px; }
      button:focus-visible { outline: 2px solid #C9A24B; outline-offset: 2px; }
      ::-webkit-scrollbar { width: 8px; height: 8px; } ::-webkit-scrollbar-thumb { background: #3A4232; border-radius: 4px; }
      .trimmer-opt:hover { background: #232B1D; }
      /* html5-qrcode injects its own <video>; make it fill and cover the scan region */
      #stem-scan-region video { width: 100% !important; height: 100% !important; object-fit: cover !important; display: block; }
      #stem-scan-region { line-height: 0; }
      #stem-scan-region img { display: none; } /* hide the library's default scan-region image overlay */
    `}</style>
  );

  if (session === undefined) {
    return <div style={styles.loadingScreen}><Loader2 className="animate-spin" size={28} color="#C9A24B" /></div>;
  }

  if (!session) {
    return (
      <div style={styles.app}>
        <GlobalStyle />
        <AuthScreen />
      </div>
    );
  }

  if (loadError) {
    return (
      <div style={styles.app}>
        <GlobalStyle />
        <div style={styles.gateWrap}>
          <Logo size={48} />
          <div style={styles.gateTitle}>The Stem <span style={styles.versionTag}>v{APP_VERSION}</span>{IS_DEMO && <span style={styles.demoBadge}>DEMO</span>}</div>
          <div style={{ ...styles.authForm, textAlign: "center" }}>
            <div style={{ fontSize: 14, color: "#EDE8D8", marginBottom: 10 }}>Couldn't load your data</div>
            <div style={{ fontSize: 12.5, color: "#8C9483", marginBottom: 16 }}>
              Something went wrong reaching the database — likely a temporary connection issue. Nothing was changed. Try reloading; if this keeps happening, stop and let us know rather than continuing.
            </div>
            <button style={styles.submitBtn} onClick={() => window.location.reload()}>Reload</button>
          </div>
        </div>
      </div>
    );
  }

  if (!profile || loadingData || !data) {
    return <div style={styles.loadingScreen}><Loader2 className="animate-spin" size={28} color="#C9A24B" /></div>;
  }

  return (
    <div style={styles.app}>
      <GlobalStyle />
      <header style={styles.header}>
        <div style={styles.headerLeft}>
          <Logo size={30} />
          <div>
            <div style={styles.title}>The Stem <span style={styles.versionTag}>v{APP_VERSION}</span>{IS_DEMO && <span style={styles.demoBadge}>DEMO</span>}</div>
            <div style={styles.subtitle}>{currentUser.displayName} · {roleLabel}</div>
          </div>
        </div>
        <div style={styles.headerRight}>
          <UnitToggle unit={unit} setUnit={setUnit} />
          {saving && <span style={styles.savingTag}>saving…</span>}
          <button style={styles.switchRoleBtn} onClick={() => supabase.auth.signOut()}><LogOut size={13} /> Sign out</button>
        </div>
      </header>
      {saveError && (
        <div style={styles.saveErrorBanner}>
          <AlertTriangle size={15} /> Your last change didn't save — {saveError} Don't keep working until this is fixed, or you'll lose it. Try reloading; if it keeps happening, stop and get help.
        </div>
      )}

      <nav style={styles.tabBar}>
        {isAdmin && <TabButton active={tab === "overview"} onClick={() => setTab("overview")} icon={Gauge} label="Dashboard" />}
        <TabButton active={tab === "dashboard"} onClick={() => setTab("dashboard")} icon={Warehouse} label={fullAccess ? "Inventory" : "Menu"} />
        {can("receive") && <TabButton active={tab === "receive"} onClick={() => setTab("receive")} icon={ArrowDownCircle} label="Intake" />}
        {(fullAccess || isTrimmer) && <TabButton active={tab === "trim"} onClick={() => setTab("trim")} icon={Scissors} label="Trim Log" />}
        {fullAccess && <TabButton active={tab === "cultivation"} onClick={() => setTab("cultivation")} icon={Sprout} label="Cultivation" />}
        {fullAccess && <TabButton active={tab === "vaults"} onClick={() => setTab("vaults")} icon={ShieldCheck} label="Vaults" />}
        {!isTrimmer && <TabButton active={tab === "orders"} onClick={() => { setTab("orders"); setInvoiceOrderId(null); }} icon={ClipboardList} label="Orders" />}
        {!isTrimmer && <TabButton active={tab === "photomenu"} onClick={() => setTab("photomenu")} icon={LayoutGrid} label="Menu" />}
        {can("reports") && <TabButton active={tab === "reports"} onClick={() => setTab("reports")} icon={FileText} label="Reports" />}
        {fullAccess && <TabButton active={tab === "reconcile"} onClick={() => setTab("reconcile")} icon={ClipboardCheck} label="Reconcile" />}
        {(isAdmin || isManager || can("catalog")) && <TabButton active={tab === "admin"} onClick={() => setTab("admin")} icon={ShieldCheck} label="Admin" />}
        <TabButton active={tab === "history"} onClick={() => setTab("history")} icon={HistoryIcon} label="History" />
      </nav>

      <main style={styles.main}>
        {tab === "dashboard" && (
          <Dashboard
            batches={scopedInStock} totalOnHand={scopedTotalOnHand} totalDaddysPipes={scopedTotalDaddysPipes} totalMerc={scopedTotalMerc} unit={unit}
            photos={data.photos} canEditPhotos={fullAccess}
            onUpdatePhotos={(photos) => persist({ ...data, photos })}
            rooms={scopedRooms} customGrades={data.customGrades} gradeDescriptions={data.gradeDescriptions} shipments={scopedShipments}
            canEditBatch={can("delete")} visibleSources={visibleSources} shipper={data.shipper}
            onEditBatch={(batchId, patch) => persist({ ...data, batches: data.batches.map((b) => (b.id === batchId ? { ...b, ...patch } : b)) })}
          />
        )}
        {tab === "receive" && can("receive") && (
          <div>
            {fullAccess && (
              <div style={{ ...styles.groupToggle, marginBottom: 14, flexWrap: "wrap" }}>
                <button onClick={() => setIntakeMode("graded")} style={{ ...styles.groupBtn, background: intakeMode === "graded" ? "#C9A24B" : "transparent", color: intakeMode === "graded" ? "#12160F" : "#B9BFA9" }}>Bulk Flower</button>
                <button onClick={() => setIntakeMode("bucked")} style={{ ...styles.groupBtn, background: intakeMode === "bucked" ? "#C9A24B" : "transparent", color: intakeMode === "bucked" ? "#12160F" : "#B9BFA9" }}>Bucked Flower</button>
                <button onClick={() => setIntakeMode("harvest")} style={{ ...styles.groupBtn, background: intakeMode === "harvest" ? "#C9A24B" : "transparent", color: intakeMode === "harvest" ? "#12160F" : "#B9BFA9" }}>Harvest</button>
              </div>
            )}
            {(!fullAccess || intakeMode === "graded") && (
              <ReceiveForm
                rooms={scopedRooms} strains={data.strains} currentUser={currentUser} customGrades={data.customGrades} gradeDescriptions={data.gradeDescriptions} existingBatches={scopedAllBatches}
                enteringFacility={userFacility === "both" ? "daddyspipes" : userFacility} shipper={data.shipper} unit={unit}
                onSubmitManifest={(results, newStrainList) => {
                  // Applies every line item from one manifest submission in a single persist call —
                  // looping separate persist() calls here would race against each other, since each
                  // one closes over the same stale `data` and the later ones would clobber the earlier.
                  let batches = data.batches;
                  results.forEach((item) => {
                    if (item.type === "new") {
                      batches = [...batches, item.batch];
                    } else {
                      batches = batches.map((b) => (b.id === item.batchId
                        ? { ...b, receivedGrams: b.receivedGrams + item.addition.grams, additions: [...(b.additions || []), item.addition] }
                        : b));
                    }
                  });
                  let strains = data.strains;
                  newStrainList.forEach((newStrain) => {
                    if (!strains.some((s) => s.toLowerCase() === newStrain.toLowerCase())) strains = [...strains, newStrain];
                  });
                  persist({ ...data, batches, strains: strains.sort() });
                  // Deliberately staying on this tab — the confirmation + print-label buttons below
                  // need to actually be visible, and mid-manifest you don't want to be bounced away.
                }}
              />
            )}
            {fullAccess && intakeMode === "bucked" && (
              <BuckedIntakeForm
                rooms={scopedRooms} strains={data.strains} currentUser={currentUser} existingLots={scopedBuckedLots}
                enteringFacility={userFacility === "both" ? "daddyspipes" : userFacility}
                onSubmitManifest={handleRawLotSubmit}
              />
            )}
            {fullAccess && intakeMode === "harvest" && (
              <HarvestIntakeForm
                rooms={scopedRooms} strains={data.strains} currentUser={currentUser} existingLots={scopedBuckedLots}
                enteringFacility={userFacility === "both" ? "daddyspipes" : userFacility}
                onSubmitManifest={handleRawLotSubmit}
              />
            )}
          </div>
        )}
        {tab === "trim" && (fullAccess || isTrimmer) && (
          <TrimTab
            buckedLots={scopedBuckedLots} trimmers={data.trimmers} currentUser={currentUser} customGrades={data.customGrades} gradeDescriptions={data.gradeDescriptions}
            unit={unit} canManage={fullAccess} entries={scopedTrimEntries} gradeEntries={scopedGradeEntries} wasteEntries={scopedWasteEntries}
            onSubmitEntry={(entry, trimmerName) => {
              const trimmers = trimmerName && !data.trimmers.some((t) => t.toLowerCase() === trimmerName.toLowerCase())
                ? [...data.trimmers, trimmerName].sort() : data.trimmers;
              persist({ ...data, trimEntries: [...data.trimEntries, entry], trimmers });
            }}
            onEditEntry={(entryId, patch) => {
              const trimmers = patch.trimmerName && !data.trimmers.some((t) => t.toLowerCase() === patch.trimmerName.toLowerCase())
                ? [...data.trimmers, patch.trimmerName].sort() : data.trimmers;
              persist({ ...data, trimEntries: data.trimEntries.map((t) => (t.id === entryId ? { ...t, ...patch } : t)), trimmers });
            }}
            onDeleteEntry={(entryId) => persist({ ...data, trimEntries: data.trimEntries.filter((t) => t.id !== entryId) })}
            onSubmitGradeEntry={(entry) => persist({ ...data, gradeEntries: [...(data.gradeEntries || []), entry] })}
            onLogWaste={(lotId, { date, grams, notes }) => {
              const wasteEntry = { id: uid(), buckedLotId: lotId, date, grams, notes, staff: currentUser.displayName, createdAt: Date.now() };
              persist({ ...data, wasteEntries: [...(data.wasteEntries || []), wasteEntry] });
            }}
            onMarkFinished={(lot) => {
              // Whatever's still remaining on the lot when it's closed out gets logged as a final
              // waste entry first, so the received/trimmed/waste numbers always add up.
              let wasteEntries = data.wasteEntries || [];
              if (lot.remainingGrams > 0) {
                wasteEntries = [...wasteEntries, {
                  id: uid(), buckedLotId: lot.id, date: todayISO(), grams: lot.remainingGrams,
                  notes: "Batch closed out — remaining weight logged as waste", staff: currentUser.displayName, createdAt: Date.now(),
                }];
              }
              const buckedLots = data.buckedLots.map((l) => (l.id === lot.id ? { ...l, archived: true } : l));
              persist({ ...data, buckedLots, wasteEntries });
            }}
            onDeleteWaste={handleDeleteWaste}
          />
        )}
        {tab === "orders" && !isTrimmer && !invoiceOrderId && (
          <OrdersTab
            batches={scopedInStock} currentUser={currentUser} orders={scopedOrders} customers={data.customers}
            priceList={data.priceList} priceOverrides={data.priceOverrides} canDelete={can("delete")} canMovement={can("receive")}
            heldByBatch={heldByBatch}
            nextOrderNumber={data.nextOrderNumber || data.orders.length + 1}
            unit={unit}
            onCreatePending={(order, newCustomer) => {
              // New Order: create as PENDING with no shipments — product is held (via heldByBatch),
              // not deducted, not yet revenue.
              const customers = newCustomer ? [...data.customers, newCustomer] : data.customers;
              const usedNumber = data.nextOrderNumber || data.orders.length + 1;
              persist({ ...data, orders: [...data.orders, order], customers, nextOrderNumber: usedNumber + 1 });
              setInvoiceOrderId(order.id);
            }}
            onCreateSample={(order, newCustomer) => {
              // Sample: same as pending (held) but status "sample" and never counts as revenue.
              const customers = newCustomer ? [...data.customers, newCustomer] : data.customers;
              const usedNumber = data.nextOrderNumber || data.orders.length + 1;
              persist({ ...data, orders: [...data.orders, order], customers, nextOrderNumber: usedNumber + 1 });
              setInvoiceOrderId(order.id);
            }}
            onShipOrder={(orderId, metrc) => {
              // Ship: create the shipments now (this is what deducts inventory), stamp per-line Metrc
              // package #s + one manifest # + order notes, flip to "shipped" (counts as revenue).
              const order = data.orders.find((o) => o.id === orderId);
              if (!order) return;
              const pkgByLine = metrc.packagesByLine || {};
              const newShipments = order.lineItems.map((li) => ({
                id: uid(), batchId: li.batchId, strain: li.strain, grams: li.grams, destination: "sale",
                date: todayISO(), staff: currentUser.displayName,
                notes: `Order ${order.orderNumber} — ${order.customer}`, orderId: order.id,
                packageTag: (pkgByLine[li.id] || "").trim(), createdAt: Date.now(),
              }));
              // Also stamp each line item with its package # so the invoice/history can show it.
              const stampedLineItems = order.lineItems.map((li) => ({ ...li, metrcPackageNumber: (pkgByLine[li.id] || "").trim() }));
              const orders = data.orders.map((o) => o.id === orderId ? {
                ...o, status: "shipped", shippedAt: Date.now(), lineItems: stampedLineItems,
                manifestNumber: (metrc.manifestNumber || "").trim(),
                shipNotes: (metrc.notes || "").trim(),
              } : o);
              persist({ ...data, orders, shipments: [...data.shipments, ...newShipments] });
            }}
            onReturnSample={(orderId, metrc) => {
              // Return a sample: release the hold (status → sample_returned so heldByBatch drops it),
              // note it, attach optional Metrc tag. No money movement (samples were never revenue).
              const orders = data.orders.map((o) => o.id === orderId ? {
                ...o, status: "sample_returned", returnedAt: Date.now(),
                manifestNumber: (metrc.manifestNumber || o.manifestNumber || "").trim(),
                metrcPackageNumber: (metrc.metrcPackageNumber || o.metrcPackageNumber || "").trim(),
              } : o);
              persist({ ...data, orders });
            }}
            onConvertSample={(orderId) => {
              // Convert a sample to a sale: becomes a normal PENDING order (still held), then shipped
              // in the Ship tab. Stays fully editable as a pending order.
              const orders = data.orders.map((o) => o.id === orderId ? { ...o, status: "pending", convertedFromSample: true } : o);
              persist({ ...data, orders });
              setInvoiceOrderId(orderId);
            }}
            onCancelPending={(orderId) => {
              // Cancel a pending order: remove it entirely, releasing its hold. No shipments existed
              // (nothing was deducted), so inventory just returns to available.
              persist({ ...data, orders: data.orders.filter((o) => o.id !== orderId) });
            }}
            onLogMovement={(shipment) => persist({ ...data, shipments: [...data.shipments, shipment] })}
            onOpenInvoice={(id) => setInvoiceOrderId(id)}
            onDeleteOrder={(orderId) => {
              persist({ ...data, orders: data.orders.filter((o) => o.id !== orderId), shipments: data.shipments.filter((s) => s.orderId !== orderId) });
            }}
          />
        )}
        {tab === "orders" && invoiceOrderId && (
          <InvoiceView
            order={data.orders.find((o) => o.id === invoiceOrderId)}
            distributionEmail={data.distributionEmail}
            shipper={data.shipper}
            paymentInfo={data.paymentInfo}
            editableBatches={scopedEditableBatches}
            canEdit={can("delete")}
            onBack={() => setInvoiceOrderId(null)}
            onMarkEmailed={(orderId) => persist({ ...data, orders: data.orders.map((o) => (o.id === orderId ? { ...o, emailedAt: Date.now() } : o)) })}
            onTogglePaid={(orderId) => persist({ ...data, orders: data.orders.map((o) => (o.id === orderId ? { ...o, paymentStatus: o.paymentStatus === "paid" ? "unpaid" : "paid" } : o)) })}
            onSaveEdit={(orderId, patch) => {
              const order = data.orders.find((o) => o.id === orderId);
              if (!order) return;
              const otherShipments = data.shipments.filter((s) => s.orderId !== orderId);
              const updatedOrder = { ...order, ...patch };
              // Only shipped orders have shipments (deductions). Editing a pending or sample order must
              // NOT create shipments — the product is only held, not deducted, until it ships.
              if (order.status === "shipped") {
                const newShipments = patch.lineItems.map((li) => ({
                  id: uid(), batchId: li.batchId, strain: li.strain, grams: li.grams, destination: "sale",
                  date: order.date, staff: order.staff, notes: `Order ${patch.orderNumber || order.orderNumber} — ${patch.customer} (edited)`,
                  orderId: order.id, createdAt: Date.now(),
                }));
                persist({ ...data, orders: data.orders.map((o) => (o.id === orderId ? updatedOrder : o)), shipments: [...otherShipments, ...newShipments] });
              } else {
                persist({ ...data, orders: data.orders.map((o) => (o.id === orderId ? updatedOrder : o)) });
              }
            }}
          />
        )}
        {tab === "cultivation" && fullAccess && (
          <CultivationTab data={data} currentUser={currentUser} facility={userFacility === "both" ? "daddyspipes" : userFacility} />
        )}
        {tab === "vaults" && fullAccess && (
          <VaultsTab data={data} currentUser={currentUser} facility={userFacility} unit={unit} onPersist={persist} />
        )}
        {tab === "photomenu" && !isTrimmer && (
          <MenuTab batches={scopedInStock} photos={data.photos} priceList={data.priceList} priceOverrides={data.priceOverrides} shipper={data.shipper} unit={unit} />
        )}
        {tab === "reports" && can("reports") && <ReportsTab batches={scopedInStock} orders={scopedOrders} trimLogs={scopedTrimLogs} buckedLots={scopedBuckedLots} trimEntries={scopedTrimEntries} gradeEntries={scopedGradeEntries} wasteEntries={scopedWasteEntries} unit={unit} visibleSources={visibleSources} />}
        {tab === "reconcile" && fullAccess && (
          <ReconciliationTab
            batches={scopedInStock} unit={unit} currentUser={currentUser} reconciliations={data.reconciliations}
            onSubmitReconciliation={(rec) => {
              const batches = data.batches.map((b) => {
                const adj = rec.adjustments.find((a) => a.batchId === b.id);
                if (!adj) return b;
                return { ...b, receivedGrams: b.receivedGrams + (adj.after - adj.before) };
              });
              persist({ ...data, batches, reconciliations: [...(data.reconciliations || []), rec] });
            }}
          />
        )}
        {tab === "overview" && isAdmin && <ExecDashboard batches={inStock} orders={data.orders} priceList={data.priceList} priceOverrides={data.priceOverrides} unit={unit} cashOnHand={data.cashOnHand} onUpdateCashOnHand={(cashOnHand) => persist({ ...data, cashOnHand })} />}
        {tab === "admin" && (isAdmin || isManager || can("catalog")) && (
          <AdminTab
            data={data} currentProfile={profile} isAdmin={isAdmin} isManager={isManager} batches={scopedInStock} visibleSources={visibleSources}
            onUpdateRooms={(rooms) => persist({ ...data, rooms })}
            onUpdateStrains={(strains) => persist({ ...data, strains })}
            onUpdateDistributionEmail={(email) => persist({ ...data, distributionEmail: email })}
            onUpdateShipper={(shipper) => persist({ ...data, shipper })}
            onUpdatePaymentInfo={(paymentInfo) => persist({ ...data, paymentInfo })}
            onUpdatePriceList={(priceList) => persist({ ...data, priceList })}
            onUpdatePriceOverrides={(priceOverrides) => persist({ ...data, priceOverrides })}
            onUpdatePhotos={(photos) => persist({ ...data, photos })}
            onUpdateMenuPassword={(menuPassword) => persist({ ...data, menuPassword })}
            onUpdateCustomers={(customers) => persist({ ...data, customers })}
            onUpdateGradeDescriptions={(gradeDescriptions) => persist({ ...data, gradeDescriptions })}
            onUpdateCustomGrades={(customGrades) => persist({ ...data, customGrades })}
            onRestoreBackup={(dateKey) => {
              const snap = data.backups[dateKey];
              if (!snap) return;
              persist({ ...data, ...snap });
            }}
            onUpdateNextOrderNumber={(n) => persist({ ...data, nextOrderNumber: n })}
            onUpdateLicenses={(licenses) => persist({ ...data, licenses })}
          />
        )}
        {tab === "history" && (
          <HistoryView
            batches={scopedBatchesWithRemaining} shipments={scopedShipments} buckedLots={scopedBuckedLots} trimEntries={scopedTrimEntries} gradeEntries={scopedGradeEntries} wasteEntries={scopedWasteEntries}
            unit={unit} canDelete={can("delete")} canEdit={can("delete")}
            rooms={scopedRooms} customGrades={data.customGrades} gradeDescriptions={data.gradeDescriptions}
            onDeleteBatch={(batchId) => persist({ ...data, batches: data.batches.filter((b) => b.id !== batchId), shipments: data.shipments.filter((s) => s.batchId !== batchId) })}
            onDeleteShipment={(shipmentId) => persist({ ...data, shipments: data.shipments.filter((s) => s.id !== shipmentId) })}
            onEditBatch={(batchId, patch) => persist({ ...data, batches: data.batches.map((b) => (b.id === batchId ? { ...b, ...patch } : b)) })}
            onEditAddition={(batchId, additionIndex, patch) => {
              const batches = data.batches.map((b) => {
                if (b.id !== batchId) return b;
                const additions = (b.additions && b.additions.length > 0)
                  ? b.additions
                  : [{ grams: b.receivedGrams, date: b.dateReceived, staff: b.staff, notes: b.notes, manifestNumber: b.manifestNumber, packageTag: b.packageTag }];
                const oldGrams = additions[additionIndex].grams;
                const updatedAdditions = additions.map((a, i) => (i === additionIndex ? { ...a, ...patch } : a));
                const receivedGrams = b.receivedGrams + (patch.grams != null ? patch.grams - oldGrams : 0);
                // The first addition's date/notes/manifest #/package tag double as the batch's own
                // top-level fields (used for labels etc.), so keep those mirrored when editing that entry.
                const topLevelPatch = additionIndex === 0
                  ? { dateReceived: patch.date ?? b.dateReceived, notes: patch.notes ?? b.notes, manifestNumber: patch.manifestNumber ?? b.manifestNumber, packageTag: patch.packageTag ?? b.packageTag }
                  : {};
                return { ...b, additions: updatedAdditions, receivedGrams, ...topLevelPatch };
              });
              persist({ ...data, batches });
            }}
            onEditShipment={(shipmentId, patch) => persist({ ...data, shipments: data.shipments.map((s) => (s.id === shipmentId ? { ...s, ...patch } : s)) })}
            onEditBuckedLot={handleEditBuckedLot}
            onEditBuckedDelivery={handleEditBuckedDelivery}
            onDeleteBuckedLot={handleDeleteBuckedLot}
            onEditWaste={handleEditWaste}
            onDeleteWaste={handleDeleteWaste}
            onEditGrade={handleEditGrade}
            onDeleteGrade={handleDeleteGrade}
          />
        )}
      </main>
    </div>
  );
}

// ---------- AUTH (Supabase) ----------
function AuthScreen() {
  const [mode, setMode] = useState("signin"); // signin | signup | forgot
  const [email, setEmail] = useState("");
  const [displayName, setDisplayName] = useState("");
  const [password, setPassword] = useState("");
  const [error, setError] = useState("");
  const [notice, setNotice] = useState("");
  const [busy, setBusy] = useState(false);

  async function handleSignIn(e) {
    e.preventDefault(); setBusy(true); setError(""); setNotice("");
    const { error } = await supabase.auth.signInWithPassword({ email: email.trim(), password });
    setBusy(false);
    if (error) setError(error.message);
  }

  async function handleSignUp(e) {
    e.preventDefault(); setBusy(true); setError(""); setNotice("");
    if (!displayName.trim()) { setBusy(false); return setError("Enter your name."); }
    if (password.length < 6) { setBusy(false); return setError("Password must be at least 6 characters."); }
    const { data, error } = await supabase.auth.signUp({
      email: email.trim(), password,
      options: { data: { display_name: displayName.trim() } },
    });
    setBusy(false);
    if (error) return setError(error.message);
    if (data.session) return; // signed in automatically
    setNotice("Check your email to confirm your account, then sign in.");
    setMode("signin");
  }

  async function handleForgot(e) {
    e.preventDefault(); setBusy(true); setError(""); setNotice("");
    const { error } = await supabase.auth.resetPasswordForEmail(email.trim());
    setBusy(false);
    if (error) return setError(error.message);
    setNotice("Password reset email sent — check your inbox.");
  }

  return (
    <div style={styles.gateWrap}>
      <Logo size={48} />
      <div style={styles.gateTitle}>The Stem <span style={styles.versionTag}>v{APP_VERSION}</span>{IS_DEMO && <span style={styles.demoBadge}>DEMO</span>}</div>
      <div style={styles.gateSubtitle}>
        {mode === "signin" ? "Sign in to your account" : mode === "signup" ? "Create your account" : "Reset your password"}
      </div>

      {mode === "signup" && (
        <form onSubmit={handleSignUp} style={styles.authForm}>
          <FieldLabel>Your name</FieldLabel>
          <input style={styles.input} value={displayName} onChange={(e) => setDisplayName(e.target.value)} />
          <FieldLabel>Email</FieldLabel>
          <input style={styles.input} type="email" value={email} onChange={(e) => setEmail(e.target.value)} />
          <FieldLabel>Password</FieldLabel>
          <input style={styles.input} type="password" value={password} onChange={(e) => setPassword(e.target.value)} />
          {error && <div style={styles.errorText}>{error}</div>}
          {notice && <div style={styles.noticeText}>{notice}</div>}
          <button style={styles.submitBtn} disabled={busy}>{busy ? "Creating…" : "Create Account"}</button>
          <div style={styles.pinHint}>The very first person to sign up on a fresh project becomes admin automatically. Everyone after that starts as a Sales Rep — admin can promote them or grant extra permissions under Admin → Team.</div>
          <button type="button" style={styles.linkBtn} onClick={() => setMode("signin")}>Already have an account? Sign in</button>
        </form>
      )}

      {mode === "signin" && (
        <form onSubmit={handleSignIn} style={styles.authForm}>
          <FieldLabel>Email</FieldLabel>
          <input style={styles.input} type="email" value={email} onChange={(e) => setEmail(e.target.value)} />
          <FieldLabel>Password</FieldLabel>
          <input style={styles.input} type="password" value={password} onChange={(e) => setPassword(e.target.value)} />
          {error && <div style={styles.errorText}>{error}</div>}
          {notice && <div style={styles.noticeText}>{notice}</div>}
          <button style={styles.submitBtn} disabled={busy}>{busy ? "Signing in…" : "Sign In"}</button>
          <button type="button" style={styles.linkBtn} onClick={() => setMode("signup")}>New here? Create an account</button>
          <button type="button" style={styles.linkBtn} onClick={() => setMode("forgot")}>Forgot your password?</button>
        </form>
      )}

      {mode === "forgot" && (
        <form onSubmit={handleForgot} style={styles.authForm}>
          <FieldLabel>Email</FieldLabel>
          <input style={styles.input} type="email" value={email} onChange={(e) => setEmail(e.target.value)} />
          {error && <div style={styles.errorText}>{error}</div>}
          {notice && <div style={styles.noticeText}>{notice}</div>}
          <button style={styles.submitBtn} disabled={busy}>{busy ? "Sending…" : "Send Reset Email"}</button>
          <button type="button" style={styles.linkBtn} onClick={() => setMode("signin")}>Back to sign in</button>
        </form>
      )}
    </div>
  );
}

// ---------- SHARED BITS ----------
function Logo({ size = 32 }) {
  return <img src={LOGO_DATA_URI} alt="The Stem" style={{ width: size, height: size * 1.28, objectFit: "contain", flexShrink: 0 }} />;
}

function UnitToggle({ unit, setUnit }) {
  return (
    <div style={styles.unitToggle}>
      {["lb", "g"].map((u) => (
        <button key={u} onClick={() => setUnit(u)} style={{ ...styles.unitBtn, background: unit === u ? "#C9A24B" : "transparent", color: unit === u ? "#12160F" : "#B9BFA9" }}>{u === "lb" ? "LB" : "G"}</button>
      ))}
    </div>
  );
}
function TabButton({ active, onClick, icon: Icon, label }) {
  return (
    <button onClick={onClick} style={{ ...styles.tabBtn, color: active ? "#EDE8D8" : "#7C8571", borderBottom: active ? "2px solid #C9A24B" : "2px solid transparent" }}>
      <Icon size={16} />{label}
    </button>
  );
}
function GradeBadge({ grade, isBest }) {
  const m = GRADE_META[grade] || { short: grade, color: "#8FAF8B" };
  return <span style={{ ...styles.gradeBadge, color: m.color, borderColor: m.color }}>{m.short}{isBest && <Star size={10} fill="#C9A24B" color="#C9A24B" style={{ marginLeft: 3 }} />}</span>;
}
function SourceTag({ source }) { const m = SOURCE_META[source]; return <span style={{ ...styles.sourceTag, color: m.color, background: m.bg }}>{m.label}</span>; }
function SummaryCard({ label, value, accent }) {
  return <div style={styles.summaryCard}><div style={styles.summaryLabel}>{label}</div><div style={{ ...styles.summaryValue, color: accent }}>{value}</div></div>;
}
function FieldLabel({ children }) { return <label style={styles.fieldLabel}>{children}</label>; }

function DualWeightInput({ grams, onChange }) {
  const [lb, setLb] = useState(grams ? (grams / G_PER_LB).toFixed(2) : "");
  const [g, setG] = useState(grams ? String(Math.round(grams)) : "");
  useEffect(() => { if (!grams) { setLb(""); setG(""); } }, [grams]);
  function fromLb(v) { setLb(v); const n = parseFloat(v); if (!isNaN(n)) { setG(String(Math.round(n * G_PER_LB))); onChange(n * G_PER_LB); } else onChange(0); }
  function fromG(v) { setG(v); const n = parseFloat(v); if (!isNaN(n)) { setLb((n / G_PER_LB).toFixed(2)); onChange(n); } else onChange(0); }
  return (
    <div style={styles.twoCol}>
      <div><FieldLabel>Pounds</FieldLabel><input style={styles.input} type="number" step="0.01" min="0" value={lb} onChange={(e) => fromLb(e.target.value)} placeholder="0.00" /></div>
      <div><FieldLabel>Grams</FieldLabel><input style={styles.input} type="number" step="1" min="0" value={g} onChange={(e) => fromG(e.target.value)} placeholder="0" /></div>
    </div>
  );
}

function BatchSearchPicker({ batches, reservedByBatch, onSelect, placeholder }) {
  const [query, setQuery] = useState("");
  const [open, setOpen] = useState(false);
  const results = useMemo(() => {
    const q = query.trim().toLowerCase();
    return batches.filter((b) => {
      const avail = b.remainingGrams - (reservedByBatch?.[b.id] || 0);
      if (avail <= 0) return false;
      if (!q) return true;
      return b.strain.toLowerCase().includes(q) || b.room.toLowerCase().includes(q) || (b.lot || "").toLowerCase().includes(q) || SOURCE_META[b.source].label.toLowerCase().includes(q);
    }).slice(0, 25);
  }, [batches, query, reservedByBatch]);

  function pick(b) { onSelect(b); setQuery(""); setOpen(false); }

  return (
    <div style={{ position: "relative" }}>
      <input
        style={styles.input} value={query}
        onChange={(e) => { setQuery(e.target.value); setOpen(true); }}
        onFocus={() => setOpen(true)}
        onBlur={() => setTimeout(() => setOpen(false), 150)}
        placeholder={placeholder || "Search by strain, room, or batch #…"}
      />
      {open && (
        <div style={styles.searchResultsDropdown}>
          {results.length === 0 ? (
            <div style={{ ...styles.searchResultRow, color: "#7C8571", cursor: "default" }}>{query ? "No matches." : "Type to search available batches…"}</div>
          ) : results.map((b) => {
            const avail = b.remainingGrams - (reservedByBatch?.[b.id] || 0);
            return (
              <div key={b.id} style={styles.searchResultRow} onMouseDown={() => pick(b)}>
                <div style={{ fontWeight: 600, color: "#EDE8D8" }}>{b.strain} <GradeBadge grade={b.grade} isBest={b.isBest} /></div>
                <div style={{ fontSize: 11, color: "#8C9483" }}>{SOURCE_META[b.source].label} · {b.room}{b.lot ? ` · Batch ${b.lot}` : ""} · {fmtBoth(avail)} avail</div>
              </div>
            );
          })}
        </div>
      )}
    </div>
  );
}

function StrainPicker({ strains, value, onChange }) {
  const [mode, setMode] = useState(strains.length === 0 ? "manual" : "select");
  function handleSelect(v) {
    if (v === "__manual__") { setMode("manual"); onChange(""); return; }
    onChange(v);
  }
  if (mode === "manual") {
    return (
      <div>
        <input style={styles.input} value={value} onChange={(e) => onChange(e.target.value)} placeholder="Type the strain name" />
        {strains.length > 0 && <button type="button" style={styles.linkBtn} onClick={() => { setMode("select"); onChange(""); }}>← Choose from existing strains</button>}
      </div>
    );
  }
  return (
    <select style={styles.input} value={value} onChange={(e) => handleSelect(e.target.value)}>
      <option value="">Select a strain…</option>
      {strains.map((s) => <option key={s} value={s}>{s}</option>)}
      <option value="__manual__">+ Enter a new strain manually</option>
    </select>
  );
}

function groupBatches(batches, keyFn) {
  const map = new Map();
  for (const b of batches) {
    const key = keyFn(b);
    if (!map.has(key)) map.set(key, { key, total: 0, batches: [] });
    const entry = map.get(key);
    entry.total += b.remainingGrams;
    entry.batches.push(b);
  }
  return Array.from(map.values()).sort((a, b) => b.total - a.total);
}

// ---------- DASHBOARD ----------
const DIM_OPTIONS = [
  { k: "date", label: "By Date" },
  { k: "strain", label: "By Strain" },
  { k: "room", label: "By Room" },
  { k: "grade", label: "By Grade" },
];
function dimValue(dim, b) {
  if (dim === "date") return b.dateReceived;
  if (dim === "strain") return b.strain;
  if (dim === "room") return `${SOURCE_META[b.source].label} · ${b.room}`;
  if (dim === "grade") return `Grade ${(GRADE_META[b.grade] || { short: b.grade }).short}`;
  return "";
}

function Dashboard({ batches, totalOnHand, totalDaddysPipes, totalMerc, unit, photos, onUpdatePhotos, canEditPhotos, rooms, customGrades, gradeDescriptions, shipments, canEditBatch, onEditBatch, visibleSources, shipper }) {
  const [viewMode, setViewMode] = useState("source"); // "source" | "trimmedAt"
  const [expandedKey, setExpandedKey] = useState(null);
  const [bestOnly, setBestOnly] = useState(false);
  const [query, setQuery] = useState("");
  const [editingBatchId, setEditingBatchId] = useState(null);
  const [editForm, setEditForm] = useState(null);
  const [editError, setEditError] = useState("");
  const [printingId, setPrintingId] = useState(null);
  const [printError, setPrintError] = useState(null);
  const grades = allGradeCodes(customGrades);

  async function handlePrintLabel(b) {
    setPrintingId(b.id);
    setPrintError(null);
    try {
      await printZebraLabel(buildBatchLabelZPL(b, shipper, unit));
    } catch (err) {
      setPrintError({ batchId: b.id, message: err.message });
    } finally {
      setPrintingId(null);
    }
  }

  async function handleBatchPhoto(batchId, file) {
    if (!file || !onUpdatePhotos) return;
    const dataUri = await compressImage(file);
    onUpdatePhotos({ ...photos, byBatch: { ...photos.byBatch, [batchId]: dataUri } });
  }
  function shippedForBatch(batchId) {
    return (shipments || []).filter((s) => s.batchId === batchId).reduce((sum, s) => sum + s.grams, 0);
  }
  function startEdit(b) {
    setEditingBatchId(b.id);
    setEditForm({ source: b.source, room: b.room, strain: b.strain, lot: b.lot, grade: b.grade, isBest: b.isBest, isMachineTrimmed: !!b.isMachineTrimmed, trimmedAt: b.trimmedAt || "", grams: b.remainingGrams, notes: b.notes || "" });
    setEditError("");
  }
  function saveEdit(b) {
    if (!editForm.strain.trim()) return setEditError("Strain name is required.");
    const floor = shippedForBatch(b.id);
    if (editForm.grams < floor) return setEditError(`Can't go below ${fmtBoth(floor)} — that much has already shipped out from this batch.`);
    setEditError("");
    const addedSinceOriginal = editForm.grams - b.remainingGrams;
    onEditBatch(b.id, {
      source: editForm.source, room: editForm.room, strain: editForm.strain.trim(), lot: editForm.lot.trim(),
      grade: editForm.grade, isBest: editForm.grade === "A" ? editForm.isBest : false, isMachineTrimmed: editForm.isMachineTrimmed,
      trimmedAt: editForm.trimmedAt, notes: editForm.notes.trim(), receivedGrams: b.receivedGrams + addedSinceOriginal,
    });
    setEditingBatchId(null);
  }

  const bestTotal = batches.filter((b) => b.grade === "A" && b.isBest).reduce((s, b) => s + b.remainingGrams, 0);

  // Searches batch # and every package tag ever logged against a batch — a batch can carry several
  // tags across its deliveries, so this checks all of them, not just the first.
  function batchMatchesQuery(b, q) {
    const lotMatch = b.lot && b.lot.toLowerCase().includes(q);
    const tagMatch = (b.additions && b.additions.length > 0 ? b.additions : [{ packageTag: b.packageTag }])
      .some((a) => a.packageTag && a.packageTag.toLowerCase().includes(q));
    return lotMatch || tagMatch;
  }
  const searchQuery = query.trim().toLowerCase();
  const searchResults = searchQuery ? batches.filter((b) => batchMatchesQuery(b, searchQuery)) : null;

  const SECTIONS = (viewMode === "source"
    ? [{ key: "daddyspipes", label: SOURCE_META.daddyspipes.label, icon: Warehouse }, { key: "merc", label: SOURCE_META.merc.label, icon: Truck }]
    : [{ key: "daddyspipes", label: `Trimmed at ${SOURCE_META.daddyspipes.label}`, icon: Warehouse }, { key: "merc", label: `Trimmed at ${SOURCE_META.merc.label}`, icon: Truck }, { key: "", label: "Trimmed At Not Specified", icon: HelpCircle }]
  ).filter((s) => s.key === "" || !visibleSources || visibleSources.includes(s.key));

  function sectionBatches(sectionKey) {
    const list = viewMode === "source" ? batches.filter((b) => b.source === sectionKey) : batches.filter((b) => (b.trimmedAt || "") === sectionKey);
    return list;
  }
  function buildMatrix(list) {
    const map = new Map();
    list.forEach((b) => {
      if (!map.has(b.strain)) {
        const gradeTotals = {};
        grades.forEach((g) => (gradeTotals[g] = 0));
        map.set(b.strain, { strain: b.strain, gradeTotals, total: 0, hasBest: false, batches: [] });
      }
      const entry = map.get(b.strain);
      entry.gradeTotals[b.grade] = (entry.gradeTotals[b.grade] || 0) + b.remainingGrams;
      entry.total += b.remainingGrams;
      if (b.grade === "A" && b.isBest) entry.hasBest = true;
      entry.batches.push(b);
    });
    return Array.from(map.values()).sort((a, b) => a.strain.localeCompare(b.strain)).map((entry) => ({
      ...entry,
      // Sort each strain's batches by room in natural order so F2 comes before F10 (not alphabetical).
      batches: [...entry.batches].sort((x, y) => naturalCompare(x.room, y.room)),
    }));
  }

  function renderBatchRow(b) {
    return editingBatchId === b.id ? (
      <div key={b.id} style={styles.editBatchForm}>
        <div style={styles.twoCol}>
          <div><FieldLabel>Strain</FieldLabel><input style={styles.input} value={editForm.strain} onChange={(e) => setEditForm({ ...editForm, strain: e.target.value })} /></div>
          <div><FieldLabel>Batch #</FieldLabel><input style={styles.input} value={editForm.lot} onChange={(e) => setEditForm({ ...editForm, lot: e.target.value })} /></div>
        </div>
        <FieldLabel>Source</FieldLabel>
        <div style={styles.sourceChoice}>
          {["daddyspipes", "merc"].map((s) => (
            <button type="button" key={s} onClick={() => setEditForm({ ...editForm, source: s, room: (rooms[s] || [])[0] || "" })} style={{ ...styles.sourceChoiceBtn, borderColor: editForm.source === s ? SOURCE_META[s].color : "#2A3324", background: editForm.source === s ? SOURCE_META[s].bg : "transparent", color: editForm.source === s ? SOURCE_META[s].color : "#B9BFA9" }}>{SOURCE_META[s].label}</button>
          ))}
        </div>
        <FieldLabel>Room</FieldLabel>
        <select style={styles.input} value={editForm.room} onChange={(e) => setEditForm({ ...editForm, room: e.target.value })}>
          {(rooms[editForm.source] || []).map((r) => <option key={r} value={r}>{r}</option>)}
        </select>
        <FieldLabel>Grade</FieldLabel>
        <div style={styles.gradeChoice}>
          {grades.map((gCode) => {
            const meta = gradeMetaFor(gCode, customGrades);
            return <button type="button" key={gCode} title={gradeDescriptionFor(gCode, gradeDescriptions, customGrades)} onClick={() => setEditForm({ ...editForm, grade: gCode })} style={{ ...styles.gradeChoiceBtn, borderColor: editForm.grade === gCode ? meta.color : "#2A3324", color: editForm.grade === gCode ? meta.color : "#B9BFA9", background: editForm.grade === gCode ? "rgba(201,162,75,0.10)" : "transparent" }}>{meta.label}</button>;
          })}
        </div>
        {editForm.grade === "A" && (
          <label style={styles.bestCheckbox}>
            <input type="checkbox" checked={editForm.isBest} onChange={(e) => setEditForm({ ...editForm, isBest: e.target.checked })} />
            <Star size={13} color="#C9A24B" fill={editForm.isBest ? "#C9A24B" : "none"} /> Mark as Best A
          </label>
        )}
        <label style={styles.bestCheckbox}>
          <input type="checkbox" checked={editForm.isMachineTrimmed} onChange={(e) => setEditForm({ ...editForm, isMachineTrimmed: e.target.checked })} />
          <Cog size={13} color="#8FAF8B" /> Machine Trimmed
        </label>
        <FieldLabel>Trimmed At</FieldLabel>
        <div style={styles.sourceChoice}>
          {["", "daddyspipes", "merc"].map((s) => (
            <button type="button" key={s || "none"} onClick={() => setEditForm({ ...editForm, trimmedAt: s })} style={{ ...styles.sourceChoiceBtn, borderColor: editForm.trimmedAt === s ? (s ? SOURCE_META[s].color : "#C9A24B") : "#2A3324", background: editForm.trimmedAt === s ? (s ? SOURCE_META[s].bg : "rgba(201,162,75,0.10)") : "transparent", color: editForm.trimmedAt === s ? (s ? SOURCE_META[s].color : "#C9A24B") : "#B9BFA9" }}>{s === "" ? "N/A" : SOURCE_META[s].label}</button>
          ))}
        </div>
        <FieldLabel>Weight on hand</FieldLabel>
        <DualWeightInput grams={editForm.grams} onChange={(v) => setEditForm({ ...editForm, grams: v })} />
        <FieldLabel>Notes</FieldLabel>
        <textarea style={{ ...styles.input, minHeight: 50 }} value={editForm.notes} onChange={(e) => setEditForm({ ...editForm, notes: e.target.value })} />
        {editError && <div style={styles.errorText}>{editError}</div>}
        <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
          <button type="button" style={styles.submitBtn} onClick={() => saveEdit(b)}>Save</button>
          <button type="button" style={styles.backBtn} onClick={() => setEditingBatchId(null)}>Cancel</button>
        </div>
      </div>
    ) : (
      <div key={b.id}>
        <div style={styles.batchRow}>
          <OriginDot origin={b.origin} source={b.source} />
          <span style={styles.batchLot}>
            {b.strain} · {b.room} · <GradeBadge grade={b.grade} isBest={b.isBest} />{b.lot ? ` · Batch ${b.lot}` : ""}
            {b.isMachineTrimmed && <span style={styles.machineTag} title="Machine Trimmed — internal only"><Cog size={10} /> MT</span>}
            {b.trimmedAt && <span style={{ fontSize: 10.5, color: "#6B7264" }}> · trimmed at {SOURCE_META[b.trimmedAt].label}</span>}
          </span>
          <span style={styles.batchDate}>{b.dateReceived}</span>
          <span style={styles.batchWeight}>{fmtWeight(b.remainingGrams, unit)}</span>
          <button type="button" style={styles.batchPhotoBtn} title="Print Zebra label" onClick={() => handlePrintLabel(b)} disabled={printingId === b.id}>
            {printingId === b.id ? <Loader2 size={12} color="#8FAF8B" className="animate-spin" /> : <Printer size={12} color="#8FAF8B" />}
          </button>
          {canEditPhotos && (
            <label style={styles.batchPhotoBtn} title={photos?.byBatch?.[b.id] ? "Replace photo" : "Add photo"}>
              {photos?.byBatch?.[b.id] ? <img src={photos.byBatch[b.id]} style={{ width: 18, height: 18, borderRadius: 3, objectFit: "cover" }} /> : <ImageIcon size={13} color="#8FAF8B" />}
              <input type="file" accept="image/*" style={{ display: "none" }} onChange={(e) => handleBatchPhoto(b.id, e.target.files[0])} />
            </label>
          )}
          {canEditBatch && (
            <button type="button" style={styles.batchPhotoBtn} title="Edit this batch" onClick={() => startEdit(b)}><Edit3 size={12} color="#8FAF8B" /></button>
          )}
        </div>
        {printError && printError.batchId === b.id && (
          <div style={{ ...styles.errorText, marginTop: 2, marginBottom: 6 }}>{printError.message}</div>
        )}
      </div>
    );
  }

  return (
    <div>
      <div style={styles.summaryRow}>
        <SummaryCard label="Total On Hand" value={fmtWeight(totalOnHand, unit)} accent="#EDE8D8" />
        {(!visibleSources || visibleSources.includes("daddyspipes")) && <SummaryCard label={SOURCE_META.daddyspipes.label} value={fmtWeight(totalDaddysPipes, unit)} accent={SOURCE_META.daddyspipes.color} />}
        {(!visibleSources || visibleSources.includes("merc")) && <SummaryCard label={SOURCE_META.merc.label} value={fmtWeight(totalMerc, unit)} accent={SOURCE_META.merc.color} />}
        {bestTotal > 0 && <SummaryCard label="★ Best A" value={fmtWeight(bestTotal, unit)} accent="#C9A24B" />}
      </div>
      <div style={styles.dashControls}>
        <div style={styles.groupToggle}>
          <button onClick={() => setViewMode("source")} style={{ ...styles.groupBtn, background: viewMode === "source" ? "#C9A24B" : "transparent", color: viewMode === "source" ? "#12160F" : "#B9BFA9" }}>By Location</button>
          <button onClick={() => setViewMode("trimmedAt")} style={{ ...styles.groupBtn, background: viewMode === "trimmedAt" ? "#C9A24B" : "transparent", color: viewMode === "trimmedAt" ? "#12160F" : "#B9BFA9" }}>By Location Trimmed</button>
        </div>
        <button onClick={() => setBestOnly(!bestOnly)} style={{ ...styles.bestFilterBtn, background: bestOnly ? "rgba(201,162,75,0.18)" : "transparent", borderColor: bestOnly ? "#C9A24B" : "#2A3324", color: bestOnly ? "#C9A24B" : "#8C9483" }}>
          <Star size={12} fill={bestOnly ? "#C9A24B" : "none"} /> Best A only
        </button>
      </div>
      <div style={styles.searchBar}>
        <Search size={15} color="#7C8571" />
        <input style={styles.searchInput} placeholder="Search by batch # or Metrc package tag…" value={query} onChange={(e) => setQuery(e.target.value)} />
      </div>
      <div style={{ display: "flex", alignItems: "center", gap: 16, margin: "10px 0 6px", fontSize: 11.5, color: "#8C9483" }}>
        <span style={{ display: "flex", alignItems: "center", gap: 5 }}><OriginDot source="daddyspipes" /> Grown at {SOURCE_META.daddyspipes.label}</span>
        <span style={{ display: "flex", alignItems: "center", gap: 5 }}><OriginDot source="merc" /> Grown at {SOURCE_META.merc.label}</span>
      </div>
      {!searchQuery && <div style={{ fontSize: 11, color: "#6B7264", margin: "4px 0 16px" }}>Every grade shown at a glance — tap a strain to see individual batches, add photos, or edit.</div>}

      {searchQuery ? (
        <div style={{ marginTop: 12 }}>
          <div style={{ fontSize: 12, color: "#8C9483", marginBottom: 10 }}>{searchResults.length} batch{searchResults.length === 1 ? "" : "es"} matching "{query.trim()}"</div>
          {searchResults.length === 0 && <div style={styles.emptyState}>No batches match that batch # or package tag.</div>}
          {searchResults.length > 0 && <div style={styles.batchList}>{searchResults.map((b) => renderBatchRow(b))}</div>}
        </div>
      ) : (
      SECTIONS.map((section) => {
        const list = sectionBatches(section.key);
        const sectionTotal = list.reduce((s, b) => s + b.remainingGrams, 0);
        let rows = buildMatrix(list);
        if (bestOnly) rows = rows.filter((r) => r.hasBest);
        if (list.length === 0 && rows.length === 0) return null;
        const Icon = section.icon;
        return (
          <div key={section.key || "none"} style={styles.sourceSection}>
            <div style={styles.sourceSectionHeader}>
              <Icon size={15} color={section.key ? SOURCE_META[section.key].color : "#8C9483"} />
              <span>{section.label}</span>
              <span style={styles.sourceSectionTotal}>{fmtWeight(sectionTotal, unit)}</span>
            </div>
            {rows.length === 0 ? (
              <div style={styles.emptyState}>{bestOnly ? "No Best-A flower here right now." : "Nothing here yet."}</div>
            ) : (
              <div style={{ overflowX: "auto" }}>
                <table style={styles.matrixTable}>
                  <thead>
                    <tr>
                      <th style={styles.matrixTh}>Strain</th>
                      {grades.map((g) => <th key={g} style={styles.matrixTh}>{gradeMetaFor(g, customGrades).short}</th>)}
                      <th style={styles.matrixTh}>Total</th>
                    </tr>
                  </thead>
                  <tbody>
                    {rows.map((row) => {
                      const rowKey = `${section.key}::${row.strain}`;
                      const isOpen = expandedKey === rowKey;
                      const low = row.total < LOW_STOCK_G;
                      return (
                        <React.Fragment key={rowKey}>
                          <tr style={styles.matrixRow} onClick={() => setExpandedKey(isOpen ? null : rowKey)}>
                            <td style={styles.matrixTdStrain}>
                              {row.strain}
                              {(() => {
                                const origins = Array.from(new Set(row.batches.map((b) => b.origin || b.source)));
                                return origins.map((src) => <OriginDot key={src} origin={src} style={{ marginLeft: 5 }} />);
                              })()}
                              {row.hasBest && <Star size={11} color="#C9A24B" fill="#C9A24B" style={{ marginLeft: 5, verticalAlign: "middle" }} />}
                              {low && <span style={{ ...styles.lowTag, marginLeft: 6 }}><AlertTriangle size={10} /> LOW</span>}
                            </td>
                            {grades.map((g) => <td key={g} style={styles.matrixTd}>{row.gradeTotals[g] > 0 ? fmtWeight(row.gradeTotals[g], unit) : "—"}</td>)}
                            <td style={{ ...styles.matrixTd, fontWeight: 700, color: "#EDE8D8" }}>{fmtWeight(row.total, unit)}</td>
                          </tr>
                          {isOpen && (
                            <tr>
                              <td colSpan={grades.length + 2} style={{ padding: "4px 0 10px" }}>
                                <div style={styles.batchList}>{row.batches.map((b) => renderBatchRow(b))}</div>
                              </td>
                            </tr>
                          )}
                        </React.Fragment>
                      );
                    })}
                  </tbody>
                </table>
              </div>
            )}
          </div>
        );
      }))}
    </div>
  );
}

function ReceiveForm({ onSubmitManifest, currentUser, rooms, strains, customGrades, gradeDescriptions, existingBatches, enteringFacility, shipper, unit }) {
  // Manifest-level fields — entered once, shared by everything logged in this session.
  const [manifestNumber, setManifestNumber] = useState("");
  const [date, setDate] = useState(todayISO());

  // The facility physically receiving/holding this product (where it lives now). Everything logged
  // in this form gets this as its location. Origin (who grew it) is picked per-item below.
  const location = enteringFacility || "daddyspipes";

  // Per-item fields — these can differ from line to line within the same manifest.
  // `source` here is the ORIGIN — who grew it. Bulk flower can be grown at either facility (MERC
  // sends theirs down to Daddy's), so both options are always offered regardless of who's entering.
  const [source, setSource] = useState("daddyspipes");
  const roomList = rooms[source] || [];  // rooms follow origin (where grown), so MERC origin shows F1–F15
  const [room, setRoom] = useState(roomList[0] || "");
  const [strain, setStrain] = useState("");
  const [lot, setLot] = useState("");
  const [packageTag, setPackageTag] = useState("");
  const [grade, setGrade] = useState("A");
  const [isBest, setIsBest] = useState(false);
  const [isMachineTrimmed, setIsMachineTrimmed] = useState(false);
  const [trimmedAt, setTrimmedAt] = useState("");
  const [grams, setGrams] = useState(0);
  const [itemNotes, setItemNotes] = useState("");

  const [lineItems, setLineItems] = useState([]);
  const [error, setError] = useState("");
  const [justLogged, setJustLogged] = useState(null);
  const [printingId, setPrintingId] = useState(null);
  const [printError, setPrintError] = useState("");
  const grades = allGradeCodes(customGrades);

  const recentBatches = useMemo(() => recentDistinct(
    existingBatches,
    (b) => `${b.source}__${b.room}__${b.grade}__${b.lot.toLowerCase()}__${b.strain.toLowerCase()}`,
    "additions"
  ), [existingBatches]);

  function applyQuickFill(b) {
    setSource(b.source); setRoom(b.room); setStrain(b.strain); setLot(b.lot);
    setGrade(b.grade); setIsBest(b.isBest || false); setIsMachineTrimmed(!!b.isMachineTrimmed); setTrimmedAt(b.trimmedAt || "");
  }

  function handleSourceChange(s) { setSource(s); setRoom((rooms[s] || [])[0] || ""); }

  function clearItemFields() {
    setStrain(""); setLot(""); setPackageTag(""); setGrams(0); setItemNotes(""); setIsBest(false); setIsMachineTrimmed(false); setTrimmedAt("");
  }

  function currentItemFromForm() {
    return { source, location, room, strain: strain.trim(), lot: lot.trim(), packageTag: packageTag.trim(), grade, isBest, isMachineTrimmed, trimmedAt, grams, notes: itemNotes.trim() };
  }

  function addLine() {
    if (!strain.trim()) return setError("Strain name is required for this item.");
    if (grams <= 0) return setError("Enter a weight greater than zero for this item.");
    if (packageTag.trim()) {
      const owner = findPackageTagOwner(packageTag, existingBatches, "additions", lineItems);
      if (owner && !confirm(`Package tag …${packageTag.trim()} was already logged for ${owner}. Add this item anyway?`)) return;
    }
    setError("");
    setLineItems([...lineItems, { id: uid(), ...currentItemFromForm() }]);
    clearItemFields();
  }

  function removeLine(id) { setLineItems(lineItems.filter((l) => l.id !== id)); }
  const manifestTotal = lineItems.reduce((s, l) => s + l.grams, 0);

  async function handlePrintLabel(entry) {
    setPrintingId(entry.id);
    setPrintError("");
    try {
      await printZebraLabel(buildBatchLabelZPL(entry.batchForLabel, shipper, unit));
    } catch (err) {
      setPrintError(err.message);
    } finally {
      setPrintingId(null);
    }
  }

  function handleSubmit(e) {
    e.preventDefault();
    let items = [...lineItems];
    // Whatever's still sitting in the item form gets folded in automatically — so a single
    // delivery can be logged without ever touching "Add Item to Manifest".
    const hasCurrentInput = strain.trim() || lot.trim() || grams > 0 || itemNotes.trim();
    if (hasCurrentInput) {
      if (!strain.trim()) return setError("Strain name is required.");
      if (grams <= 0) return setError("Enter a weight greater than zero.");
      if (packageTag.trim()) {
        const owner = findPackageTagOwner(packageTag, existingBatches, "additions", lineItems);
        if (owner && !confirm(`Package tag …${packageTag.trim()} was already logged for ${owner}. Log this item anyway?`)) return;
      }
      items = [...items, currentItemFromForm()];
    }
    if (items.length === 0) return setError("Add at least one item before logging.");
    setError("");
    setPrintError("");

    const manifestNum = manifestNumber.trim() || undefined;
    const results = [];
    const localNew = []; // batches created earlier in this same submission, so repeated lines can merge into each other
    const newStrainsUsed = new Set();

    items.forEach((it) => {
      const strainTrim = it.strain.trim();
      const lotTrim = it.lot.trim();
      if (strainTrim && !strains.some((s) => s.toLowerCase() === strainTrim.toLowerCase())) newStrainsUsed.add(strainTrim);

      const matchExisting = lotTrim && (existingBatches || []).find((b) =>
        b.source === it.source && b.room === it.room && b.grade === it.grade &&
        b.lot.trim().toLowerCase() === lotTrim.toLowerCase() &&
        b.strain.trim().toLowerCase() === strainTrim.toLowerCase());
      const matchLocal = !matchExisting && lotTrim && localNew.find((b) =>
        b.source === it.source && b.room === it.room && b.grade === it.grade &&
        b.lot.toLowerCase() === lotTrim.toLowerCase() && b.strain.toLowerCase() === strainTrim.toLowerCase());

      const addition = { grams: it.grams, date, staff: currentUser.displayName, notes: it.notes, createdAt: Date.now(), manifestNumber: manifestNum, packageTag: it.packageTag || undefined };

      if (matchExisting) {
        results.push({
          type: "merge", batchId: matchExisting.id, addition,
          display: { merged: true, strain: strainTrim, lot: lotTrim, grams: it.grams, batchForLabel: { ...matchExisting, remainingGrams: matchExisting.remainingGrams + it.grams, additions: [...(matchExisting.additions || []), addition] } },
        });
      } else if (matchLocal) {
        matchLocal.receivedGrams += it.grams;
        matchLocal.additions.push(addition);
        const existingResult = results.find((r) => r.type === "new" && r.batch.id === matchLocal.id);
        if (existingResult) {
          existingResult.display = { ...existingResult.display, grams: existingResult.display.grams + it.grams, batchForLabel: { ...matchLocal, remainingGrams: matchLocal.receivedGrams } };
        }
      } else {
        const newBatch = {
          id: uid(), source: it.source, origin: it.source, location: it.location || "daddyspipes", room: it.room, strain: strainTrim, lot: lotTrim, grade: it.grade,
          isBest: it.grade === "A" ? it.isBest : false, isMachineTrimmed: it.isMachineTrimmed, trimmedAt: it.trimmedAt,
          receivedGrams: it.grams, dateReceived: date, staff: currentUser.displayName, notes: it.notes, createdAt: Date.now(),
          manifestNumber: manifestNum, packageTag: it.packageTag || undefined, additions: [addition],
        };
        localNew.push(newBatch);
        results.push({ type: "new", batch: newBatch, display: { merged: false, strain: strainTrim, lot: lotTrim, grams: it.grams, batchForLabel: { ...newBatch, remainingGrams: it.grams } } });
      }
    });

    onSubmitManifest(results, Array.from(newStrainsUsed));
    setJustLogged({
      count: items.length, total: items.reduce((s, i) => s + i.grams, 0), manifestNumber: manifestNum,
      entries: results.map((r, i) => ({ id: `${Date.now()}-${i}`, ...r.display })),
    });
    setLineItems([]);
    clearItemFields();
  }

  return (
    <form onSubmit={handleSubmit} style={styles.form}>
      <div style={styles.formHeader}><ArrowDownCircle size={18} color="#7C9473" /><span>Intake Flower</span></div>

      {justLogged && (
        <div style={styles.noticeBox}>
          <div>
            Logged {justLogged.count} item{justLogged.count > 1 ? "s" : ""} ({fmtBoth(justLogged.total)} total){justLogged.manifestNumber ? ` — Manifest ${justLogged.manifestNumber}` : ""}.
          </div>
          <div style={{ marginTop: 8, display: "flex", flexDirection: "column", gap: 6 }}>
            {justLogged.entries.map((entry) => (
              <div key={entry.id} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", fontSize: 12.5, gap: 8 }}>
                <span>{entry.merged ? "Added to" : "New batch:"} {entry.strain}{entry.lot ? ` (${entry.lot})` : ""} — {fmtBoth(entry.grams)}</span>
                <button type="button" style={styles.iconLabelBtn} onClick={() => handlePrintLabel(entry)} disabled={printingId === entry.id}>
                  <Printer size={12} /> {printingId === entry.id ? "Printing…" : "Print Label"}
                </button>
              </div>
            ))}
          </div>
          {printError && <div style={{ ...styles.errorText, marginTop: 6 }}>{printError}</div>}
        </div>
      )}

      <div style={styles.twoCol}>
        <div><FieldLabel>Manifest #</FieldLabel><input style={styles.input} value={manifestNumber} onChange={(e) => setManifestNumber(e.target.value)} placeholder="Metrc manifest number" /></div>
        <div><FieldLabel>Date received</FieldLabel><input style={styles.input} type="date" value={date} onChange={(e) => setDate(e.target.value)} /></div>
      </div>
      <div style={{ fontSize: 12, color: "#8C9483", margin: "-4px 0 12px" }}>These two apply to everything logged below. For a manifest with several items, add each one, then log the whole thing at once.</div>

      <QuickFillStrip
        items={recentBatches}
        renderLabel={(b) => (<>{b.lot ? `${b.lot} — ` : ""}{b.strain} <GradeBadge grade={b.grade} isBest={b.isBest} /></>)}
        onPick={applyQuickFill}
      />

      <FieldLabel>Grown At (Origin)</FieldLabel>
      <div style={styles.sourceChoice}>
        {["daddyspipes", "merc"].map((s) => (
          <button type="button" key={s} onClick={() => handleSourceChange(s)} style={{ ...styles.sourceChoiceBtn, borderColor: source === s ? SOURCE_META[s].color : "#2A3324", background: source === s ? SOURCE_META[s].bg : "transparent", color: source === s ? SOURCE_META[s].color : "#B9BFA9" }}>
            <OriginDot origin={s} style={{ marginRight: 2 }} />{SOURCE_META[s].label}
          </button>
        ))}
      </div>
      <FieldLabel>Room</FieldLabel>
      <select style={styles.input} value={room} onChange={(e) => setRoom(e.target.value)}>
        {roomList.map((r) => <option key={r} value={r}>{r}</option>)}
      </select>
      <FieldLabel>Strain</FieldLabel>
      <StrainPicker strains={strains} value={strain} onChange={setStrain} />
      <FieldLabel>Batch #</FieldLabel>
      <input style={styles.input} value={lot} onChange={(e) => setLot(e.target.value)} placeholder="If this matches an existing batch, the new weight is added to it" />
      <FieldLabel>Package Tag (last 4 digits)</FieldLabel>
      <input style={styles.input} value={packageTag} onChange={(e) => setPackageTag(e.target.value)} placeholder="e.g. 4821 — same batch can carry several tags" maxLength={8} />
      <FieldLabel>Grade</FieldLabel>
      <div style={styles.gradeChoice}>
        {grades.map((g) => {
          const meta = gradeMetaFor(g, customGrades);
          return <button type="button" key={g} title={gradeDescriptionFor(g, gradeDescriptions, customGrades)} onClick={() => setGrade(g)} style={{ ...styles.gradeChoiceBtn, borderColor: grade === g ? meta.color : "#2A3324", color: grade === g ? meta.color : "#B9BFA9", background: grade === g ? "rgba(201,162,75,0.10)" : "transparent" }}>{meta.label}</button>;
        })}
      </div>
      {grade === "A" && (
        <label style={styles.bestCheckbox}>
          <input type="checkbox" checked={isBest} onChange={(e) => setIsBest(e.target.checked)} />
          <Star size={13} color="#C9A24B" fill={isBest ? "#C9A24B" : "none"} /> Mark as Best A (exceptional yield/quality)
        </label>
      )}
      <label style={styles.bestCheckbox}>
        <input type="checkbox" checked={isMachineTrimmed} onChange={(e) => setIsMachineTrimmed(e.target.checked)} />
        <Cog size={13} color="#8FAF8B" /> Machine Trimmed <span style={{ color: "#6B7264", fontWeight: 400 }}>— internal only, never shown to customers</span>
      </label>
      <FieldLabel>Trimmed At (optional)</FieldLabel>
      <div style={styles.sourceChoice}>
        {["", "daddyspipes", "merc"].map((s) => (
          <button type="button" key={s || "none"} onClick={() => setTrimmedAt(s)} style={{ ...styles.sourceChoiceBtn, borderColor: trimmedAt === s ? (s ? SOURCE_META[s].color : "#C9A24B") : "#2A3324", background: trimmedAt === s ? (s ? SOURCE_META[s].bg : "rgba(201,162,75,0.10)") : "transparent", color: trimmedAt === s ? (s ? SOURCE_META[s].color : "#C9A24B") : "#B9BFA9" }}>
            {s === "" ? "Not sure / N/A" : SOURCE_META[s].label}
          </button>
        ))}
      </div>
      <FieldLabel>Weight</FieldLabel>
      <DualWeightInput grams={grams} onChange={setGrams} />
      <FieldLabel>Notes (optional)</FieldLabel>
      <textarea style={{ ...styles.input, minHeight: 50 }} value={itemNotes} onChange={(e) => setItemNotes(e.target.value)} placeholder="COA received, moisture content, etc." />

      <button type="button" style={{ ...styles.submitBtn, background: "#7C9473", marginTop: 10 }} onClick={addLine}><Plus size={16} /> Add Item to Manifest</button>

      {lineItems.length > 0 && (
        <div style={styles.lineItemList}>
          {lineItems.map((l) => (
            <div key={l.id} style={styles.lineItemRow}>
              <div style={styles.lineItemStrain}>
                {l.strain} · {SOURCE_META[l.source].label} · {l.room} · <GradeBadge grade={l.grade} isBest={l.isBest} />{l.lot ? ` · ${l.lot}` : ""}{l.packageTag ? ` · Tag …${l.packageTag}` : ""}
              </div>
              <div style={styles.lineItemRight}>
                <span style={styles.lineItemTotal}>{fmtBoth(l.grams)}</span>
                <button type="button" onClick={() => removeLine(l.id)} style={styles.iconBtn}><X size={14} /></button>
              </div>
            </div>
          ))}
          <div style={styles.subtotalRow}><span>Manifest total so far</span><span style={styles.subtotalValue}>{fmtBoth(manifestTotal)}</span></div>
        </div>
      )}

      {error && <div style={styles.errorText}>{error}</div>}
      <button type="submit" style={styles.submitBtn}><Plus size={16} /> {lineItems.length > 0 ? "Log Full Manifest" : "Log Intake"}</button>
    </form>
  );
}

// ---------- TRIMMER PICKER ----------
function TrimmerPicker({ trimmers, value, onChange }) {
  const [open, setOpen] = useState(false);
  const [query, setQuery] = useState(null);
  const boxRef = React.useRef(null);
  const shown = query !== null ? query : (value || "");
  const matches = React.useMemo(() => {
    const q = (query || "").trim().toLowerCase();
    const list = q === "" ? trimmers : trimmers.filter((t) => t.toLowerCase().includes(q));
    return list.slice(0, 8);
  }, [trimmers, query]);
  const exactExists = trimmers.some((t) => t.toLowerCase() === (query || "").trim().toLowerCase());
  useEffect(() => {
    function onDocClick(e) { if (boxRef.current && !boxRef.current.contains(e.target)) { setOpen(false); setQuery(null); } }
    document.addEventListener("mousedown", onDocClick);
    return () => document.removeEventListener("mousedown", onDocClick);
  }, []);
  function pick(name) { onChange(name); setQuery(null); setOpen(false); }
  return (
    <div ref={boxRef} style={{ position: "relative" }}>
      <input
        style={styles.input}
        value={shown}
        placeholder="Search or type a trimmer's name"
        onFocus={() => { setOpen(true); setQuery(value || ""); }}
        onChange={(e) => { setQuery(e.target.value); onChange(e.target.value); setOpen(true); }}
      />
      {open && (matches.length > 0 || (query && query.trim() && !exactExists)) && (
        <div style={styles.trimmerDropdown}>
          {matches.map((t) => (
            <div key={t} className="trimmer-opt" style={styles.trimmerOption} onMouseDown={() => pick(t)}>{t}</div>
          ))}
          {query && query.trim() && !exactExists && (
            <div className="trimmer-opt" style={{ ...styles.trimmerOption, color: "#C9A24B", borderTop: matches.length ? "1px solid #2A3324" : "none" }} onMouseDown={() => pick(query.trim())}>
              + Add "{query.trim()}"
            </div>
          )}
        </div>
      )}
    </div>
  );
}

// ---------- BUCKED FLOWER INTAKE ----------
function lotTypeLabel(l) { return l.type === "harvest" ? "Harvest" : "Bucked Flower"; }

function BuckedLotPicker({ lots, selectedId, onSelect, unit, requireRemaining = true, includeArchived = false }) {
  const [query, setQuery] = useState("");
  const active = lots.filter((l) => (includeArchived || !l.archived) && (!requireRemaining || l.remainingGrams > 0));
  const q = query.trim().toLowerCase();
  const filtered = q ? active.filter((l) => l.lot.toLowerCase().includes(q) || l.strain.toLowerCase().includes(q)) : active;
  const selected = lots.find((l) => l.id === selectedId);

  if (selected) {
    return (
      <div style={styles.noticeBox}>
        <div style={{ fontWeight: 600 }}>{selected.strain} — Batch {selected.lot} <span style={{ fontWeight: 400, fontSize: 11.5, color: "#9BA38C" }}>({lotTypeLabel(selected)}{selected.archived ? " · Finished" : ""})</span></div>
        <div style={{ fontSize: 12, color: "#9BA38C", marginTop: 4 }}>
          {SOURCE_META[selected.source].label} · {selected.room} · {fmtWeight(selected.remainingGrams, unit)} left of {fmtWeight(selected.receivedGrams, unit)}
        </div>
        <button type="button" style={{ ...styles.linkBtn, marginTop: 6 }} onClick={() => onSelect(null)}>← Choose a different batch</button>
      </div>
    );
  }

  return (
    <div>
      <input style={styles.input} value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search by batch # or strain…" />
      <div style={{ ...styles.historyList, marginTop: 8, maxHeight: 260, overflowY: "auto" }}>
        {filtered.length === 0 && <div style={styles.emptyState}>{active.length === 0 ? "No bucked flower on hand — log it through Intake first." : "No matches."}</div>}
        {filtered.map((l) => (
          <div key={l.id} style={{ ...styles.historyRow, cursor: "pointer" }} onClick={() => onSelect(l.id)}>
            <div style={{ flex: 1 }}>
              <div style={styles.historyTop}>
                <span style={styles.historyStrain}>{l.strain} — Batch {l.lot}</span>
                <span style={styles.historyWeight}>{fmtWeight(l.remainingGrams, unit)} left</span>
              </div>
              <div style={styles.historyMeta}>{lotTypeLabel(l)}{l.archived ? " · Finished" : ""} · {SOURCE_META[l.source].label} · {l.room}</div>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}

function BuckedIntakeForm({ onSubmitManifest, currentUser, rooms, strains, existingLots, enteringFacility }) {
  const [manifestNumber, setManifestNumber] = useState("");
  const [date, setDate] = useState(todayISO());

  const location = enteringFacility || "daddyspipes";

  // `source` here is the ORIGIN — who grew it. Bucked flower can be grown at either facility, so
  // both options are always offered; location is set to whoever's entering it.
  const [source, setSource] = useState("merc");
  const roomList = rooms[source] || [];  // rooms follow origin (where grown)
  const [room, setRoom] = useState(roomList[0] || "");
  const [strain, setStrain] = useState("");
  const [lot, setLot] = useState("");
  const [packageTag, setPackageTag] = useState("");
  const [grams, setGrams] = useState(0);
  const [itemNotes, setItemNotes] = useState("");

  const [lineItems, setLineItems] = useState([]);
  const [error, setError] = useState("");
  const [justLogged, setJustLogged] = useState(null);

  const recentLots = useMemo(() => recentDistinct(
    existingLots,
    (l) => `${l.source}__${l.room}__${l.lot.toLowerCase()}__${l.strain.toLowerCase()}`,
    "deliveries"
  ), [existingLots]);

  function applyQuickFill(l) { setSource(l.source); setRoom(l.room); setStrain(l.strain); setLot(l.lot); }

  function handleSourceChange(s) { setSource(s); setRoom((rooms[s] || [])[0] || ""); }
  function clearItemFields() { setStrain(""); setLot(""); setPackageTag(""); setGrams(0); setItemNotes(""); }
  function currentItemFromForm() { return { source, location, room, strain: strain.trim(), lot: lot.trim(), packageTag: packageTag.trim(), grams, notes: itemNotes.trim() }; }

  function addLine() {
    if (!strain.trim()) return setError("Strain name is required for this item.");
    if (!lot.trim()) return setError("Batch # is required for this item.");
    if (grams <= 0) return setError("Enter a weight greater than zero for this item.");
    if (packageTag.trim()) {
      const owner = findPackageTagOwner(packageTag, existingLots, "deliveries", lineItems);
      if (owner && !confirm(`Package tag …${packageTag.trim()} was already logged for ${owner}. Add this item anyway?`)) return;
    }
    setError("");
    setLineItems([...lineItems, { id: uid(), ...currentItemFromForm() }]);
    clearItemFields();
  }
  function removeLine(id) { setLineItems(lineItems.filter((l) => l.id !== id)); }
  const manifestTotal = lineItems.reduce((s, l) => s + l.grams, 0);

  function handleSubmit(e) {
    e.preventDefault();
    let items = [...lineItems];
    const hasCurrentInput = strain.trim() || lot.trim() || packageTag.trim() || grams > 0 || itemNotes.trim();
    if (hasCurrentInput) {
      if (!strain.trim()) return setError("Strain name is required.");
      if (!lot.trim()) return setError("Batch # is required.");
      if (grams <= 0) return setError("Enter a weight greater than zero.");
      if (packageTag.trim()) {
        const owner = findPackageTagOwner(packageTag, existingLots, "deliveries", lineItems);
        if (owner && !confirm(`Package tag …${packageTag.trim()} was already logged for ${owner}. Log this item anyway?`)) return;
      }
      items = [...items, currentItemFromForm()];
    }
    if (items.length === 0) return setError("Add at least one item before logging.");
    setError("");

    const manifestNum = manifestNumber.trim() || undefined;
    const results = [];
    const localNew = []; // lots created earlier in this same submission, so repeated lines can merge into each other
    const newStrainsUsed = new Set();

    items.forEach((it) => {
      const strainTrim = it.strain.trim();
      const lotTrim = it.lot.trim();
      if (strainTrim && !strains.some((s) => s.toLowerCase() === strainTrim.toLowerCase())) newStrainsUsed.add(strainTrim);

      const delivery = { grams: it.grams, date, manifestNumber: manifestNum, packageTag: it.packageTag || undefined, staff: currentUser.displayName, notes: it.notes, createdAt: Date.now() };

      // Same batch # + strain + source + room, even under a different manifest weeks later, is the
      // same harvest — it adds to the running lot instead of creating a duplicate. Matching on room
      // too (not just source+strain+lot) mirrors exactly how graded-flower Intake merges deliveries.
      // Lots already marked finished are excluded — a batch # showing up again after being closed
      // out starts a fresh lot rather than silently reopening the old one.
      const matchExisting = (existingLots || []).find((l) =>
        !l.archived && (l.type || "bucked") === "bucked" && l.source === it.source && l.room === it.room && l.strain.trim().toLowerCase() === strainTrim.toLowerCase() && l.lot.trim().toLowerCase() === lotTrim.toLowerCase());
      const matchLocal = !matchExisting && localNew.find((l) =>
        l.source === it.source && l.room === it.room && l.strain.toLowerCase() === strainTrim.toLowerCase() && l.lot.toLowerCase() === lotTrim.toLowerCase());

      if (matchExisting) {
        results.push({ type: "merge", lotId: matchExisting.id, delivery, display: { merged: true, strain: strainTrim, lot: lotTrim, grams: it.grams } });
      } else if (matchLocal) {
        matchLocal.receivedGrams += it.grams;
        matchLocal.deliveries.push(delivery);
        const existingResult = results.find((r) => r.type === "new" && r.lot.id === matchLocal.id);
        if (existingResult) existingResult.display = { ...existingResult.display, grams: existingResult.display.grams + it.grams };
      } else {
        const newLot = {
          id: uid(), type: "bucked", source: it.source, origin: it.source, location: it.location || "daddyspipes", room: it.room, strain: strainTrim, lot: lotTrim,
          receivedGrams: it.grams, createdAt: Date.now(), archived: false,
          deliveries: [delivery],
        };
        localNew.push(newLot);
        results.push({ type: "new", lot: newLot, display: { merged: false, strain: strainTrim, lot: lotTrim, grams: it.grams } });
      }
    });

    onSubmitManifest(results, Array.from(newStrainsUsed));
    setJustLogged({
      count: items.length, total: items.reduce((s, i) => s + i.grams, 0), manifestNumber: manifestNum,
      entries: results.map((r, i) => ({ id: `${Date.now()}-${i}`, ...r.display })),
    });
    setLineItems([]);
    clearItemFields();
  }

  return (
    <form onSubmit={handleSubmit} style={styles.form}>
      <div style={styles.formHeader}><Leaf size={18} color="#7C9473" /><span>Intake Bucked Flower</span></div>
      <div style={{ fontSize: 12, color: "#8C9483", marginBottom: 8 }}>Raw, ungraded material headed for the trim room — not yet sellable. Once trimmed, bring the graded output into inventory through the Bulk Flower side of Intake.</div>

      {justLogged && (
        <div style={styles.noticeBox}>
          <div>Logged {justLogged.count} item{justLogged.count > 1 ? "s" : ""} ({fmtBoth(justLogged.total)} total){justLogged.manifestNumber ? ` — Manifest ${justLogged.manifestNumber}` : ""}.</div>
          <div style={{ marginTop: 8, display: "flex", flexDirection: "column", gap: 4 }}>
            {justLogged.entries.map((entry) => (
              <div key={entry.id} style={{ fontSize: 12.5 }}>{entry.merged ? "Added to" : "New lot:"} {entry.strain} (Batch {entry.lot}) — {fmtBoth(entry.grams)}</div>
            ))}
          </div>
        </div>
      )}

      <div style={styles.twoCol}>
        <div><FieldLabel>Manifest #</FieldLabel><input style={styles.input} value={manifestNumber} onChange={(e) => setManifestNumber(e.target.value)} placeholder="Metrc transfer manifest number" /></div>
        <div><FieldLabel>Date received</FieldLabel><input style={styles.input} type="date" value={date} onChange={(e) => setDate(e.target.value)} /></div>
      </div>
      <div style={{ fontSize: 12, color: "#8C9483", margin: "-4px 0 12px" }}>These apply to everything logged below. The same batch # arriving under a new manifest adds to the same lot, as long as the room matches too.</div>

      <QuickFillStrip
        items={recentLots}
        renderLabel={(l) => `${l.lot} — ${l.strain}`}
        onPick={applyQuickFill}
      />

      <FieldLabel>Grown At (Origin)</FieldLabel>
      <div style={styles.sourceChoice}>
        {["daddyspipes", "merc"].map((s) => (
          <button type="button" key={s} onClick={() => handleSourceChange(s)} style={{ ...styles.sourceChoiceBtn, borderColor: source === s ? SOURCE_META[s].color : "#2A3324", background: source === s ? SOURCE_META[s].bg : "transparent", color: source === s ? SOURCE_META[s].color : "#B9BFA9" }}>
            <OriginDot origin={s} style={{ marginRight: 2 }} />{SOURCE_META[s].label}
          </button>
        ))}
      </div>
      <FieldLabel>Room</FieldLabel>
      <select style={styles.input} value={room} onChange={(e) => setRoom(e.target.value)}>
        {roomList.map((r) => <option key={r} value={r}>{r}</option>)}
      </select>
      <FieldLabel>Strain</FieldLabel>
      <StrainPicker strains={strains} value={strain} onChange={setStrain} />
      <FieldLabel>Batch #</FieldLabel>
      <input style={styles.input} value={lot} onChange={(e) => setLot(e.target.value)} placeholder="Metrc harvest/batch number" />
      <FieldLabel>Package Tag (last 4 digits)</FieldLabel>
      <input style={styles.input} value={packageTag} onChange={(e) => setPackageTag(e.target.value)} placeholder="e.g. 4821" maxLength={8} />
      <FieldLabel>Weight</FieldLabel>
      <DualWeightInput grams={grams} onChange={setGrams} />
      <FieldLabel>Notes (optional)</FieldLabel>
      <textarea style={{ ...styles.input, minHeight: 50 }} value={itemNotes} onChange={(e) => setItemNotes(e.target.value)} />

      <button type="button" style={{ ...styles.submitBtn, background: "#7C9473", marginTop: 10 }} onClick={addLine}><Plus size={16} /> Add Item to Manifest</button>

      {lineItems.length > 0 && (
        <div style={styles.lineItemList}>
          {lineItems.map((l) => (
            <div key={l.id} style={styles.lineItemRow}>
              <div style={styles.lineItemStrain}>{l.strain} · {SOURCE_META[l.source].label} · {l.room} · Batch {l.lot}{l.packageTag ? ` · Tag …${l.packageTag}` : ""}</div>
              <div style={styles.lineItemRight}>
                <span style={styles.lineItemTotal}>{fmtBoth(l.grams)}</span>
                <button type="button" onClick={() => removeLine(l.id)} style={styles.iconBtn}><X size={14} /></button>
              </div>
            </div>
          ))}
          <div style={styles.subtotalRow}><span>Manifest total so far</span><span style={styles.subtotalValue}>{fmtBoth(manifestTotal)}</span></div>
        </div>
      )}

      {error && <div style={styles.errorText}>{error}</div>}
      <button type="submit" style={styles.submitBtn}><Plus size={16} /> {lineItems.length > 0 ? "Log Full Manifest" : "Log Bucked Flower"}</button>
    </form>
  );
}

function HarvestIntakeForm({ onSubmitManifest, currentUser, rooms, strains, existingLots, enteringFacility }) {
  const [date, setDate] = useState(todayISO());

  const facility = enteringFacility || "daddyspipes";
  const [room, setRoom] = useState((rooms[facility] || [])[0] || "");
  const [strain, setStrain] = useState("");
  const [lot, setLot] = useState("");
  const [grams, setGrams] = useState(0);
  const [itemNotes, setItemNotes] = useState("");

  const [lineItems, setLineItems] = useState([]);
  const [error, setError] = useState("");
  const [justLogged, setJustLogged] = useState(null);

  const recentLots = useMemo(() => recentDistinct(
    (existingLots || []).filter((l) => l.type === "harvest"),
    (l) => `${l.room}__${l.lot.toLowerCase()}__${l.strain.toLowerCase()}`,
    "deliveries"
  ), [existingLots]);

  function applyQuickFill(l) { setRoom(l.room); setStrain(l.strain); setLot(l.lot); }

  function clearItemFields() { setStrain(""); setLot(""); setGrams(0); setItemNotes(""); }
  function currentItemFromForm() { return { room, strain: strain.trim(), lot: lot.trim(), grams, notes: itemNotes.trim() }; }

  function addLine() {
    if (!strain.trim()) return setError("Strain name is required for this item.");
    if (!lot.trim()) return setError("Harvest ID is required for this item.");
    if (grams <= 0) return setError("Enter a wet weight greater than zero for this item.");
    setError("");
    setLineItems([...lineItems, { id: uid(), ...currentItemFromForm() }]);
    clearItemFields();
  }
  function removeLine(id) { setLineItems(lineItems.filter((l) => l.id !== id)); }
  const manifestTotal = lineItems.reduce((s, l) => s + l.grams, 0);

  function handleSubmit(e) {
    e.preventDefault();
    let items = [...lineItems];
    const hasCurrentInput = strain.trim() || lot.trim() || grams > 0 || itemNotes.trim();
    if (hasCurrentInput) {
      if (!strain.trim()) return setError("Strain name is required.");
      if (!lot.trim()) return setError("Harvest ID is required.");
      if (grams <= 0) return setError("Enter a wet weight greater than zero.");
      items = [...items, currentItemFromForm()];
    }
    if (items.length === 0) return setError("Add at least one item before logging.");
    setError("");

    const results = [];
    const localNew = []; // lots created earlier in this same submission, so repeated lines can merge into each other
    const newStrainsUsed = new Set();

    items.forEach((it) => {
      const strainTrim = it.strain.trim();
      const lotTrim = it.lot.trim();
      if (strainTrim && !strains.some((s) => s.toLowerCase() === strainTrim.toLowerCase())) newStrainsUsed.add(strainTrim);

      const delivery = { grams: it.grams, date, staff: currentUser.displayName, notes: it.notes, createdAt: Date.now() };

      // Same Harvest ID + strain + room, logged again (e.g. harvesting a room over a couple days),
      // adds to the same running lot. Restricted to harvest-type lots so a Harvest ID never
      // accidentally merges with an unrelated Bucked Flower lot that happens to share a batch #.
      const matchExisting = (existingLots || []).find((l) =>
        !l.archived && l.type === "harvest" && l.room === it.room && l.strain.trim().toLowerCase() === strainTrim.toLowerCase() && l.lot.trim().toLowerCase() === lotTrim.toLowerCase());
      const matchLocal = !matchExisting && localNew.find((l) =>
        l.room === it.room && l.strain.toLowerCase() === strainTrim.toLowerCase() && l.lot.toLowerCase() === lotTrim.toLowerCase());

      if (matchExisting) {
        results.push({ type: "merge", lotId: matchExisting.id, delivery, display: { merged: true, strain: strainTrim, lot: lotTrim, grams: it.grams } });
      } else if (matchLocal) {
        matchLocal.receivedGrams += it.grams;
        matchLocal.deliveries.push(delivery);
        const existingResult = results.find((r) => r.type === "new" && r.lot.id === matchLocal.id);
        if (existingResult) existingResult.display = { ...existingResult.display, grams: existingResult.display.grams + it.grams };
      } else {
        const newLot = {
          id: uid(), type: "harvest", source: facility, origin: facility, location: facility, room: it.room, strain: strainTrim, lot: lotTrim,
          receivedGrams: it.grams, createdAt: Date.now(), archived: false,
          deliveries: [delivery],
        };
        localNew.push(newLot);
        results.push({ type: "new", lot: newLot, display: { merged: false, strain: strainTrim, lot: lotTrim, grams: it.grams } });
      }
    });

    onSubmitManifest(results, Array.from(newStrainsUsed));
    setJustLogged({
      count: items.length, total: items.reduce((s, i) => s + i.grams, 0),
      entries: results.map((r, i) => ({ id: `${Date.now()}-${i}`, ...r.display })),
    });
    setLineItems([]);
    clearItemFields();
  }

  return (
    <form onSubmit={handleSubmit} style={styles.form}>
      <div style={styles.formHeader}><Leaf size={18} color="#7C9473" /><span>Intake Harvest</span></div>
      <div style={{ fontSize: 12, color: "#8C9483", marginBottom: 8 }}>Daddy's Pipes in-house cultivation — record the wet weight straight off the harvest. Raw and ungraded, headed for the trim room, not yet sellable. Once trimmed, bring the graded output into inventory through the Bulk Flower side of Intake.</div>

      {justLogged && (
        <div style={styles.noticeBox}>
          <div>Logged {justLogged.count} item{justLogged.count > 1 ? "s" : ""} ({fmtBoth(justLogged.total)} total wet weight).</div>
          <div style={{ marginTop: 8, display: "flex", flexDirection: "column", gap: 4 }}>
            {justLogged.entries.map((entry) => (
              <div key={entry.id} style={{ fontSize: 12.5 }}>{entry.merged ? "Added to" : "New harvest:"} {entry.strain} (Harvest {entry.lot}) — {fmtBoth(entry.grams)}</div>
            ))}
          </div>
        </div>
      )}

      <FieldLabel>Date harvested</FieldLabel>
      <input style={styles.input} type="date" value={date} onChange={(e) => setDate(e.target.value)} />
      <div style={{ fontSize: 12, color: "#8C9483", margin: "-4px 0 12px" }}>Applies to everything logged below. The same Harvest ID logged again just adds to the same lot, as long as the room matches too.</div>

      <QuickFillStrip
        items={recentLots}
        renderLabel={(l) => `${l.lot} — ${l.strain}`}
        onPick={applyQuickFill}
      />

      <FieldLabel>Room</FieldLabel>
      <select style={styles.input} value={room} onChange={(e) => setRoom(e.target.value)}>
        {(rooms[facility] || []).map((r) => <option key={r} value={r}>{r}</option>)}
      </select>
      <FieldLabel>Strain</FieldLabel>
      <StrainPicker strains={strains} value={strain} onChange={setStrain} />
      <FieldLabel>Harvest ID</FieldLabel>
      <input style={styles.input} value={lot} onChange={(e) => setLot(e.target.value)} placeholder="Metrc harvest name / ID" />
      <FieldLabel>Wet Weight</FieldLabel>
      <DualWeightInput grams={grams} onChange={setGrams} />
      <FieldLabel>Notes (optional)</FieldLabel>
      <textarea style={{ ...styles.input, minHeight: 50 }} value={itemNotes} onChange={(e) => setItemNotes(e.target.value)} />

      <button type="button" style={{ ...styles.submitBtn, background: "#7C9473", marginTop: 10 }} onClick={addLine}><Plus size={16} /> Add Item</button>

      {lineItems.length > 0 && (
        <div style={styles.lineItemList}>
          {lineItems.map((l) => (
            <div key={l.id} style={styles.lineItemRow}>
              <div style={styles.lineItemStrain}>{l.strain} · {l.room} · Harvest {l.lot}</div>
              <div style={styles.lineItemRight}>
                <span style={styles.lineItemTotal}>{fmtBoth(l.grams)}</span>
                <button type="button" onClick={() => removeLine(l.id)} style={styles.iconBtn}><X size={14} /></button>
              </div>
            </div>
          ))}
          <div style={styles.subtotalRow}><span>Total wet weight so far</span><span style={styles.subtotalValue}>{fmtBoth(manifestTotal)}</span></div>
        </div>
      )}

      {error && <div style={styles.errorText}>{error}</div>}
      <button type="submit" style={styles.submitBtn}><Plus size={16} /> {lineItems.length > 0 ? "Log Full Harvest" : "Log Harvest"}</button>
    </form>
  );
}

// ---------- TRIM LOG ----------
// One screen, matching the paper log exactly: pick the batch and date once, then add each
// trimmer's weight and the grade breakdown right here — no switching screens mid-session.
function TrimSessionForm({ buckedLots, trimmers, currentUser, customGrades, gradeDescriptions, unit, entries, gradeEntries, onSubmitEntry, onSubmitGradeEntry }) {
  const [selectedLotId, setSelectedLotId] = useState(null);
  const [date, setDate] = useState(todayISO());

  // Add-trimmer section
  const [trimmerName, setTrimmerName] = useState("");
  const [trimGrams, setTrimGrams] = useState(0);
  const [trimNotes, setTrimNotes] = useState("");
  const [trimError, setTrimError] = useState("");
  const [trimJustLogged, setTrimJustLogged] = useState(null);

  // Add-grades section
  const [lineGrade, setLineGrade] = useState("A");
  const [lineGrams, setLineGrams] = useState(0);
  const [lineItems, setLineItems] = useState([]);
  const [gradeNotes, setGradeNotes] = useState("");
  const [gradeError, setGradeError] = useState("");
  const [gradeJustLogged, setGradeJustLogged] = useState(null);

  const grades = allGradeCodes(customGrades);
  const selectedLot = buckedLots.find((l) => l.id === selectedLotId);

  const sessionEntries = useMemo(() => {
    if (!selectedLot) return [];
    return (entries || []).filter((t) => t.buckedLotId === selectedLot.id && t.date === date);
  }, [entries, selectedLot, date]);
  const sessionTotal = sessionEntries.reduce((s, t) => s + trimEntryTotal(t), 0);

  function handleAddTrimmer(e) {
    e.preventDefault();
    if (!selectedLot) return setTrimError("Search and select the bucked flower batch this trim came from.");
    if (!trimmerName.trim()) return setTrimError("Enter or select the trimmer's name.");
    if (trimGrams <= 0) return setTrimError("Enter a weight greater than zero.");
    if (trimGrams > selectedLot.remainingGrams) return setTrimError(`That's more than what's left of this batch — only ${fmtBoth(selectedLot.remainingGrams)} remaining.`);
    const trimmerTrim = trimmerName.trim();
    const alreadyLogged = (entries || []).some((en) => en.buckedLotId === selectedLot.id && en.date === date && en.trimmerName.toLowerCase() === trimmerTrim.toLowerCase());
    if (alreadyLogged && !confirm(`${trimmerTrim} already has a trim entry logged for this batch on ${date}. Log this as an additional entry?`)) return;
    setTrimError("");
    const entry = {
      id: uid(), buckedLotId: selectedLot.id, date, trimmerName: trimmerTrim,
      grams: trimGrams, notes: trimNotes.trim(), staff: currentUser.displayName, createdAt: Date.now(),
    };
    onSubmitEntry(entry, trimmerTrim);
    setTrimJustLogged({ trimmer: trimmerTrim, total: trimGrams });
    setTrimGrams(0); setTrimNotes(""); setTrimmerName("");
  }

  function addGradeLine() {
    if (lineGrams <= 0) return setGradeError("Enter a weight greater than zero for this grade.");
    setGradeError("");
    setLineItems([...lineItems, { id: uid(), grade: lineGrade, grams: lineGrams }]);
    setLineGrams(0);
  }
  function removeGradeLine(id) { setLineItems(lineItems.filter((l) => l.id !== id)); }
  const gradedTotal = lineItems.reduce((s, l) => s + l.grams, 0);
  const diff = gradedTotal - sessionTotal;

  function handleSubmitGrades(e) {
    e.preventDefault();
    if (!selectedLot) return setGradeError("Search and select the bucked flower batch these grades came from.");
    if (lineItems.length === 0) return setGradeError("Add at least one grade with a weight.");
    if (sessionEntries.length === 0) {
      if (!confirm(`No trimmer weight was logged for this batch on ${date} yet. Log grades anyway?`)) return;
    } else if (diff !== 0) {
      const diffText = diff > 0 ? `${fmtBoth(diff)} more than` : `${fmtBoth(-diff)} less than`;
      if (!confirm(`That's ${diffText} what the trimmers logged for this batch on ${date} (${fmtBoth(sessionTotal)}). Log it anyway?`)) return;
    }
    const alreadyLogged = (gradeEntries || []).some((g) => g.buckedLotId === selectedLot.id && g.date === date);
    if (alreadyLogged && !confirm(`Grades were already logged for this batch on ${date}. Log this as an additional entry?`)) return;
    setGradeError("");
    const gradeGrams = {};
    lineItems.forEach((l) => { gradeGrams[l.grade] = (gradeGrams[l.grade] || 0) + l.grams; });
    const entry = {
      id: uid(), buckedLotId: selectedLot.id, date, gradeGrams, notes: gradeNotes.trim(), staff: currentUser.displayName, createdAt: Date.now(),
    };
    onSubmitGradeEntry(entry);
    setGradeJustLogged({ total: gradedTotal });
    setLineItems([]); setGradeNotes("");
  }

  return (
    <div>
      <div style={styles.form}>
        <div style={styles.formHeader}><Scissors size={18} color="#C08FD8" /><span>Daily Trim Session</span></div>
        <div style={{ fontSize: 12, color: "#8C9483", marginBottom: 8 }}>Pick the batch and date once, then log each trimmer's weight and the grade breakdown right here.</div>

        <FieldLabel>Bucked Flower Batch</FieldLabel>
        <BuckedLotPicker lots={buckedLots} selectedId={selectedLotId} onSelect={setSelectedLotId} unit={unit} requireRemaining={false} includeArchived />

        {selectedLot && (
          <>
            <FieldLabel>Date</FieldLabel>
            <input style={styles.input} type="date" value={date} onChange={(e) => setDate(e.target.value)} />
          </>
        )}
      </div>

      {selectedLot && (
        <>
          <div style={{ marginTop: 20 }}>
            <div style={{ fontSize: 11, color: "#7C8571", marginBottom: 6 }}>TRIMMERS LOGGED THIS DAY</div>
            {sessionEntries.length === 0 && <div style={styles.emptyState}>No trim logged for this batch on this date yet.</div>}
            {sessionEntries.length > 0 && (
              <div style={styles.historyList}>
                {sessionEntries.map((t) => (
                  <div key={t.id} style={styles.historyRow}>
                    <div style={{ flex: 1, display: "flex", justifyContent: "space-between" }}>
                      <span style={styles.historyStrain}>{t.trimmerName}</span>
                      <span style={styles.historyWeight}>{fmtBoth(trimEntryTotal(t))}</span>
                    </div>
                  </div>
                ))}
                <div style={styles.subtotalRow}><span>Total trimmed this day</span><span style={styles.subtotalValue}>{fmtBoth(sessionTotal)}</span></div>
              </div>
            )}
          </div>

          <form onSubmit={handleAddTrimmer} style={{ ...styles.form, marginTop: 16 }}>
            <div style={styles.formHeader}><Plus size={16} color="#7C9473" /><span>Add Trimmer</span></div>
            {trimJustLogged && <div style={styles.noticeBox}>Logged {fmtBoth(trimJustLogged.total)} for {trimJustLogged.trimmer}.</div>}
            <FieldLabel>Trimmer</FieldLabel>
            <TrimmerPicker trimmers={trimmers} value={trimmerName} onChange={setTrimmerName} />
            <FieldLabel>Weight Trimmed</FieldLabel>
            <DualWeightInput grams={trimGrams} onChange={setTrimGrams} />
            <FieldLabel>Notes (optional)</FieldLabel>
            <textarea style={{ ...styles.input, minHeight: 50 }} value={trimNotes} onChange={(e) => setTrimNotes(e.target.value)} />
            {trimError && <div style={styles.errorText}>{trimError}</div>}
            <button type="submit" style={styles.submitBtn}><Plus size={16} /> Add Trimmer Entry</button>
          </form>

          <form onSubmit={handleSubmitGrades} style={{ ...styles.form, marginTop: 16 }}>
            <div style={styles.formHeader}><ClipboardCheck size={18} color="#C08FD8" /><span>Grade Breakdown</span></div>
            {gradeJustLogged && <div style={styles.noticeBox}>Logged {fmtBoth(gradeJustLogged.total)} of graded output for this batch.</div>}
            <div style={styles.gradeChoice}>
              {grades.map((g) => {
                const meta = gradeMetaFor(g, customGrades);
                return <button type="button" key={g} onClick={() => setLineGrade(g)} title={gradeDescriptionFor(g, gradeDescriptions, customGrades)} style={{ ...styles.gradeChoiceBtn, borderColor: lineGrade === g ? meta.color : "#2A3324", color: lineGrade === g ? meta.color : "#B9BFA9", background: lineGrade === g ? "rgba(201,162,75,0.10)" : "transparent" }}>{meta.label}</button>;
              })}
            </div>
            <DualWeightInput grams={lineGrams} onChange={setLineGrams} />
            <button type="button" style={{ ...styles.submitBtn, background: "#7C9473", marginTop: 10 }} onClick={addGradeLine}><Plus size={16} /> Add Grade</button>

            {lineItems.length > 0 && (
              <div style={styles.lineItemList}>
                {lineItems.map((l) => (
                  <div key={l.id} style={styles.lineItemRow}>
                    <div style={styles.lineItemStrain}><GradeBadge grade={l.grade} isBest={false} /> Grade {l.grade}</div>
                    <div style={styles.lineItemRight}>
                      <span style={styles.lineItemTotal}>{fmtBoth(l.grams)}</span>
                      <button type="button" onClick={() => removeGradeLine(l.id)} style={styles.iconBtn}><X size={14} /></button>
                    </div>
                  </div>
                ))}
                <div style={styles.subtotalRow}>
                  <span>Graded total</span>
                  <span style={{ ...styles.subtotalValue, color: sessionEntries.length > 0 && diff !== 0 ? "#C97B63" : undefined }}>
                    {fmtBoth(gradedTotal)}{sessionEntries.length > 0 && diff !== 0 ? ` (${diff > 0 ? "+" : "−"}${fmtWeight(Math.abs(diff), unit)} vs trimmers)` : sessionEntries.length > 0 ? " ✓ matches" : ""}
                  </span>
                </div>
              </div>
            )}
            <FieldLabel>Notes (optional)</FieldLabel>
            <textarea style={{ ...styles.input, minHeight: 50 }} value={gradeNotes} onChange={(e) => setGradeNotes(e.target.value)} />
            {gradeError && <div style={styles.errorText}>{gradeError}</div>}
            <button type="submit" style={styles.submitBtn}><Plus size={16} /> Log Grades</button>
          </form>
        </>
      )}
    </div>
  );
}

function DailyTrimTotals({ entries, unit }) {
  const [dateFilter, setDateFilter] = useState(todayISO());

  const dayEntries = entries.filter((e) => e.date === dateFilter);
  const dailyTotals = useMemo(() => {
    const map = new Map();
    let grand = 0;
    dayEntries.forEach((e) => {
      const total = trimEntryTotal(e);
      grand += total;
      map.set(e.trimmerName, (map.get(e.trimmerName) || 0) + total);
    });
    return { rows: Array.from(map.entries()).sort((a, b) => b[1] - a[1]), grand };
  }, [dayEntries]);

  return (
    <div>
      <div style={styles.sectionLabel}>DAILY TOTALS</div>
      <FieldLabel>Date</FieldLabel>
      <input style={{ ...styles.input, maxWidth: 200 }} type="date" value={dateFilter} onChange={(e) => setDateFilter(e.target.value)} />
      {dailyTotals.rows.length === 0 && <div style={{ ...styles.emptyState, marginTop: 10 }}>No trim logged this day.</div>}
      {dailyTotals.rows.length > 0 && (
        <div style={{ ...styles.historyList, marginTop: 10 }}>
          {dailyTotals.rows.map(([name, total]) => (
            <div key={name} style={styles.historyRow}>
              <div style={{ flex: 1, display: "flex", justifyContent: "space-between" }}>
                <span style={styles.historyStrain}>{name}</span>
                <span style={styles.historyWeight}>{fmtBoth(total)}</span>
              </div>
            </div>
          ))}
          <div style={styles.subtotalRow}><span>Total trimmed this day</span><span style={styles.subtotalValue}>{fmtBoth(dailyTotals.grand)}</span></div>
        </div>
      )}
    </div>
  );
}

// Admin/Manager only — every trim entry ever logged, searchable, with edit and delete. Unlike the
// day-to-day entry form, this isn't capped or scoped to "recent" — it's the audit/correction view.
function AllTrimEntriesView({ entries, buckedLots, onEditEntry, onDeleteEntry }) {
  const [query, setQuery] = useState("");
  const [editingId, setEditingId] = useState(null);
  const [editForm, setEditForm] = useState(null);
  const [editError, setEditError] = useState("");

  function lotFor(id) { return buckedLots.find((l) => l.id === id); }
  function lotLabel(id) {
    const lot = lotFor(id);
    return lot ? `${lot.strain} — Batch ${lot.lot}` : "Batch (deleted)";
  }

  const sorted = useMemo(() => [...entries].sort((a, b) => (a.date < b.date ? 1 : -1)), [entries]);
  const filtered = useMemo(() => {
    const q = query.trim().toLowerCase();
    if (!q) return sorted;
    return sorted.filter((e) => {
      const lot = lotFor(e.buckedLotId);
      return e.trimmerName.toLowerCase().includes(q)
        || (lot && lot.strain.toLowerCase().includes(q))
        || (lot && lot.lot.toLowerCase().includes(q));
    });
  }, [sorted, query, buckedLots]);

  function startEdit(e) {
    setEditingId(e.id);
    setEditForm({ trimmerName: e.trimmerName, date: e.date, grams: trimEntryTotal(e), notes: e.notes || "" });
    setEditError("");
  }
  function saveEdit(e) {
    if (!editForm.trimmerName.trim()) return setEditError("Trimmer name is required.");
    if (editForm.grams <= 0) return setEditError("Enter a weight greater than zero.");
    const lot = lotFor(e.buckedLotId);
    const oldTotal = trimEntryTotal(e);
    const available = lot ? lot.remainingGrams + oldTotal : Infinity; // what's left on the lot, counting this entry's own weight back in
    if (editForm.grams > available) return setEditError(`That's more than what's left of this batch — only ${fmtBoth(available)} available.`);
    setEditError("");
    onEditEntry(e.id, {
      trimmerName: editForm.trimmerName.trim(), date: editForm.date, grams: editForm.grams, notes: editForm.notes.trim(),
    });
    setEditingId(null);
  }

  return (
    <div>
      <div style={styles.searchBar}>
        <Search size={15} color="#7C8571" />
        <input style={styles.searchInput} placeholder="Search by trimmer, strain, or batch #…" value={query} onChange={(e) => setQuery(e.target.value)} />
      </div>
      <div style={{ fontSize: 12, color: "#8C9483", margin: "10px 0" }}>{filtered.length} entr{filtered.length === 1 ? "y" : "ies"}</div>
      {filtered.length === 0 && <div style={styles.emptyState}>No trim entries yet.</div>}
      <div style={styles.historyList}>
        {filtered.map((e) => {
          if (editingId === e.id) {
            return (
              <div key={e.id} style={styles.editBatchForm}>
                <div style={styles.twoCol}>
                  <div><FieldLabel>Trimmer</FieldLabel><input style={styles.input} value={editForm.trimmerName} onChange={(ev) => setEditForm({ ...editForm, trimmerName: ev.target.value })} /></div>
                  <div><FieldLabel>Date</FieldLabel><input style={styles.input} type="date" value={editForm.date} onChange={(ev) => setEditForm({ ...editForm, date: ev.target.value })} /></div>
                </div>
                <FieldLabel>Weight Trimmed</FieldLabel>
                <DualWeightInput grams={editForm.grams} onChange={(v) => setEditForm({ ...editForm, grams: v })} />
                <FieldLabel>Notes</FieldLabel>
                <textarea style={{ ...styles.input, minHeight: 50 }} value={editForm.notes} onChange={(ev) => setEditForm({ ...editForm, notes: ev.target.value })} />
                {editError && <div style={styles.errorText}>{editError}</div>}
                <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
                  <button type="button" style={styles.submitBtn} onClick={() => saveEdit(e)}>Save</button>
                  <button type="button" style={styles.backBtn} onClick={() => setEditingId(null)}>Cancel</button>
                </div>
              </div>
            );
          }
          const total = trimEntryTotal(e);
          return (
            <div key={e.id} style={styles.historyRow}>
              <div style={{ ...styles.historyIcon, background: "rgba(192,143,216,0.16)" }}><Scissors size={14} color="#C08FD8" /></div>
              <div style={{ flex: 1 }}>
                <div style={styles.historyTop}>
                  <span style={styles.historyStrain}>{e.trimmerName} — {lotLabel(e.buckedLotId)}</span>
                  <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
                    <span style={styles.historyWeight}>{fmtBoth(total)}</span>
                    <button style={styles.iconBtn} onClick={() => startEdit(e)}><Edit3 size={13} /></button>
                    <button style={styles.iconBtnDanger} onClick={() => { if (confirm(`Delete this trim entry for ${e.trimmerName}? The weight goes back to the bucked batch.`)) onDeleteEntry(e.id); }}><Trash2 size={13} /></button>
                  </div>
                </div>
                <div style={styles.historyMeta}>{e.date} · {e.staff}</div>
                {e.notes && <div style={styles.historyNotes}>{e.notes}</div>}
              </div>
            </div>
          );
        })}
      </div>
    </div>
  );
}

// ---------- WASTE (Manager/Admin only — logging waste and closing out finished harvests) ----------
function BuckedLotsManager({ lots, unit, onLogWaste, onMarkFinished, onDeleteWaste, wasteEntries }) {
  const [wasteLotId, setWasteLotId] = useState(null);
  const [wasteGrams, setWasteGrams] = useState(0);
  const [wasteDate, setWasteDate] = useState(todayISO());
  const [wasteNotes, setWasteNotes] = useState("");
  const [error, setError] = useState("");

  function startWaste(l) { setWasteLotId(l.id); setWasteGrams(0); setWasteDate(todayISO()); setWasteNotes(""); setError(""); }
  function saveWaste(l) {
    if (wasteGrams <= 0) return setError("Enter a weight greater than zero.");
    if (wasteGrams > l.remainingGrams) return setError(`That's more than what's left — only ${fmtBoth(l.remainingGrams)} remaining.`);
    const alreadyLogged = (wasteEntries || []).some((w) => w.buckedLotId === l.id && w.date === wasteDate);
    if (alreadyLogged && !confirm(`Waste was already logged for this batch on ${wasteDate}. Log this as an additional entry?`)) return;
    setError("");
    onLogWaste(l.id, { date: wasteDate, grams: wasteGrams, notes: wasteNotes.trim() });
    setWasteLotId(null);
  }
  function handleMarkFinished(l) {
    const msg = l.remainingGrams > 0
      ? `Mark ${l.strain} — Batch ${l.lot} finished? The remaining ${fmtBoth(l.remainingGrams)} will be logged as waste and this batch will be closed out.`
      : `Mark ${l.strain} — Batch ${l.lot} finished? This closes out the batch.`;
    if (confirm(msg)) onMarkFinished(l);
  }

  const recentWaste = useMemo(() => [...(wasteEntries || [])].sort((a, b) => (a.date < b.date ? 1 : -1)).slice(0, 20), [wasteEntries]);
  function lotLabel(id) {
    const lot = lots.find((l) => l.id === id);
    return lot ? `${lot.strain} — Batch ${lot.lot}` : "Batch (closed)";
  }

  return (
    <div>
      <div style={styles.sectionLabel}>ACTIVE BUCKED LOTS</div>
      <div style={{ fontSize: 12, color: "#8C9483", marginBottom: 10 }}>Log waste — anything that can't be sold or used — against a batch, or close a batch out once it's fully worked through.</div>
      {lots.length === 0 && <div style={styles.emptyState}>No active bucked flower.</div>}
      <div style={styles.historyList}>
        {lots.map((l) => (
          <div key={l.id} style={styles.historyRow}>
            <div style={{ flex: 1 }}>
              {wasteLotId === l.id ? (
                <div>
                  <div style={styles.historyStrain}>{l.strain} — Batch {l.lot} <span style={{ fontWeight: 400, fontSize: 11.5, color: "#9BA38C" }}>({lotTypeLabel(l)})</span></div>
                  <FieldLabel>Waste weight</FieldLabel>
                  <DualWeightInput grams={wasteGrams} onChange={setWasteGrams} />
                  <FieldLabel>Date</FieldLabel>
                  <input style={styles.input} type="date" value={wasteDate} onChange={(e) => setWasteDate(e.target.value)} />
                  <FieldLabel>Notes (optional)</FieldLabel>
                  <textarea style={{ ...styles.input, minHeight: 50 }} value={wasteNotes} onChange={(e) => setWasteNotes(e.target.value)} placeholder="Moisture loss, stems, unusable product, etc." />
                  {error && <div style={styles.errorText}>{error}</div>}
                  <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
                    <button type="button" style={styles.submitBtn} onClick={() => saveWaste(l)}>Save</button>
                    <button type="button" style={styles.backBtn} onClick={() => setWasteLotId(null)}>Cancel</button>
                  </div>
                </div>
              ) : (
                <>
                  <div style={styles.historyTop}>
                    <span style={styles.historyStrain}>{l.strain} — Batch {l.lot}</span>
                    <span style={styles.historyWeight}>{fmtBoth(l.remainingGrams)} left</span>
                  </div>
                  <div style={styles.historyMeta}>{lotTypeLabel(l)} · {SOURCE_META[l.source].label} · {l.room} · {fmtBoth(l.receivedGrams)} received · {fmtBoth(l.trimmedGrams || 0)} trimmed · {fmtBoth(l.wasteGrams || 0)} waste so far</div>
                  <div style={{ display: "flex", gap: 8, marginTop: 8 }}>
                    <button type="button" style={styles.iconLabelBtn} onClick={() => startWaste(l)}><AlertTriangle size={12} /> Log Waste</button>
                    <button type="button" style={styles.iconLabelBtn} onClick={() => handleMarkFinished(l)}><CheckCircle2 size={12} /> Mark Finished</button>
                  </div>
                </>
              )}
            </div>
          </div>
        ))}
      </div>

      <div style={{ ...styles.sectionLabel, marginTop: 20 }}>RECENT WASTE ENTRIES</div>
      {recentWaste.length === 0 && <div style={styles.emptyState}>No waste logged yet.</div>}
      <div style={styles.historyList}>
        {recentWaste.map((w) => (
          <div key={w.id} style={styles.historyRow}>
            <div style={{ ...styles.historyIcon, background: "rgba(201,162,75,0.16)" }}><AlertTriangle size={14} color="#C9A24B" /></div>
            <div style={{ flex: 1 }}>
              <div style={styles.historyTop}>
                <span style={styles.historyStrain}>{lotLabel(w.buckedLotId)}</span>
                <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
                  <span style={styles.historyWeight}>{fmtBoth(w.grams)}</span>
                  <button style={styles.iconBtnDanger} onClick={() => { if (confirm("Delete this waste entry? The weight goes back to the bucked batch.")) onDeleteWaste(w.id); }}><Trash2 size={13} /></button>
                </div>
              </div>
              <div style={styles.historyMeta}>{w.date} · {w.staff}</div>
              {w.notes && <div style={styles.historyNotes}>{w.notes}</div>}
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}

function TrimTab({ buckedLots, trimmers, currentUser, customGrades, gradeDescriptions, unit, canManage, entries, gradeEntries, wasteEntries, onSubmitEntry, onEditEntry, onDeleteEntry, onSubmitGradeEntry, onLogWaste, onMarkFinished, onDeleteWaste }) {
  const [view, setView] = useState("log");
  const activeLots = buckedLots.filter((l) => !l.archived);
  return (
    <div>
      {canManage && (
        <div style={{ ...styles.groupToggle, marginBottom: 14, flexWrap: "wrap" }}>
          <button onClick={() => setView("log")} style={{ ...styles.groupBtn, background: view === "log" ? "#C9A24B" : "transparent", color: view === "log" ? "#12160F" : "#B9BFA9" }}>Log Trim</button>
          <button onClick={() => setView("all")} style={{ ...styles.groupBtn, background: view === "all" ? "#C9A24B" : "transparent", color: view === "all" ? "#12160F" : "#B9BFA9" }}>All Trim Entries</button>
        </div>
      )}
      {(!canManage || view === "log") && (
        <>
          <TrimSessionForm buckedLots={buckedLots} trimmers={trimmers} currentUser={currentUser} customGrades={customGrades} gradeDescriptions={gradeDescriptions} unit={unit} entries={entries} gradeEntries={gradeEntries} onSubmitEntry={onSubmitEntry} onSubmitGradeEntry={onSubmitGradeEntry} />
          {canManage && (
            <div style={{ marginTop: 24 }}>
              <BuckedLotsManager lots={activeLots} unit={unit} wasteEntries={wasteEntries} onLogWaste={onLogWaste} onMarkFinished={onMarkFinished} onDeleteWaste={onDeleteWaste} />
            </div>
          )}
          <div style={{ marginTop: 24 }}>
            <DailyTrimTotals entries={entries} unit={unit} />
          </div>
        </>
      )}
      {canManage && view === "all" && (
        <AllTrimEntriesView entries={entries} buckedLots={buckedLots} onEditEntry={onEditEntry} onDeleteEntry={onDeleteEntry} />
      )}
    </div>
  );
}

// ---------- ORDERS (combines customer orders + internal movements) ----------


function OrdersTab({ batches, currentUser, orders, customers, priceList, priceOverrides, nextOrderNumber, canDelete, canMovement, heldByBatch, unit, onCreatePending, onCreateSample, onShipOrder, onReturnSample, onConvertSample, onCancelPending, onLogMovement, onOpenInvoice, onDeleteOrder }) {
  // Three-tab lifecycle: "new" (create + hold), "ship" (add Metrc + deduct), "samples" (temp hold).
  const [subtab, setSubtab] = useState("new");
  const [entryKind, setEntryKind] = useState("order"); // within "new": "order" | "movement"

  // order-builder state (shared by New Order + Sample entry)
  const [customerId, setCustomerId] = useState("");
  const [customer, setCustomer] = useState("");
  const [customerLicense, setCustomerLicense] = useState("");
  const [customerAddress, setCustomerAddress] = useState("");
  const [buyerName, setBuyerName] = useState("");
  const [lineItems, setLineItems] = useState([]);
  const [batchId, setBatchId] = useState("");
  const [sellAs, setSellAs] = useState("");
  const [grams, setGrams] = useState(0);
  const [pricePerLb, setPricePerLb] = useState("");
  const [error, setError] = useState("");
  const [orderSearch, setOrderSearch] = useState("");

  // sample-specific
  // (salesperson/buyer name is captured via the shared customer field for samples)
  // ship-specific (per-order Metrc entry) — keyed by order id
  const [shipManifest, setShipManifest] = useState({});
  const [shipPackage, setShipPackage] = useState({}); // { orderId: { lineItemId: pkg# } }
  const [shipNotes, setShipNotes] = useState({});
  const [returnManifest, setReturnManifest] = useState({});
  const [returnPackage, setReturnPackage] = useState({});

  // movement state
  const [mBatchId, setMBatchId] = useState("");
  const [mGrams, setMGrams] = useState(0);
  const [mReason, setMReason] = useState("transfer");
  const [mDate, setMDate] = useState(todayISO());
  const [mNotes, setMNotes] = useState("");
  const [mError, setMError] = useState("");
  const [mLogged, setMLogged] = useState(null);

  // Reserved by THIS in-progress order draft (before it's saved), on top of app-wide holds.
  const reservedByBatch = useMemo(() => {
    const m = {};
    lineItems.forEach((li) => { m[li.batchId] = (m[li.batchId] || 0) + li.grams; });
    return m;
  }, [lineItems]);

  // Available = remaining − app-wide holds (other pending/sample orders) − this draft's reservations.
  function availableFor(b) {
    return b.remainingGrams - (heldByBatch?.[b.id] || 0) - (reservedByBatch[b.id] || 0);
  }

  function handleCustomerSelect(id) {
    setCustomerId(id);
    if (!id) { setCustomer(""); setCustomerLicense(""); setCustomerAddress(""); return; }
    const c = customers.find((x) => x.id === id);
    if (c) { setCustomer(c.name); setCustomerLicense(c.license || ""); setCustomerAddress(c.address || ""); }
  }
  function handleBatchSelect(id) {
    setBatchId(id);
    const b = batches.find((x) => x.id === id);
    setSellAs(b ? b.strain : "");
    const suggested = b ? getEffectivePrice(b, priceList, priceOverrides) : 0;
    setPricePerLb(suggested ? String(suggested) : "");
  }
  const selectedBatch = batches.find((b) => b.id === batchId);

  function addLineItem() {
    const b = batches.find((x) => x.id === batchId);
    if (!b) return setError("Select a batch.");
    if (grams <= 0) return setError("Enter a quantity greater than zero.");
    const available = availableFor(b);
    if (grams > available) return setError(`Only ${fmtBoth(available)} available for that batch (rest is on hold).`);
    const p = parseFloat(pricePerLb);
    if (isNaN(p) || p < 0) return setError("Enter a price per pound.");
    setError("");
    const qtyLb = grams / G_PER_LB;
    setLineItems([...lineItems, {
      id: uid(), batchId: b.id, strain: b.strain, displayStrain: sellAs.trim() || b.strain, lot: b.lot,
      source: b.source, room: b.room, grade: b.grade, isBest: b.isBest, grams, pricePerLb: p, lineTotal: qtyLb * p,
    }]);
    setBatchId(""); setSellAs(""); setGrams(0); setPricePerLb("");
  }
  function removeLineItem(id) { setLineItems(lineItems.filter((li) => li.id !== id)); }
  const subtotal = lineItems.reduce((s, li) => s + li.lineTotal, 0);

  function buildBaseOrder(status) {
    const orderNumber = `ORD-${String(nextOrderNumber).padStart(4, "0")}`;
    return {
      id: uid(), orderNumber, customer: customer.trim(), customerId: customerId || null,
      customerLicense: customerLicense.trim(), customerAddress: customerAddress.trim(), buyerName: buyerName.trim(),
      date: todayISO(), staff: currentUser.displayName, lineItems, subtotal, status,
      paymentStatus: "unpaid", createdAt: Date.now(), emailedAt: null,
    };
  }
  function maybeNewCustomer() {
    if (!customerId && customer.trim() && !customers.some((c) => c.name.toLowerCase() === customer.trim().toLowerCase())) {
      return { id: uid(), name: customer.trim(), license: customerLicense.trim(), address: customerAddress.trim(), createdAt: Date.now() };
    }
    return null;
  }
  function resetEntry() {
    setCustomer(""); setCustomerId(""); setCustomerLicense(""); setCustomerAddress(""); setBuyerName(""); setLineItems([]); setSampleSalesman("");
  }

  function createPending() {
    if (lineItems.length === 0) return setError("Add at least one line item.");
    if (!customer.trim()) return setError("Enter a customer / distributor name.");
    setError("");
    onCreatePending(buildBaseOrder("pending"), maybeNewCustomer());
    resetEntry();
  }
  function createSample() {
    if (lineItems.length === 0) return setError("Add at least one line item.");
    if (!customer.trim()) return setError("Enter the salesperson / buyer name.");
    setError("");
    // For samples the single name field doubles as customer + salesman.
    const order = { ...buildBaseOrder("sample"), salesman: customer.trim() };
    onCreateSample(order, null); // don't auto-save samples as customers
    resetEntry();
  }

  function handleMovementSubmit(e) {
    e.preventDefault();
    const b = batches.find((x) => x.id === mBatchId);
    if (!b) return setMError("Select a batch to move from.");
    if (mGrams <= 0) return setMError("Enter a weight greater than zero.");
    if (mGrams > b.remainingGrams) return setMError(`Only ${fmtWeight(b.remainingGrams, "lb")} remaining in that batch.`);
    setMError("");
    onLogMovement({ id: uid(), batchId: mBatchId, strain: b.strain, grams: mGrams, destination: mReason, date: mDate, staff: currentUser.displayName, notes: mNotes.trim(), createdAt: Date.now() });
    setMLogged({ strain: b.strain, grams: mGrams, reason: mReason });
    setMBatchId(""); setMGrams(0); setMNotes("");
  }
  const mSelectedBatch = batches.find((b) => b.id === mBatchId);

  // Order groupings by status
  const pendingOrders = useMemo(() => orders.filter((o) => o.status === "pending"), [orders]);
  const sampleOrders = useMemo(() => orders.filter((o) => o.status === "sample"), [orders]);
  const shippedOrders = useMemo(() => [...orders].filter((o) => o.status === "shipped").reverse(), [orders]);

  const searchedShipped = useMemo(() => {
    const q = orderSearch.trim().toLowerCase();
    if (!q) return shippedOrders;
    return shippedOrders.filter((o) => o.customer.toLowerCase().includes(q) || (o.buyerName || "").toLowerCase().includes(q) || o.orderNumber.toLowerCase().includes(q));
  }, [shippedOrders, orderSearch]);

  function StatusBadge({ status }) {
    const map = {
      pending: { label: "HELD", color: "#C9A24B" },
      shipped: { label: "SHIPPED", color: "#7C9473" },
      sample: { label: "SAMPLE", color: "#C08FD8" },
      sample_returned: { label: "RETURNED", color: "#8C9483" },
    };
    const m = map[status] || { label: status, color: "#8C9483" };
    return <span style={{ fontSize: 10, fontWeight: 700, padding: "1px 6px", borderRadius: 4, border: `1px solid ${m.color}`, color: m.color, fontFamily: "'IBM Plex Mono', monospace", whiteSpace: "nowrap" }}>{m.label}</span>;
  }

  // Shared customer + line-item entry block (used by New Order and Sample)
  function EntryForm({ mode }) {
    return (
      <>
        {mode === "sample" ? (
          <>
            <FieldLabel>Salesperson / Buyer name</FieldLabel>
            <input style={styles.input} value={customer} onChange={(e) => setCustomer(e.target.value)} placeholder="Name of who's taking the sample" />
            <FieldLabel>Distro License #</FieldLabel>
            <input style={styles.input} value={customerLicense} onChange={(e) => setCustomerLicense(e.target.value)} placeholder="Their distributor license number" />
          </>
        ) : (
          <>
            <FieldLabel>Customer / Distributor</FieldLabel>
            <select style={styles.input} value={customerId} onChange={(e) => handleCustomerSelect(e.target.value)}>
              <option value="">+ New customer</option>
              {customers.map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}
            </select>
            <input style={{ ...styles.input, marginTop: 6 }} value={customer} onChange={(e) => setCustomer(e.target.value)} placeholder="Business name" />
            <FieldLabel>Customer State License #</FieldLabel>
            <input style={styles.input} value={customerLicense} onChange={(e) => setCustomerLicense(e.target.value)} placeholder="e.g. C11-0000000-LIC" />
            <FieldLabel>Customer Address</FieldLabel>
            <input style={styles.input} value={customerAddress} onChange={(e) => setCustomerAddress(e.target.value)} placeholder="Street, City, State ZIP" />
            <FieldLabel>Buyer's Name (person)</FieldLabel>
            <input style={styles.input} value={buyerName} onChange={(e) => setBuyerName(e.target.value)} placeholder="Who at the company you're selling to" />
            <div style={styles.pinHint}>New customers are saved automatically so you won't need to retype them next time.</div>
          </>
        )}
        <FieldLabel>Add line item</FieldLabel>
        {selectedBatch ? (
          <div style={styles.selectedBatchChip}>
            <div>
              <div style={{ fontWeight: 600, color: "#EDE8D8" }}>{selectedBatch.strain} <GradeBadge grade={selectedBatch.grade} isBest={selectedBatch.isBest} /></div>
              <div style={{ fontSize: 11, color: "#8C9483" }}>{SOURCE_META[selectedBatch.source].label} · {selectedBatch.room}{selectedBatch.lot ? ` · Batch ${selectedBatch.lot}` : ""} · {fmtBoth(availableFor(selectedBatch))} available</div>
            </div>
            <button type="button" style={styles.iconLabelBtn} onClick={() => handleBatchSelect("")}>Change</button>
          </div>
        ) : (
          <BatchSearchPicker batches={batches} reservedByBatch={{ ...heldByBatch, ...reservedByBatch }} onSelect={(b) => handleBatchSelect(b.id)} />
        )}
        {selectedBatch && (
          <>
            <FieldLabel>Sell as (display strain)</FieldLabel>
            <input style={styles.input} value={sellAs} onChange={(e) => setSellAs(e.target.value)} placeholder={selectedBatch.strain} />
            <div style={styles.twoCol}>
              <div><FieldLabel>Quantity</FieldLabel><DualWeightInput grams={grams} onChange={setGrams} /></div>
              <div><FieldLabel>Price / lb</FieldLabel><input style={styles.input} type="number" step="0.01" value={pricePerLb} onChange={(e) => setPricePerLb(e.target.value)} placeholder="0.00" /></div>
            </div>
            <button type="button" style={{ ...styles.submitBtn, background: "#7C9473", marginTop: 10 }} onClick={addLineItem}><Plus size={16} /> Add Item</button>
          </>
        )}

        {lineItems.length > 0 && (
          <div style={{ marginTop: 14 }}>
            {lineItems.map((li) => (
              <div key={li.id} style={styles.lineItemRow}>
                <div>
                  <div style={styles.lineItemStrain}><OriginDot origin={li.source} style={{ marginRight: 6 }} />{li.displayStrain} <GradeBadge grade={li.grade} isBest={li.isBest} /></div>
                  <div style={styles.lineItemMeta}>{SOURCE_META[li.source].label}{li.lot ? ` · Batch ${li.lot}` : ""} · {fmtBoth(li.grams)} @ {fmtMoney(li.pricePerLb)}/lb</div>
                </div>
                <div style={styles.lineItemRight}>
                  <span style={styles.lineItemTotal}>{fmtMoney(li.lineTotal)}</span>
                  <button type="button" onClick={() => removeLineItem(li.id)} style={styles.iconBtn}><X size={14} /></button>
                </div>
              </div>
            ))}
            <div style={styles.subtotalRow}><span>Subtotal</span><span style={styles.subtotalValue}>{fmtMoney(subtotal)}</span></div>
          </div>
        )}
        {error && <div style={styles.errorText}>{error}</div>}
      </>
    );
  }

  return (
    <div>
      <div style={{ ...styles.groupToggle, marginBottom: 16 }}>
        <button onClick={() => setSubtab("new")} style={{ ...styles.groupBtn, background: subtab === "new" ? "#C9A24B" : "transparent", color: subtab === "new" ? "#12160F" : "#B9BFA9" }}>New Order</button>
        <button onClick={() => setSubtab("ship")} style={{ ...styles.groupBtn, background: subtab === "ship" ? "#C9A24B" : "transparent", color: subtab === "ship" ? "#12160F" : "#B9BFA9", position: "relative" }}>
          Ship {pendingOrders.length > 0 && <span style={styles.shipBadge}>{pendingOrders.length}</span>}
        </button>
        <button onClick={() => setSubtab("samples")} style={{ ...styles.groupBtn, background: subtab === "samples" ? "#C9A24B" : "transparent", color: subtab === "samples" ? "#12160F" : "#B9BFA9" }}>
          Samples {sampleOrders.length > 0 && <span style={{ ...styles.shipBadge, background: "#C08FD8" }}>{sampleOrders.length}</span>}
        </button>
      </div>

      {/* ---------- TAB 1: NEW ORDER ---------- */}
      {subtab === "new" && (
        <>
          {canMovement && (
            <div style={{ ...styles.groupToggle, marginBottom: 14 }}>
              <button onClick={() => setEntryKind("order")} style={{ ...styles.groupBtnSm, background: entryKind === "order" ? "#2A3324" : "transparent", color: entryKind === "order" ? "#EDE8D8" : "#8C9483" }}>Customer Order</button>
              <button onClick={() => setEntryKind("movement")} style={{ ...styles.groupBtnSm, background: entryKind === "movement" ? "#2A3324" : "transparent", color: entryKind === "movement" ? "#EDE8D8" : "#8C9483" }}>Internal Movement</button>
            </div>
          )}
          {entryKind === "order" && (
            <div style={styles.form}>
              <div style={styles.formHeader}><ClipboardList size={18} color="#C9A24B" /><span>New Order</span></div>
              <div style={{ fontSize: 12, color: "#8C9483", marginBottom: 10 }}>Creates the order, prints the invoice, and puts the product on hold. Add Metrc info and ship it from the Ship tab.</div>
              {EntryForm({ mode: "order" })}
              <button type="button" style={{ ...styles.submitBtn, marginTop: 12 }} onClick={createPending}><FileText size={16} /> Create Order &amp; Print Invoice</button>
            </div>
          )}
          {entryKind === "movement" && (
            <form onSubmit={handleMovementSubmit} style={styles.form}>
              <div style={styles.formHeader}><Repeat size={18} color="#8FAF8B" /><span>Internal Movement</span></div>
              <div style={{ fontSize: 12, color: "#8C9483", marginBottom: 8 }}>Transfers, waste, or samples — no price, no invoice, just an inventory adjustment.</div>
              {mLogged && <div style={styles.noticeBox}>Logged {fmtBoth(mLogged.grams)} of {mLogged.strain} as {mLogged.reason}.</div>}
              {batches.length === 0 ? <div style={styles.emptyState}>No stock available.</div> : (
                <>
                  <FieldLabel>Batch</FieldLabel>
                  {mSelectedBatch ? (
                    <div style={styles.selectedBatchChip}>
                      <div>
                        <div style={{ fontWeight: 600, color: "#EDE8D8" }}>{mSelectedBatch.strain} <GradeBadge grade={mSelectedBatch.grade} isBest={mSelectedBatch.isBest} /></div>
                        <div style={{ fontSize: 11, color: "#8C9483" }}>{SOURCE_META[mSelectedBatch.source].label} · {mSelectedBatch.room}{mSelectedBatch.lot ? ` · Batch ${mSelectedBatch.lot}` : ""} · {fmtWeight(mSelectedBatch.remainingGrams, "lb")} left</div>
                      </div>
                      <button type="button" style={styles.iconLabelBtn} onClick={() => setMBatchId("")}>Change</button>
                    </div>
                  ) : (
                    <BatchSearchPicker batches={batches} onSelect={(b) => setMBatchId(b.id)} />
                  )}
                  <FieldLabel>Weight</FieldLabel>
                  <DualWeightInput grams={mGrams} onChange={setMGrams} />
                  <FieldLabel>Reason</FieldLabel>
                  <select style={styles.input} value={mReason} onChange={(e) => setMReason(e.target.value)}>
                    <option value="transfer">Transfer to another location</option>
                    <option value="waste">Waste / loss</option>
                    <option value="sample">Sample / testing</option>
                  </select>
                  <FieldLabel>Date</FieldLabel>
                  <input style={styles.input} type="date" value={mDate} onChange={(e) => setMDate(e.target.value)} />
                  <FieldLabel>Notes</FieldLabel>
                  <textarea style={{ ...styles.input, minHeight: 60 }} value={mNotes} onChange={(e) => setMNotes(e.target.value)} placeholder="Manifest #, reason, etc." />
                  {mError && <div style={styles.errorText}>{mError}</div>}
                  <button type="submit" style={styles.submitBtn}><Plus size={16} /> Log Movement</button>
                </>
              )}
            </form>
          )}

          {/* Recent shipped orders list for reference */}
          <div style={styles.sectionLabel}>RECENT ORDERS</div>
          <input style={styles.searchInput} value={orderSearch} onChange={(e) => setOrderSearch(e.target.value)} placeholder="Search shipped orders by customer, buyer, or order #…" />
          {searchedShipped.length === 0 ? <div style={styles.emptyState}>No shipped orders yet.</div> : searchedShipped.slice(0, 20).map((o) => (
            <div key={o.id} style={styles.orderRow}>
              <div>
                <div style={{ fontWeight: 600, color: "#EDE8D8" }}>{o.orderNumber} · {o.customer} <StatusBadge status={o.status} /></div>
                <div style={{ fontSize: 11, color: "#8C9483" }}>{o.date} · {fmtMoney(o.subtotal)} · {o.paymentStatus === "paid" ? "Paid" : "Unpaid"}</div>
              </div>
              <div style={{ display: "flex", gap: 6, alignItems: "center" }}>
                <button style={styles.viewInvoiceBtn} onClick={() => onOpenInvoice(o.id)}>View</button>
                {canDelete && <button style={styles.iconBtnDanger} onClick={() => { if (confirm(`Delete order ${o.orderNumber}? This restores its stock.`)) onDeleteOrder(o.id); }}><Trash2 size={14} /></button>}
              </div>
            </div>
          ))}
        </>
      )}

      {/* ---------- TAB 2: SHIP ---------- */}
      {subtab === "ship" && (
        <>
          <div style={styles.form}>
            <div style={styles.formHeader}><Truck size={18} color="#7C9473" /><span>Ship / Finalize</span></div>
            <div style={{ fontSize: 12, color: "#8C9483" }}>Orders waiting to be manifested and shipped. Add the Metrc info and finalize — that's when inventory is deducted and the sale counts.</div>
          </div>
          {pendingOrders.length === 0 ? (
            <div style={styles.emptyState}>Nothing waiting to ship. New orders show up here after you create them.</div>
          ) : pendingOrders.map((o) => (
            <div key={o.id} style={styles.shipCard}>
              <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start" }}>
                <div>
                  <div style={{ fontWeight: 600, color: "#EDE8D8" }}>{o.orderNumber} · {o.customer} <StatusBadge status="pending" /></div>
                  <div style={{ fontSize: 11, color: "#8C9483" }}>{o.date} · {o.lineItems.length} item{o.lineItems.length !== 1 ? "s" : ""} · {fmtMoney(o.subtotal)}{o.convertedFromSample ? " · from sample" : ""}</div>
                </div>
                <button style={styles.viewInvoiceBtn} onClick={() => onOpenInvoice(o.id)}>View / Edit</button>
              </div>
              <div style={{ marginTop: 6 }}>
                <FieldLabel>Manifest Number</FieldLabel>
                <input style={styles.input} value={shipManifest[o.id] || ""} onChange={(e) => setShipManifest({ ...shipManifest, [o.id]: e.target.value })} placeholder="Metrc manifest # (whole order)" />
              </div>
              <FieldLabel>Metrc Package # per item</FieldLabel>
              {o.lineItems.map((li) => (
                <div key={li.id} style={{ marginBottom: 6 }}>
                  <div style={{ fontSize: 11.5, color: "#8C9483", marginBottom: 2 }}>{li.displayStrain || li.strain} · {fmtBoth(li.grams)}</div>
                  <input
                    style={{ ...styles.input, fontFamily: "monospace" }}
                    value={(shipPackage[o.id] && shipPackage[o.id][li.id]) || ""}
                    onChange={(e) => setShipPackage({ ...shipPackage, [o.id]: { ...(shipPackage[o.id] || {}), [li.id]: e.target.value } })}
                    placeholder="1A40602…"
                  />
                </div>
              ))}
              <FieldLabel>Notes</FieldLabel>
              <textarea style={{ ...styles.input, minHeight: 50 }} value={shipNotes[o.id] || ""} onChange={(e) => setShipNotes({ ...shipNotes, [o.id]: e.target.value })} placeholder="Anything to note about this shipment" />
              <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
                <button style={{ ...styles.submitBtn, background: "#7C9473" }} onClick={() => onShipOrder(o.id, { manifestNumber: shipManifest[o.id], packagesByLine: shipPackage[o.id] || {}, notes: shipNotes[o.id] })}><Truck size={16} /> Mark Shipped</button>
                {canDelete && <button style={styles.backBtn} onClick={() => { if (confirm(`Cancel order ${o.orderNumber}? This releases the hold — no inventory is deducted.`)) onCancelPending(o.id); }}>Cancel</button>}
              </div>
            </div>
          ))}
        </>
      )}

      {/* ---------- TAB 3: SAMPLES ---------- */}
      {subtab === "samples" && (
        <>
          <div style={styles.form}>
            <div style={styles.formHeader}><ClipboardList size={18} color="#C08FD8" /><span>New Sample</span></div>
            <div style={{ fontSize: 12, color: "#8C9483", marginBottom: 10 }}>A sample lb out with a salesman. Temp-holds it from inventory. Doesn't count as a sale unless it converts.</div>
            {EntryForm({ mode: "sample" })}
            <button type="button" style={{ ...styles.submitBtn, background: "#C08FD8", color: "#12160F", marginTop: 12 }} onClick={createSample}><ClipboardList size={16} /> Send Out Sample</button>
          </div>

          <div style={styles.sectionLabel}>SAMPLES OUT</div>
          {sampleOrders.length === 0 ? (
            <div style={styles.emptyState}>No samples currently out.</div>
          ) : sampleOrders.map((o) => (
            <div key={o.id} style={styles.shipCard}>
              <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start" }}>
                <div>
                  <div style={{ fontWeight: 600, color: "#EDE8D8" }}>{o.orderNumber} · {o.customer} <StatusBadge status="sample" /></div>
                  <div style={{ fontSize: 11, color: "#8C9483" }}>{o.date}{o.salesman ? ` · ${o.salesman}` : ""} · {o.lineItems.length} item{o.lineItems.length !== 1 ? "s" : ""} · {fmtMoney(o.subtotal)}</div>
                </div>
                <button style={styles.viewInvoiceBtn} onClick={() => onOpenInvoice(o.id)}>Slip</button>
              </div>
              <div style={styles.twoCol}>
                <div><FieldLabel>Manifest Number</FieldLabel><input style={styles.input} value={returnManifest[o.id] || ""} onChange={(e) => setReturnManifest({ ...returnManifest, [o.id]: e.target.value })} placeholder="Metrc manifest #" /></div>
                <div><FieldLabel>Metrc Package Number</FieldLabel><input style={{ ...styles.input, fontFamily: "monospace" }} value={returnPackage[o.id] || ""} onChange={(e) => setReturnPackage({ ...returnPackage, [o.id]: e.target.value })} placeholder="1A40602…" /></div>
              </div>
              <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
                <button style={{ ...styles.submitBtn, background: "#7C9473" }} onClick={() => onConvertSample(o.id)}><Repeat size={16} /> Convert to Sale</button>
                <button style={styles.backBtn} onClick={() => { if (confirm(`Mark ${o.orderNumber} returned? This releases the hold back to inventory.`)) onReturnSample(o.id, { manifestNumber: returnManifest[o.id], metrcPackageNumber: returnPackage[o.id] }); }}>Returned</button>
              </div>
            </div>
          ))}

          {/* Returned samples log */}
          {orders.some((o) => o.status === "sample_returned") && (
            <>
              <div style={styles.sectionLabel}>RETURNED SAMPLES</div>
              {orders.filter((o) => o.status === "sample_returned").reverse().slice(0, 10).map((o) => (
                <div key={o.id} style={styles.orderRow}>
                  <div>
                    <div style={{ fontWeight: 600, color: "#EDE8D8" }}>{o.orderNumber} · {o.customer} <StatusBadge status="sample_returned" /></div>
                    <div style={{ fontSize: 11, color: "#8C9483" }}>{o.salesman ? `${o.salesman} · ` : ""}{o.metrcPackageNumber ? `Tag …${normalizeTag(o.metrcPackageNumber).slice(-6)}` : "no tag"}</div>
                  </div>
                  <button style={styles.viewInvoiceBtn} onClick={() => onOpenInvoice(o.id)}>Slip</button>
                </div>
              ))}
            </>
          )}
        </>
      )}
    </div>
  );
}


function InvoiceView({ order, distributionEmail, shipper, paymentInfo, editableBatches, canEdit, onBack, onMarkEmailed, onTogglePaid, onSaveEdit }) {
  const [editing, setEditing] = useState(false);
  const [editOrderNumber, setEditOrderNumber] = useState("");
  const [editCustomer, setEditCustomer] = useState("");
  const [editLicense, setEditLicense] = useState("");
  const [editAddress, setEditAddress] = useState("");
  const [editBuyerName, setEditBuyerName] = useState("");
  const [editLineItems, setEditLineItems] = useState([]);
  const [addBatchId, setAddBatchId] = useState("");
  const [addSellAs, setAddSellAs] = useState("");
  const [addGrams, setAddGrams] = useState(0);
  const [addPrice, setAddPrice] = useState("");
  const [editError, setEditError] = useState("");

  if (!order) return <div style={styles.emptyState}>Order not found.</div>;
  const isPaid = order.paymentStatus === "paid";

  function startEdit() {
    setEditOrderNumber(order.orderNumber);
    setEditCustomer(order.customer); setEditLicense(order.customerLicense || ""); setEditAddress(order.customerAddress || ""); setEditBuyerName(order.buyerName || "");
    setEditLineItems(order.lineItems); setEditError(""); setEditing(true);
  }
  function cancelEdit() { setEditing(false); setEditError(""); }

  const reservedInEdit = useMemo(() => {
    const m = {};
    editLineItems.forEach((li) => { m[li.batchId] = (m[li.batchId] || 0) + li.grams; });
    return m;
  }, [editLineItems]);
  const addSelectedBatch = (editableBatches || []).find((b) => b.id === addBatchId);

  function addEditItem() {
    const b = (editableBatches || []).find((x) => x.id === addBatchId);
    if (!b) return setEditError("Select a batch.");
    if (addGrams <= 0) return setEditError("Enter a quantity greater than zero.");
    const available = b.remainingGrams - (reservedInEdit[b.id] || 0);
    if (addGrams > available) return setEditError(`Only ${fmtBoth(available)} available for that batch.`);
    const p = parseFloat(addPrice);
    if (isNaN(p) || p < 0) return setEditError("Enter a price per pound.");
    setEditError("");
    const qtyLb = addGrams / G_PER_LB;
    setEditLineItems([...editLineItems, {
      id: uid(), batchId: b.id, strain: b.strain, displayStrain: addSellAs.trim() || b.strain, lot: b.lot,
      source: b.source, room: b.room, grade: b.grade, isBest: b.isBest, grams: addGrams, pricePerLb: p, lineTotal: qtyLb * p,
    }]);
    setAddBatchId(""); setAddSellAs(""); setAddGrams(0); setAddPrice("");
  }
  function removeEditItem(id) { setEditLineItems(editLineItems.filter((li) => li.id !== id)); }
  function changeEditPrice(id, value) {
    const p = parseFloat(value);
    setEditLineItems(editLineItems.map((li) => li.id === id ? { ...li, pricePerLb: isNaN(p) ? 0 : p, lineTotal: (li.grams / G_PER_LB) * (isNaN(p) ? 0 : p) } : li));
  }
  const editSubtotal = editLineItems.reduce((s, li) => s + li.lineTotal, 0);

  function saveEdit() {
    if (editLineItems.length === 0) return setEditError("The order needs at least one item — remove the whole order instead if it should go away.");
    if (!editCustomer.trim()) return setEditError("Enter a customer name.");
    if (!editOrderNumber.trim()) return setEditError("Order number can't be blank.");
    setEditError("");
    onSaveEdit(order.id, {
      orderNumber: editOrderNumber.trim(), customer: editCustomer.trim(), customerLicense: editLicense.trim(),
      customerAddress: editAddress.trim(), buyerName: editBuyerName.trim(),
      lineItems: editLineItems, subtotal: editSubtotal,
    });
    setEditing(false);
  }

  function buildBody() {
    const lines = [
      `Order ${order.orderNumber}`, `Date: ${order.date}`,
      `Shipper: ${shipper?.name || ""}${shipper?.license ? " (License " + shipper.license + ")" : ""}`,
      `Customer / Distributor: ${order.customer}${order.customerLicense ? " (License " + order.customerLicense + ")" : ""}`,
      order.buyerName ? `Buyer: ${order.buyerName}` : "",
      `Prepared by: ${order.staff}`, `Payment status: ${isPaid ? "PAID" : "UNPAID"}`, "",
      "Line items:",
      ...order.lineItems.map((li) => `- ${li.displayStrain || li.strain} | Batch ${li.lot || "—"} | ${SOURCE_META[li.source].label} | Grade ${li.grade}${li.isBest ? " (Best)" : ""} | ${fmtBoth(li.grams)} | ${fmtMoney(li.pricePerLb)}/lb | ${fmtMoney(li.lineTotal)}`),
      "", `Subtotal: ${fmtMoney(order.subtotal)}`,
      paymentInfo?.terms ? `Payment terms: ${paymentInfo.terms}` : "",
      paymentInfo?.instructions ? `Payment instructions: ${paymentInfo.instructions}` : "",
      "", "Please enter this order into METRC.",
    ].filter(Boolean);
    return lines.join("\n");
  }

  function emailToDistribution() {
    const subject = encodeURIComponent(`METRC Order ${order.orderNumber} — ${order.customer}`);
    const body = encodeURIComponent(buildBody());
    const to = distributionEmail ? encodeURIComponent(distributionEmail) : "";
    window.location.href = `mailto:${to}?subject=${subject}&body=${body}`;
    onMarkEmailed(order.id);
  }

  function invoiceHTML() {
    const isShipped = order.status === "shipped";
    // Once shipped, show a Metrc Package # column so each line carries its own tag on the final invoice.
    const rows = order.lineItems.map((li) => `<tr><td>${li.displayStrain || li.strain}</td><td>${li.lot || "—"}</td><td>${SOURCE_META[li.source].label}</td><td>${li.grade}${li.isBest ? " ★" : ""}</td>${isShipped ? `<td>${li.metrcPackageNumber || "—"}</td>` : ""}<td>${fmtBoth(li.grams)}</td><td>${fmtMoney(li.pricePerLb)}/lb</td><td>${fmtMoney(li.lineTotal)}</td></tr>`).join("");
    const shipperHTML = shipper && shipper.name
      ? `<div class="shipper-top"><div class="biz">${shipper.name}</div>${shipper.license ? `<div>License: ${shipper.license}</div>` : ""}${shipper.address ? `<div>${shipper.address}</div>` : ""}${shipper.contact ? `<div>${shipper.contact}</div>` : ""}</div>`
      : `<div class="shipper-top"><div class="biz">Shipper info not set</div></div>`;
    const paymentHTML = (paymentInfo?.terms || paymentInfo?.instructions)
      ? `<div class="payment-block">${paymentInfo?.terms ? `<div><b>Payment Terms:</b> ${paymentInfo.terms}</div>` : ""}${paymentInfo?.instructions ? `<div style="margin-top:4px;"><b>Payment Instructions:</b><br/>${paymentInfo.instructions.replace(/\n/g, "<br/>")}</div>` : ""}</div>`
      : "";
    const paidBadgeHTML = `<span class="paid-badge" style="background:${isPaid ? "#DCEEDC" : "#F5E0DC"};color:${isPaid ? "#2E5B2E" : "#8A3B2A"};">${isPaid ? "PAID" : "UNPAID"}</span>`;
    // Metrc tracking block — manifest + shipped date, shown only once shipped.
    const metrcHTML = isShipped
      ? `<div class="billto" style="margin-top:-6px"><div>${order.manifestNumber ? `<div><b>Manifest #:</b> ${order.manifestNumber}</div>` : ""}${order.shipNotes ? `<div><b>Notes:</b> ${order.shipNotes}</div>` : ""}</div><div style="text-align:right">${order.shippedAt ? `<div><b>Shipped:</b> ${new Date(order.shippedAt).toLocaleDateString()}</div>` : ""}</div></div>`
      : "";
    const colHead = isShipped
      ? `<tr><th>Strain</th><th>Batch #</th><th>Source</th><th>Grade</th><th>Metrc Package #</th><th>Qty</th><th>Price</th><th>Total</th></tr>`
      : `<tr><th>Strain</th><th>Batch #</th><th>Source</th><th>Grade</th><th>Qty</th><th>Price</th><th>Total</th></tr>`;
    return `
      <div class="headrow">
        ${shipperHTML}
        <div style="text-align:right"><h1>INVOICE</h1><div class="sub">${order.orderNumber}</div><div style="margin-top:6px">${paidBadgeHTML}</div></div>
      </div>
      <div class="billto">
        <div><div class="label">Bill To</div><div>${order.customer}</div>${order.buyerName ? `<div>Attn: ${order.buyerName}</div>` : ""}${order.customerLicense ? `<div>License: ${order.customerLicense}</div>` : ""}${order.customerAddress ? `<div>${order.customerAddress}</div>` : ""}</div>
        <div style="text-align:right"><div><b>Date:</b> ${order.date}</div><div><b>Prepared by:</b> ${order.staff}</div></div>
      </div>
      ${metrcHTML}
      <table><thead>${colHead}</thead><tbody>${rows}</tbody></table>
      <div class="total"><span>Subtotal</span><span><b>${fmtMoney(order.subtotal)}</b></span></div>
      ${paymentHTML}`;
  }

  function printInvoice() { printHTML(`Invoice ${order.orderNumber}`, invoiceHTML()); }
  function downloadInvoice() { downloadHTML(`Invoice-${order.orderNumber}`, `Invoice ${order.orderNumber}`, invoiceHTML()); }

  if (editing) {
    return (
      <div>
        <div style={{ display: "flex", gap: 10, marginBottom: 14 }}>
          <button style={styles.backBtn} onClick={cancelEdit}>← Cancel</button>
        </div>
        <div style={styles.form}>
          <div style={styles.formHeader}><FileText size={18} color="#C9A24B" /><span>Editing {order.orderNumber}</span></div>
          <FieldLabel>Order Number</FieldLabel>
          <input style={styles.input} value={editOrderNumber} onChange={(e) => setEditOrderNumber(e.target.value)} />
          <FieldLabel>Customer / Distributor</FieldLabel>
          <input style={styles.input} value={editCustomer} onChange={(e) => setEditCustomer(e.target.value)} />
          <FieldLabel>Customer State License #</FieldLabel>
          <input style={styles.input} value={editLicense} onChange={(e) => setEditLicense(e.target.value)} />
          <FieldLabel>Customer Address (optional)</FieldLabel>
          <input style={styles.input} value={editAddress} onChange={(e) => setEditAddress(e.target.value)} />
          <FieldLabel>Buyer's Name (person, optional)</FieldLabel>
          <input style={styles.input} value={editBuyerName} onChange={(e) => setEditBuyerName(e.target.value)} />

          <div style={{ borderTop: "1px solid #232B1D", marginTop: 16, paddingTop: 14 }}>
            <div style={styles.sectionLabel}>LINE ITEMS</div>
            <div style={styles.lineItemList}>
              {editLineItems.map((li) => (
                <div key={li.id} style={styles.lineItemRow}>
                  <div>
                    <div style={styles.lineItemStrain}><OriginDot source={li.source} style={{ marginRight: 6 }} />{li.displayStrain || li.strain} <GradeBadge grade={li.grade} isBest={li.isBest} /></div>
                    <div style={styles.lineItemMeta}>{SOURCE_META[li.source].label}{li.lot ? ` · Batch ${li.lot}` : ""} · {fmtBoth(li.grams)}</div>
                  </div>
                  <div style={styles.lineItemRight}>
                    <input style={{ ...styles.input, width: 90, padding: "6px 8px" }} type="number" step="0.01" min="0" value={li.pricePerLb} onChange={(e) => changeEditPrice(li.id, e.target.value)} />
                    <span style={{ fontSize: 11, color: "#8C9483" }}>/lb</span>
                    <button type="button" onClick={() => removeEditItem(li.id)} style={styles.iconBtn}><X size={14} /></button>
                  </div>
                </div>
              ))}
              <div style={styles.subtotalRow}><span>Subtotal</span><span style={styles.subtotalValue}>{fmtMoney(editSubtotal)}</span></div>
            </div>

            <div style={{ marginTop: 14 }}>
              <FieldLabel>Add another item</FieldLabel>
              {addSelectedBatch ? (
                <div style={styles.selectedBatchChip}>
                  <div>
                    <div style={{ fontWeight: 600, color: "#EDE8D8" }}>{addSelectedBatch.strain} <GradeBadge grade={addSelectedBatch.grade} isBest={addSelectedBatch.isBest} /></div>
                    <div style={{ fontSize: 11, color: "#8C9483" }}>{SOURCE_META[addSelectedBatch.source].label} · {addSelectedBatch.room}{addSelectedBatch.lot ? ` · Batch ${addSelectedBatch.lot}` : ""} · {fmtBoth(addSelectedBatch.remainingGrams - (reservedInEdit[addSelectedBatch.id] || 0))} avail</div>
                  </div>
                  <button type="button" style={styles.iconLabelBtn} onClick={() => setAddBatchId("")}>Change</button>
                </div>
              ) : (
                <BatchSearchPicker
                  batches={editableBatches || []} reservedByBatch={reservedInEdit}
                  onSelect={(b) => { setAddBatchId(b.id); setAddSellAs(b.strain); }}
                />
              )}
              {addBatchId && (
                <>
                  <FieldLabel>Sell as (optional)</FieldLabel>
                  <input style={styles.input} value={addSellAs} onChange={(e) => setAddSellAs(e.target.value)} />
                </>
              )}
              <FieldLabel>Quantity</FieldLabel>
              <DualWeightInput grams={addGrams} onChange={setAddGrams} />
              <FieldLabel>Price per pound ($)</FieldLabel>
              <input style={styles.input} type="number" step="0.01" min="0" value={addPrice} onChange={(e) => setAddPrice(e.target.value)} placeholder="0.00" />
              <button type="button" style={{ ...styles.submitBtn, background: "#7C9473" }} onClick={addEditItem}><Plus size={16} /> Add Item</button>
            </div>
          </div>

          {editError && <div style={styles.errorText}>{editError}</div>}
          <button type="button" style={styles.submitBtn} onClick={saveEdit}><FileText size={16} /> Save Changes</button>
        </div>
      </div>
    );
  }

  return (
    <div>
      <div style={{ display: "flex", gap: 10, marginBottom: 14, flexWrap: "wrap" }}>
        <button style={styles.backBtn} onClick={onBack}>← Back to Orders</button>
        {canEdit && <button style={styles.iconLabelBtn} onClick={startEdit}><FileText size={14} /> Edit Invoice</button>}
        <button style={styles.iconLabelBtn} onClick={printInvoice}><Printer size={14} /> Print (8.5×11)</button>
        <button style={styles.iconLabelBtn} onClick={downloadInvoice}><Download size={14} /> Download</button>
        <button style={styles.iconLabelBtn} onClick={emailToDistribution}><Mail size={14} /> Email to Distribution</button>
        <button
          style={{ ...styles.iconLabelBtn, background: isPaid ? "rgba(124,148,115,0.16)" : "rgba(201,162,75,0.16)", borderColor: isPaid ? "#4A5A42" : "#5A4A2A" }}
          onClick={() => onTogglePaid(order.id)}
        >
          {isPaid ? <CheckCircle2 size={14} color="#8FAF8B" /> : <Circle size={14} color="#C9A24B" />} Mark as {isPaid ? "Unpaid" : "Paid"}
        </button>
      </div>
      <div style={{ fontSize: 11.5, color: "#6B7264", marginBottom: 14 }}>If Print doesn't trigger your browser's print dialog, use Download — it saves the invoice as a file you can open and print from anywhere.</div>
      {!distributionEmail && <div style={styles.noticeBox}>No distribution email is set yet — this will open your mail app blank so you can pick a recipient. An admin can set a default under Admin → Settings.</div>}
      <div style={styles.paperOuter}>
        <div style={styles.paper}>
          <div style={styles.paperHeader}>
            <div style={styles.shipperTop}>
              <div style={styles.shipperBizName}>{shipper?.name || "Shipper info not set"}</div>
              {shipper?.license && <div>License: {shipper.license}</div>}
              {shipper?.address && <div>{shipper.address}</div>}
              {shipper?.contact && <div>{shipper.contact}</div>}
            </div>
            <div style={{ textAlign: "right" }}>
              <div style={styles.paperTitle}>INVOICE</div>
              <div style={styles.paperSub}>{order.orderNumber}</div>
              <span style={{ ...styles.paidBadge, ...(isPaid ? styles.paidBadgePaid : styles.paidBadgeUnpaid), marginTop: 6, display: "inline-block" }}>{isPaid ? "PAID" : "UNPAID"}</span>
            </div>
          </div>
          <div style={styles.billToRow}>
            <div>
              <div style={styles.billToLabel}>Bill To</div>
              <div>{order.customer}</div>
              {order.buyerName && <div>Attn: {order.buyerName}</div>}
              {order.customerLicense && <div>License: {order.customerLicense}</div>}
              {order.customerAddress && <div>{order.customerAddress}</div>}
            </div>
            <div style={styles.paperMetaBlock}>
              <div><b>Date:</b> {order.date}</div><div><b>Prepared by:</b> {order.staff}</div>
              {order.emailedAt && <div style={{ color: "#7C9473" }}>Emailed to distribution</div>}
            </div>
          </div>
          {order.status === "shipped" && (order.manifestNumber || order.shipNotes || order.shippedAt) && (
            <div style={{ ...styles.billToRow, marginTop: -4, paddingTop: 8, borderTop: "1px dashed #ccc" }}>
              <div>
                {order.manifestNumber && <div><b>Manifest #:</b> {order.manifestNumber}</div>}
                {order.shipNotes && <div><b>Notes:</b> {order.shipNotes}</div>}
              </div>
              <div style={styles.paperMetaBlock}>
                {order.shippedAt && <div><b>Shipped:</b> {new Date(order.shippedAt).toLocaleDateString()}</div>}
              </div>
            </div>
          )}
          <table style={styles.paperTable}>
            <thead><tr><th style={styles.th}>Strain</th><th style={styles.th}>Batch #</th><th style={styles.th}>Source</th><th style={styles.th}>Grade</th>{order.status === "shipped" && <th style={styles.th}>Metrc Package #</th>}<th style={styles.th}>Qty</th><th style={styles.th}>Price</th><th style={styles.th}>Total</th></tr></thead>
            <tbody>
              {order.lineItems.map((li) => (
                <tr key={li.id}>
                  <td style={styles.td}>{li.displayStrain || li.strain}{li.displayStrain && li.displayStrain !== li.strain && <div style={{ fontSize: 11, color: "#777" }}>actual: {li.strain}</div>}</td>
                  <td style={styles.td}>{li.lot || "—"}</td>
                  <td style={styles.td}>{SOURCE_META[li.source].label}</td>
                  <td style={styles.td}>{li.grade}{li.isBest ? " ★" : ""}</td>
                  {order.status === "shipped" && <td style={{ ...styles.td, fontFamily: "monospace", fontSize: 11 }}>{li.metrcPackageNumber || "—"}</td>}
                  <td style={styles.td}>{fmtBoth(li.grams)}</td>
                  <td style={styles.td}>{fmtMoney(li.pricePerLb)}/lb</td><td style={styles.td}>{fmtMoney(li.lineTotal)}</td>
                </tr>
              ))}
            </tbody>
          </table>
          <div style={styles.paperTotalRow}><span>Subtotal</span><span style={{ fontWeight: 700 }}>{fmtMoney(order.subtotal)}</span></div>
          {(paymentInfo?.terms || paymentInfo?.instructions) && (
            <div style={styles.shipperBlock}>
              {paymentInfo?.terms && <div><b>Payment Terms:</b> {paymentInfo.terms}</div>}
              {paymentInfo?.instructions && <div style={{ marginTop: 4, whiteSpace: "pre-wrap" }}><b>Payment Instructions:</b><br />{paymentInfo.instructions}</div>}
            </div>
          )}
        </div>
      </div>
    </div>
  );
}

// ---------- REPORTS ----------

function ReconciliationTab({ batches, unit, currentUser, reconciliations, onSubmitReconciliation }) {
  const [counts, setCounts] = useState({});
  const [reasons, setReasons] = useState({});
  const [query, setQuery] = useState("");
  const [justSubmitted, setJustSubmitted] = useState(null);

  const filtered = useMemo(() => {
    const q = query.trim().toLowerCase();
    if (!q) return batches;
    return batches.filter((b) => b.strain.toLowerCase().includes(q) || b.room.toLowerCase().includes(q) || (b.lot || "").toLowerCase().includes(q));
  }, [batches, query]);

  function gramsFromInput(v) {
    const n = parseFloat(v);
    if (isNaN(n)) return null;
    return unit === "lb" ? n * G_PER_LB : n;
  }

  const adjustments = useMemo(() => {
    return Object.entries(counts).map(([batchId, v]) => {
      const g = gramsFromInput(v);
      if (g === null) return null;
      const b = batches.find((x) => x.id === batchId);
      if (!b) return null;
      const variance = g - b.remainingGrams;
      if (variance === 0) return null;
      return { batchId, strain: b.strain, room: b.room, grade: b.grade, source: b.source, lot: b.lot, before: b.remainingGrams, after: g, variance, reason: (reasons[batchId] || "").trim() };
    }).filter(Boolean);
  }, [counts, batches, reasons, unit]);

  function submit() {
    if (adjustments.length === 0) return;
    onSubmitReconciliation({ id: uid(), date: todayISO(), staff: currentUser.displayName, createdAt: Date.now(), adjustments });
    setJustSubmitted({ count: adjustments.length });
    setCounts({}); setReasons({});
  }

  return (
    <div>
      <div style={styles.sectionLabel}>INVENTORY RECONCILIATION</div>
      <div style={{ fontSize: 12, color: "#8C9483", marginBottom: 14 }}>
        Count what's actually on the shelf and enter it below. Only batches where your count differs from the system get adjusted — everything else stays untouched. Every change is logged with who did it and when.
      </div>
      {justSubmitted && <div style={styles.noticeBox}>Saved — {justSubmitted.count} batch{justSubmitted.count === 1 ? "" : "es"} adjusted.</div>}
      <div style={styles.searchBar}>
        <Search size={15} color="#7C8571" />
        <input style={styles.searchInput} placeholder="Search by strain, room, or batch #…" value={query} onChange={(e) => setQuery(e.target.value)} />
      </div>
      {filtered.length === 0 && <div style={styles.emptyState}>Nothing matches that search.</div>}
      <div style={styles.strainGrid}>
        {filtered.map((b) => {
          const val = counts[b.id];
          const g = val !== undefined && val !== "" ? gramsFromInput(val) : null;
          const hasVariance = g !== null && g !== b.remainingGrams;
          return (
            <div key={b.id} style={{ ...styles.orderRow, alignItems: "flex-start", borderColor: hasVariance ? "#C9A24B" : "#2A3324" }}>
              <div style={{ flex: 1 }}>
                <div style={styles.strainName}>{b.strain} · {b.room} <GradeBadge grade={b.grade} isBest={b.isBest} /></div>
                <div style={styles.historyMeta}>{SOURCE_META[b.source].label}{b.lot ? ` · Batch ${b.lot}` : ""} · System: {fmtWeight(b.remainingGrams, unit)}</div>
                {hasVariance && (
                  <input style={{ ...styles.input, marginTop: 6, maxWidth: 260 }} value={reasons[b.id] || ""} onChange={(e) => setReasons({ ...reasons, [b.id]: e.target.value })} placeholder="Reason (optional) — shrinkage, count error, etc." />
                )}
              </div>
              <div style={{ display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 4 }}>
                <input style={{ ...styles.input, width: 100, textAlign: "right" }} type="number" step="0.01" min="0" value={val || ""} onChange={(e) => setCounts({ ...counts, [b.id]: e.target.value })} placeholder={unit === "lb" ? "0.00" : "0"} />
                {hasVariance && (
                  <span style={{ fontSize: 11, fontWeight: 600, color: g > b.remainingGrams ? "#8FAF8B" : "#B9603F" }}>
                    {g > b.remainingGrams ? "+" : ""}{fmtWeight(g - b.remainingGrams, unit)}
                  </span>
                )}
              </div>
            </div>
          );
        })}
      </div>
      {adjustments.length > 0 && (
        <button style={styles.submitBtn} onClick={submit}><ClipboardCheck size={16} /> Save Reconciliation ({adjustments.length} change{adjustments.length === 1 ? "" : "s"})</button>
      )}

      <div style={styles.sectionLabel}>RECONCILIATION HISTORY</div>
      {(!reconciliations || reconciliations.length === 0) && <div style={styles.emptyState}>No reconciliations logged yet.</div>}
      <div style={styles.strainGrid}>
        {[...(reconciliations || [])].reverse().map((r) => (
          <div key={r.id} style={{ ...styles.orderRow, alignItems: "flex-start" }}>
            <div>
              <div style={styles.strainName}>{r.date} — {r.staff}</div>
              <div style={styles.historyMeta}>{r.adjustments.length} item{r.adjustments.length === 1 ? "" : "s"} adjusted</div>
              <div style={styles.strainSourceTags}>
                {r.adjustments.map((a, i) => (
                  <span key={i} style={styles.catalogChip}>
                    {a.strain} {a.variance > 0 ? "+" : ""}{fmtWeight(a.variance, unit)}{a.reason ? ` — ${a.reason}` : ""}
                  </span>
                ))}
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}

function ReportsTab({ batches, orders: allOrders, trimLogs, buckedLots, trimEntries, gradeEntries, wasteEntries, unit, visibleSources }) {
  const [report, setReport] = useState("inventory");
  const [sourceFilter, setSourceFilter] = useState("all");

  // Reports count only SHIPPED orders — pending (held) and samples aren't sales yet.
  const orders = useMemo(() => (allOrders || []).filter((o) => o.status === "shipped"), [allOrders]);

  const filteredBatches = sourceFilter === "all" ? batches : batches.filter((b) => b.source === sourceFilter);
  const byGroup = useMemo(() => {
    const map = new Map();
    for (const b of filteredBatches) {
      const key = `${b.strain}__${b.source}__${b.room}__${b.grade}`;
      if (!map.has(key)) map.set(key, { strain: b.strain, source: b.source, room: b.room, grade: b.grade, isBest: b.isBest, total: 0 });
      map.get(key).total += b.remainingGrams;
    }
    return Array.from(map.values()).sort((a, b) => a.strain.localeCompare(b.strain));
  }, [filteredBatches]);

  const filteredLineItems = useMemo(() => {
    if (sourceFilter === "all") return null;
    const rows = [];
    orders.forEach((o) => o.lineItems.forEach((li) => { if (li.source === sourceFilter) rows.push({ ...li, orderNumber: o.orderNumber, date: o.date, customer: o.customer, staff: o.staff }); }));
    return rows;
  }, [orders, sourceFilter]);
  const filteredOrdersTotal = filteredLineItems ? filteredLineItems.reduce((s, li) => s + li.lineTotal, 0) : orders.reduce((s, o) => s + o.subtotal, 0);

  const salesBySource = useMemo(() => {
    const totals = { daddyspipes: 0, merc: 0 };
    orders.forEach((o) => o.lineItems.forEach((li) => { totals[li.source] = (totals[li.source] || 0) + li.lineTotal; }));
    return totals;
  }, [orders]);

  const byCustomer = useMemo(() => {
    const map = new Map();
    orders.forEach((o) => {
      const key = o.customer;
      if (!map.has(key)) map.set(key, { customer: o.customer, orders: 0, total: 0, paid: 0, unpaid: 0 });
      const e = map.get(key);
      e.orders += 1; e.total += o.subtotal;
      if (o.paymentStatus === "paid") e.paid += o.subtotal; else e.unpaid += o.subtotal;
    });
    return Array.from(map.values()).sort((a, b) => b.total - a.total);
  }, [orders]);

  const filteredTrimLogs = sourceFilter === "all" ? trimLogs : trimLogs.filter((l) => l.source === sourceFilter);
  const trimByLot = useMemo(() => {
    const map = new Map();
    for (const log of filteredTrimLogs) {
      const key = log.lot;
      if (!map.has(key)) map.set(key, { lot: log.lot, strain: log.strain, room: log.room, source: log.source, grades: { A: 0, B: 0, C: 0, D: 0, T: 0 }, total: 0 });
      const entry = map.get(key);
      for (const li of log.lineItems) { entry.grades[li.grade] = (entry.grades[li.grade] || 0) + li.grams; entry.total += li.grams; }
    }
    return Array.from(map.values()).sort((a, b) => a.lot.localeCompare(b.lot));
  }, [filteredTrimLogs]);

  const filteredBuckedLots = sourceFilter === "all" ? buckedLots : buckedLots.filter((l) => l.source === sourceFilter);
  const buckedStatus = useMemo(() => {
    return filteredBuckedLots.map((l) => {
      const gEntries = (gradeEntries || []).filter((g) => g.buckedLotId === l.id);
      const grades = { A: 0, B: 0, C: 0, D: 0, T: 0 };
      gEntries.forEach((g) => {
        Object.entries(g.gradeGrams || {}).forEach(([grade, grams]) => { grades[grade] = (grades[grade] || 0) + grams; });
      });
      return {
        lot: l.lot, strain: l.strain, room: l.room, source: l.source, type: lotTypeLabel(l), received: l.receivedGrams,
        trimmed: l.trimmedGrams || 0, grades, waste: l.wasteGrams || 0, remaining: l.remainingGrams,
      };
    }).sort((a, b) => a.lot.localeCompare(b.lot));
  }, [filteredBuckedLots, gradeEntries]);

  const filteredTrimEntries = useMemo(() => {
    if (sourceFilter === "all") return trimEntries;
    const lotIds = new Set(filteredBuckedLots.map((l) => l.id));
    return trimEntries.filter((t) => lotIds.has(t.buckedLotId));
  }, [trimEntries, filteredBuckedLots, sourceFilter]);
  const byTrimmer = useMemo(() => {
    const map = new Map();
    filteredTrimEntries.forEach((t) => {
      map.set(t.trimmerName, (map.get(t.trimmerName) || 0) + trimEntryTotal(t));
    });
    return Array.from(map.entries()).map(([name, total]) => ({ name, total })).sort((a, b) => b.total - a.total);
  }, [filteredTrimEntries]);

  // Trimmer payroll report — a custom date range and trimmer selection, since pay periods rarely
  // line up with "all time" and a manager usually only needs a few trimmers at once.
  const [payStartDate, setPayStartDate] = useState("");
  const [payEndDate, setPayEndDate] = useState("");
  const [selectedTrimmers, setSelectedTrimmers] = useState([]); // empty = all
  const allTrimmerNames = useMemo(() => Array.from(new Set(trimEntries.map((t) => t.trimmerName))).sort(), [trimEntries]);
  function toggleTrimmerFilter(name) {
    setSelectedTrimmers((prev) => (prev.includes(name) ? prev.filter((n) => n !== name) : [...prev, name]));
  }
  const payEntries = useMemo(() => {
    return filteredTrimEntries.filter((t) => {
      if (payStartDate && t.date < payStartDate) return false;
      if (payEndDate && t.date > payEndDate) return false;
      if (selectedTrimmers.length > 0 && !selectedTrimmers.includes(t.trimmerName)) return false;
      return true;
    });
  }, [filteredTrimEntries, payStartDate, payEndDate, selectedTrimmers]);
  const payTotals = useMemo(() => {
    const map = new Map();
    payEntries.forEach((t) => {
      if (!map.has(t.trimmerName)) map.set(t.trimmerName, { name: t.trimmerName, total: 0, days: new Set() });
      const entry = map.get(t.trimmerName);
      entry.total += trimEntryTotal(t);
      entry.days.add(t.date);
    });
    return Array.from(map.values()).map((v) => ({ name: v.name, total: v.total, days: v.days.size })).sort((a, b) => b.total - a.total);
  }, [payEntries]);
  const payGrandTotal = payTotals.reduce((s, t) => s + t.total, 0);
  const payDetail = useMemo(() => [...payEntries].sort((a, b) => (a.date < b.date ? 1 : -1)), [payEntries]);
  function payLotLabel(id) {
    const lot = buckedLots.find((l) => l.id === id);
    return lot ? `${lot.strain} — ${lot.lot}` : "—";
  }

  function buildReportHTML() {
    let html;
    const sourceLabel = sourceFilter === "all" ? "All Sources" : SOURCE_META[sourceFilter].label;
    if (report === "inventory") {
      const rows = byGroup.map((r) => `<tr><td>${r.strain}</td><td>${SOURCE_META[r.source].label}</td><td>${r.room}</td><td>${r.grade}${r.isBest ? " ★" : ""}</td><td>${fmtWeight(r.total, unit)}</td></tr>`).join("");
      html = `<h1>INVENTORY REPORT</h1><div class="sub">${sourceLabel} · Generated ${todayISO()}</div><table><thead><tr><th>Strain</th><th>Source</th><th>Room</th><th>Grade</th><th>On Hand</th></tr></thead><tbody>${rows}</tbody></table>`;
    } else if (report === "orders") {
      if (filteredLineItems) {
        const rows = filteredLineItems.map((li) => `<tr><td>${li.orderNumber}</td><td>${li.date}</td><td>${li.displayStrain || li.strain}</td><td>${li.grade}</td><td>${li.room}</td><td>${fmtBoth(li.grams)}</td><td>${fmtMoney(li.pricePerLb)}/lb</td><td>${fmtMoney(li.lineTotal)}</td></tr>`).join("");
        html = `<h1>ORDERS REPORT</h1><div class="sub">${sourceLabel} · Generated ${todayISO()}</div><table><thead><tr><th>Order #</th><th>Date</th><th>Strain</th><th>Grade</th><th>Room</th><th>Qty</th><th>Price</th><th>Total</th></tr></thead><tbody>${rows}</tbody></table><div class="total"><span>Total</span><span><b>${fmtMoney(filteredOrdersTotal)}</b></span></div>`;
      } else {
        const rows = orders.map((o) => `<tr><td>${o.orderNumber}</td><td>${o.date}</td><td>${o.customer}</td><td>${o.staff}</td><td>${o.paymentStatus === "paid" ? "Paid" : "Unpaid"}</td><td>${fmtMoney(o.subtotal)}</td></tr>`).join("");
        html = `<h1>ORDERS REPORT</h1><div class="sub">${sourceLabel} · Generated ${todayISO()}</div><table><thead><tr><th>Order #</th><th>Date</th><th>Customer</th><th>Rep</th><th>Payment</th><th>Total</th></tr></thead><tbody>${rows}</tbody></table><div class="total"><span>Total Revenue</span><span><b>${fmtMoney(filteredOrdersTotal)}</b></span></div>`;
      }
    } else if (report === "customers") {
      const rows = byCustomer.map((c) => `<tr><td>${c.customer}</td><td>${c.orders}</td><td>${fmtMoney(c.paid)}</td><td>${fmtMoney(c.unpaid)}</td><td>${fmtMoney(c.total)}</td></tr>`).join("");
      html = `<h1>SALES BY CUSTOMER</h1><div class="sub">Generated ${todayISO()}</div><table><thead><tr><th>Customer</th><th>Orders</th><th>Paid</th><th>Unpaid</th><th>Total</th></tr></thead><tbody>${rows}</tbody></table>`;
    } else if (report === "trim") {
      const rows = trimByLot.map((r) => `<tr><td>${r.lot}</td><td>${r.strain}</td><td>${r.room}</td><td>${SOURCE_META[r.source].label}</td><td>${fmtWeight(r.grades.A, unit)}</td><td>${fmtWeight(r.grades.B, unit)}</td><td>${fmtWeight(r.grades.C, unit)}</td><td>${fmtWeight(r.grades.D, unit)}</td><td>${fmtWeight(r.grades.T, unit)}</td><td>${fmtWeight(r.total, unit)}</td></tr>`).join("");
      html = `<h1>TRIM REPORT — BY HARVEST/BATCH</h1><div class="sub">${sourceLabel} · Generated ${todayISO()}</div><table><thead><tr><th>Batch #</th><th>Strain</th><th>Room</th><th>Source</th><th>A</th><th>B</th><th>C</th><th>D</th><th>T</th><th>Total</th></tr></thead><tbody>${rows}</tbody></table>`;
    } else if (report === "bucked") {
      const rows = buckedStatus.map((r) => `<tr><td>${r.lot}</td><td>${r.strain}</td><td>${r.type}</td><td>${r.room}</td><td>${SOURCE_META[r.source].label}</td><td>${fmtWeight(r.received, unit)}</td><td>${fmtWeight(r.trimmed, unit)}</td><td>${fmtWeight(r.grades.A, unit)}</td><td>${fmtWeight(r.grades.B, unit)}</td><td>${fmtWeight(r.grades.C, unit)}</td><td>${fmtWeight(r.grades.D, unit)}</td><td>${fmtWeight(r.grades.T, unit)}</td><td>${fmtWeight(r.waste, unit)}</td><td>${fmtWeight(r.remaining, unit)}</td></tr>`).join("");
      const trimmerRows = byTrimmer.map((t) => `<tr><td>${t.name}</td><td>${fmtWeight(t.total, unit)}</td></tr>`).join("");
      html = `<h1>BUCKED FLOWER REPORT</h1><div class="sub">${sourceLabel} · Generated ${todayISO()}</div><table><thead><tr><th>Batch #</th><th>Strain</th><th>Type</th><th>Room</th><th>Source</th><th>Received</th><th>Trimmed</th><th>A</th><th>B</th><th>C</th><th>D</th><th>T</th><th>Waste</th><th>Remaining</th></tr></thead><tbody>${rows}</tbody></table><h1 style="margin-top:24px">ALL-TIME TOTAL BY TRIMMER</h1><table><thead><tr><th>Trimmer</th><th>Total Trimmed</th></tr></thead><tbody>${trimmerRows}</tbody></table>`;
    } else {
      const rangeLabel = (payStartDate || payEndDate) ? `${payStartDate || "start"} to ${payEndDate || "today"}` : "All time";
      const whoLabel = selectedTrimmers.length > 0 ? selectedTrimmers.join(", ") : "All trimmers";
      const totalRows = payTotals.map((t) => `<tr><td>${t.name}</td><td>${t.days}</td><td>${fmtWeight(t.total, unit)}</td></tr>`).join("");
      const detailRows = payDetail.map((t) => `<tr><td>${t.date}</td><td>${t.trimmerName}</td><td>${payLotLabel(t.buckedLotId)}</td><td>${fmtWeight(trimEntryTotal(t), unit)}</td></tr>`).join("");
      html = `<h1>TRIMMER PAY REPORT</h1><div class="sub">${rangeLabel} · ${whoLabel} · Generated ${todayISO()}</div><table><thead><tr><th>Trimmer</th><th>Days Logged</th><th>Total Weight</th></tr></thead><tbody>${totalRows}</tbody></table><div class="sub" style="margin-top:8px">Grand Total: ${fmtWeight(payGrandTotal, unit)}</div><h1 style="margin-top:24px">ENTRY DETAIL</h1><table><thead><tr><th>Date</th><th>Trimmer</th><th>Batch</th><th>Weight</th></tr></thead><tbody>${detailRows}</tbody></table>`;
    }
    return html;
  }
  const REPORT_TITLES = { inventory: "Inventory Report", orders: "Orders Report", customers: "Sales by Customer", trim: "Trim Report", bucked: "Bucked Flower Report", trimmers: "Trimmer Pay Report" };
  function printReport() { printHTML(REPORT_TITLES[report], buildReportHTML()); }
  function downloadReport() { downloadHTML(REPORT_TITLES[report].replace(/\s+/g, "-"), REPORT_TITLES[report], buildReportHTML()); }

  return (
    <div>
      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", flexWrap: "wrap", gap: 10, marginBottom: 12 }}>
        <div style={styles.groupToggle}>
          {[{ k: "inventory", label: "Inventory" }, { k: "orders", label: "Orders" }, { k: "customers", label: "By Customer" }, { k: "trim", label: "Trim by Harvest" }, { k: "bucked", label: "Bucked Flower" }, { k: "trimmers", label: "Trimmer Pay" }].map((opt) => (
            <button key={opt.k} onClick={() => setReport(opt.k)} style={{ ...styles.groupBtn, background: report === opt.k ? "#C9A24B" : "transparent", color: report === opt.k ? "#12160F" : "#B9BFA9" }}>{opt.label}</button>
          ))}
        </div>
        <div style={{ display: "flex", gap: 8 }}>
          <button style={styles.iconLabelBtn} onClick={printReport}><Printer size={14} /> Print</button>
          <button style={styles.iconLabelBtn} onClick={downloadReport}><Download size={14} /> Download</button>
        </div>
      </div>

      <div style={styles.summaryRow}>
        {(!visibleSources || visibleSources.includes("daddyspipes")) && <SummaryCard label={`Sales — ${SOURCE_META.daddyspipes.label}`} value={fmtMoney(salesBySource.daddyspipes)} accent={SOURCE_META.daddyspipes.color} />}
        {(!visibleSources || visibleSources.includes("merc")) && <SummaryCard label={`Sales — ${SOURCE_META.merc.label}`} value={fmtMoney(salesBySource.merc)} accent={SOURCE_META.merc.color} />}
        <SummaryCard label="Total Sales" value={fmtMoney(salesBySource.daddyspipes + salesBySource.merc)} accent="#EDE8D8" />
      </div>

      {report !== "customers" && (
        <div style={{ ...styles.groupToggle, marginBottom: 16 }}>
          {[{ k: "all", label: "All Sources" }, { k: "daddyspipes", label: SOURCE_META.daddyspipes.label }, { k: "merc", label: SOURCE_META.merc.label }]
            .filter((opt) => opt.k === "all" ? true : (!visibleSources || visibleSources.includes(opt.k)))
            .map((opt) => (
              <button key={opt.k} onClick={() => setSourceFilter(opt.k)} style={{ ...styles.groupBtn, background: sourceFilter === opt.k ? "#C9A24B" : "transparent", color: sourceFilter === opt.k ? "#12160F" : "#B9BFA9" }}>{opt.label}</button>
            ))}
        </div>
      )}

      <div style={styles.paperOuter}><div style={styles.paper}>
        <div style={styles.paperTitle}>{REPORT_TITLES[report].toUpperCase()}</div>
        <div style={styles.paperSub}>{report === "customers" ? "" : (sourceFilter === "all" ? "All Sources" : SOURCE_META[sourceFilter].label) + " · "}Generated {todayISO()}</div>

        {report === "inventory" && (
          <table style={styles.paperTable}>
            <thead><tr><th style={styles.th}>Strain</th><th style={styles.th}>Source</th><th style={styles.th}>Room</th><th style={styles.th}>Grade</th><th style={styles.th}>On Hand</th></tr></thead>
            <tbody>{byGroup.map((r, i) => (
              <tr key={i}><td style={styles.td}>{r.strain}</td><td style={styles.td}>{SOURCE_META[r.source].label}</td><td style={styles.td}>{r.room}</td><td style={styles.td}>{r.grade}{r.isBest ? " ★" : ""}</td><td style={styles.td}>{fmtWeight(r.total, unit)}</td></tr>
            ))}</tbody>
          </table>
        )}

        {report === "orders" && (filteredLineItems ? (
          <>
            <table style={styles.paperTable}>
              <thead><tr><th style={styles.th}>Order #</th><th style={styles.th}>Date</th><th style={styles.th}>Strain</th><th style={styles.th}>Grade</th><th style={styles.th}>Room</th><th style={styles.th}>Qty</th><th style={styles.th}>Price</th><th style={styles.th}>Total</th></tr></thead>
              <tbody>{filteredLineItems.map((li, i) => (
                <tr key={i}><td style={styles.td}>{li.orderNumber}</td><td style={styles.td}>{li.date}</td><td style={styles.td}>{li.displayStrain || li.strain}</td><td style={styles.td}>{li.grade}</td><td style={styles.td}>{li.room}</td><td style={styles.td}>{fmtBoth(li.grams)}</td><td style={styles.td}>{fmtMoney(li.pricePerLb)}/lb</td><td style={styles.td}>{fmtMoney(li.lineTotal)}</td></tr>
              ))}</tbody>
            </table>
            <div style={styles.paperTotalRow}><span>Total</span><span style={{ fontWeight: 700 }}>{fmtMoney(filteredOrdersTotal)}</span></div>
          </>
        ) : (
          <>
            <table style={styles.paperTable}>
              <thead><tr><th style={styles.th}>Order #</th><th style={styles.th}>Date</th><th style={styles.th}>Customer</th><th style={styles.th}>Rep</th><th style={styles.th}>Payment</th><th style={styles.th}>Total</th></tr></thead>
              <tbody>{orders.map((o) => (
                <tr key={o.id}><td style={styles.td}>{o.orderNumber}</td><td style={styles.td}>{o.date}</td><td style={styles.td}>{o.customer}</td><td style={styles.td}>{o.staff}</td><td style={styles.td}>{o.paymentStatus === "paid" ? "Paid" : "Unpaid"}</td><td style={styles.td}>{fmtMoney(o.subtotal)}</td></tr>
              ))}</tbody>
            </table>
            <div style={styles.paperTotalRow}><span>Total Revenue</span><span style={{ fontWeight: 700 }}>{fmtMoney(filteredOrdersTotal)}</span></div>
          </>
        ))}

        {report === "customers" && (
          <>
            <table style={styles.paperTable}>
              <thead><tr><th style={styles.th}>Customer</th><th style={styles.th}>Orders</th><th style={styles.th}>Paid</th><th style={styles.th}>Unpaid</th><th style={styles.th}>Total</th></tr></thead>
              <tbody>{byCustomer.map((c, i) => (
                <tr key={i}><td style={styles.td}>{c.customer}</td><td style={styles.td}>{c.orders}</td><td style={styles.td}>{fmtMoney(c.paid)}</td><td style={styles.td}>{fmtMoney(c.unpaid)}</td><td style={styles.td}>{fmtMoney(c.total)}</td></tr>
              ))}</tbody>
            </table>
          </>
        )}

        {report === "trim" && (
          <table style={styles.paperTable}>
            <thead><tr><th style={styles.th}>Batch #</th><th style={styles.th}>Strain</th><th style={styles.th}>Room</th><th style={styles.th}>Source</th><th style={styles.th}>A</th><th style={styles.th}>B</th><th style={styles.th}>C</th><th style={styles.th}>D</th><th style={styles.th}>T</th><th style={styles.th}>Total</th></tr></thead>
            <tbody>{trimByLot.map((r, i) => (
              <tr key={i}>
                <td style={styles.td}>{r.lot}</td><td style={styles.td}>{r.strain}</td><td style={styles.td}>{r.room}</td><td style={styles.td}>{SOURCE_META[r.source].label}</td>
                <td style={styles.td}>{fmtWeight(r.grades.A, unit)}</td><td style={styles.td}>{fmtWeight(r.grades.B, unit)}</td><td style={styles.td}>{fmtWeight(r.grades.C, unit)}</td><td style={styles.td}>{fmtWeight(r.grades.D, unit)}</td><td style={styles.td}>{fmtWeight(r.grades.T, unit)}</td>
                <td style={styles.td}>{fmtWeight(r.total, unit)}</td>
              </tr>
            ))}</tbody>
          </table>
        )}

        {report === "bucked" && (
          <>
            <table style={styles.paperTable}>
              <thead><tr><th style={styles.th}>Batch #</th><th style={styles.th}>Strain</th><th style={styles.th}>Type</th><th style={styles.th}>Room</th><th style={styles.th}>Source</th><th style={styles.th}>Received</th><th style={styles.th}>Trimmed</th><th style={styles.th}>A</th><th style={styles.th}>B</th><th style={styles.th}>C</th><th style={styles.th}>D</th><th style={styles.th}>T</th><th style={styles.th}>Waste</th><th style={styles.th}>Remaining</th></tr></thead>
              <tbody>{buckedStatus.map((r, i) => (
                <tr key={i}>
                  <td style={styles.td}>{r.lot}</td><td style={styles.td}>{r.strain}</td><td style={styles.td}>{r.type}</td><td style={styles.td}>{r.room}</td><td style={styles.td}>{SOURCE_META[r.source].label}</td>
                  <td style={styles.td}>{fmtWeight(r.received, unit)}</td><td style={styles.td}>{fmtWeight(r.trimmed, unit)}</td>
                  <td style={styles.td}>{fmtWeight(r.grades.A, unit)}</td><td style={styles.td}>{fmtWeight(r.grades.B, unit)}</td><td style={styles.td}>{fmtWeight(r.grades.C, unit)}</td><td style={styles.td}>{fmtWeight(r.grades.D, unit)}</td><td style={styles.td}>{fmtWeight(r.grades.T, unit)}</td>
                  <td style={styles.td}>{fmtWeight(r.waste, unit)}</td><td style={styles.td}>{fmtWeight(r.remaining, unit)}</td>
                </tr>
              ))}</tbody>
            </table>
            <div style={{ ...styles.sectionLabel, marginTop: 20 }}>ALL-TIME TOTAL BY TRIMMER</div>
            <table style={styles.paperTable}>
              <thead><tr><th style={styles.th}>Trimmer</th><th style={styles.th}>Total Trimmed</th></tr></thead>
              <tbody>{byTrimmer.map((t, i) => (
                <tr key={i}><td style={styles.td}>{t.name}</td><td style={styles.td}>{fmtWeight(t.total, unit)}</td></tr>
              ))}</tbody>
            </table>
          </>
        )}

        {report === "trimmers" && (
          <>
            <div style={styles.twoCol}>
              <div><FieldLabel>From</FieldLabel><input style={styles.input} type="date" value={payStartDate} onChange={(e) => setPayStartDate(e.target.value)} /></div>
              <div><FieldLabel>To</FieldLabel><input style={styles.input} type="date" value={payEndDate} onChange={(e) => setPayEndDate(e.target.value)} /></div>
            </div>
            <FieldLabel>Trimmers {selectedTrimmers.length > 0 ? `(${selectedTrimmers.length} selected)` : "(showing all)"}</FieldLabel>
            <div style={{ display: "flex", flexWrap: "wrap", gap: 6, marginBottom: 16 }}>
              {allTrimmerNames.length === 0 && <div style={{ fontSize: 12.5, color: "#8C9483" }}>No trim logged yet.</div>}
              {allTrimmerNames.map((name) => (
                <button key={name} type="button" onClick={() => toggleTrimmerFilter(name)} style={{ ...styles.quickFillChip, background: selectedTrimmers.includes(name) ? "#C9A24B" : "rgba(201,162,75,0.10)", color: selectedTrimmers.includes(name) ? "#12160F" : "#D8C08A" }}>{name}</button>
              ))}
              {selectedTrimmers.length > 0 && <button type="button" onClick={() => setSelectedTrimmers([])} style={styles.linkBtn}>Clear (show all)</button>}
            </div>

            <table style={styles.paperTable}>
              <thead><tr><th style={styles.th}>Trimmer</th><th style={styles.th}>Days Logged</th><th style={styles.th}>Total Weight</th></tr></thead>
              <tbody>{payTotals.map((t, i) => (
                <tr key={i}><td style={styles.td}>{t.name}</td><td style={styles.td}>{t.days}</td><td style={styles.td}>{fmtWeight(t.total, unit)}</td></tr>
              ))}</tbody>
            </table>
            <div style={styles.paperTotalRow}><span>Grand Total</span><span><b>{fmtWeight(payGrandTotal, unit)}</b></span></div>

            <div style={{ ...styles.sectionLabel, marginTop: 20 }}>ENTRY DETAIL</div>
            <table style={styles.paperTable}>
              <thead><tr><th style={styles.th}>Date</th><th style={styles.th}>Trimmer</th><th style={styles.th}>Batch</th><th style={styles.th}>Weight</th></tr></thead>
              <tbody>{payDetail.map((t, i) => (
                <tr key={i}><td style={styles.td}>{t.date}</td><td style={styles.td}>{t.trimmerName}</td><td style={styles.td}>{payLotLabel(t.buckedLotId)}</td><td style={styles.td}>{fmtWeight(trimEntryTotal(t), unit)}</td></tr>
              ))}</tbody>
            </table>
          </>
        )}
      </div></div>
    </div>
  );
}

// ---------- PHOTO MENU ----------
function MenuTab({ batches, photos, priceList, priceOverrides, shipper, unit }) {
  const [groupMode, setGroupMode] = useState("strain");

  const grouped = useMemo(() => {
    if (groupMode === "batch") {
      return groupBatches(batches, (b) => b.id).map((g) => ({ ...g, label: `${g.batches[0].strain} — Grade ${g.batches[0].grade}${g.batches[0].lot ? ` — Batch ${g.batches[0].lot}` : ""}` }));
    }
    if (groupMode === "strain-grade") {
      return groupBatches(batches, (b) => `${b.strain}__${b.grade}`).map((g) => ({ ...g, label: `${g.batches[0].strain} — Grade ${g.batches[0].grade}` }));
    }
    return groupBatches(batches, (b) => b.strain).map((g) => ({ ...g, label: g.key }));
  }, [batches, groupMode]);

  function photoFor(group) {
    const batchPhoto = group.batches.map((b) => photos.byBatch?.[b.id]).find(Boolean);
    return batchPhoto || photos.byStrain?.[group.batches[0].strain] || photos.bySource?.[group.batches[0]?.source] || null;
  }
  function priceRangeFor(group) {
    const prices = group.batches.map((b) => getEffectivePrice(b, priceList, priceOverrides)).filter((p) => p > 0);
    if (prices.length === 0) return "Ask for pricing";
    const min = Math.min(...prices), max = Math.max(...prices);
    return min === max ? `${fmtMoney(min)}/lb` : `${fmtMoney(min)}-${fmtMoney(max)}/lb`;
  }

  function menuHTML() {
    const cards = grouped.map((g) => {
      const photo = photoFor(g);
      const grades = groupMode === "strain" ? [...new Set(g.batches.map((b) => b.grade))].join(", ") : g.batches[0].grade;
      return `<div class="menu-card">${photo ? `<img class="photo" src="${photo}" />` : `<div class="photo" style="display:flex;align-items:center;justify-content:center;color:#999;font-size:11px;">No photo</div>`}<div class="info"><div class="name">${g.label}</div><div class="price">Grade${groupMode === "strain" ? "s" : ""}: ${grades} &middot; ${fmtWeight(g.total, unit)} on hand</div><div class="price">${priceRangeFor(g)}</div></div></div>`;
    }).join("");
    return `<h1>${shipper?.name || "Menu"}</h1><div class="sub">Available flower - Generated ${todayISO()}</div><div class="menu-grid">${cards}</div>`;
  }
  function printMenu() { printHTML("Menu", menuHTML()); }
  function downloadMenu() { downloadHTML("Menu", "Menu", menuHTML()); }

  return (
    <div>
      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", flexWrap: "wrap", gap: 10, marginBottom: 12 }}>
        <div style={styles.sectionLabel}>MENU — SHARE WITH CUSTOMERS</div>
        <div style={{ display: "flex", gap: 8 }}>
          <button style={styles.iconLabelBtn} onClick={printMenu}><Printer size={14} /> Print</button>
          <button style={styles.iconLabelBtn} onClick={downloadMenu}><Download size={14} /> Download</button>
        </div>
      </div>
      <div style={{ ...styles.groupToggle, marginBottom: 12 }}>
        {[{ k: "strain", label: "By Strain" }, { k: "strain-grade", label: "By Strain + Grade" }, { k: "batch", label: "By Batch" }].map((opt) => (
          <button key={opt.k} onClick={() => setGroupMode(opt.k)} style={{ ...styles.groupBtn, background: groupMode === opt.k ? "#C9A24B" : "transparent", color: groupMode === opt.k ? "#12160F" : "#B9BFA9" }}>{opt.label}</button>
        ))}
      </div>
      <div style={{ fontSize: 12, color: "#8C9483", marginBottom: 16 }}>
        Download or print a menu with photos to send to customers. Add photos per strain, per source, or per individual batch under Admin → Rooms & Strains and Inventory. A batch photo wins if set, then a strain photo, then a source photo — so switching to "By Batch" or "By Strain + Grade" is what shows those more specific photos as separate items instead of one blended card.
      </div>
      {grouped.length === 0 && <div style={styles.emptyState}>Nothing in stock to show yet.</div>}
      <div style={styles.menuGridScreen}>
        {grouped.map((g) => {
          const photo = photoFor(g);
          return (
            <div key={g.key} style={styles.menuCardScreen}>
              <div style={styles.menuCardPhoto}>
                {photo ? <img src={photo} style={{ width: "100%", height: "100%", objectFit: "cover" }} /> : <ImageIcon size={22} color="#4B5240" />}
              </div>
              <div style={{ padding: "10px 12px" }}>
                <div style={{ fontWeight: 700, fontSize: 14 }}>{g.label}</div>
                <div style={{ fontSize: 12, color: "#B9BFA9", marginTop: 3 }}>{fmtWeight(g.total, unit)} on hand</div>
                <div style={{ fontSize: 12.5, color: "#C9A24B", marginTop: 4, fontWeight: 600 }}>{priceRangeFor(g)}</div>
              </div>
            </div>
          );
        })}
      </div>
    </div>
  );
}

// ---------- EXECUTIVE OVERVIEW ----------


// Phase 2: Cultivation home + scanner test. Scan a tag → see what it resolves to. This is the
// foundation the clone-cut / room-move / harvest flows (Phase 3+) will build on.
function CultivationTab({ data, currentUser, facility }) {
  const [scanOpen, setScanOpen] = useState(false);
  const [scanHistory, setScanHistory] = useState([]); // [{ tag, kind, record, at }]

  function handleScan(tag) {
    const { kind, record } = resolveTag(tag, data);
    setScanHistory((prev) => [{ tag, kind, record, at: Date.now() }, ...prev].slice(0, 20));
    setScanOpen(false);
  }

  const mothers = data.mothers || [];
  const plantBatches = (data.plantBatches || []).filter((b) => !b.archived);
  const plants = (data.plants || []).filter((p) => p.status === "growing");
  const harvests = (data.harvests || []).filter((h) => h.status === "drying");

  function kindLabel(kind) {
    if (kind === "mother") return "Mother";
    if (kind === "plantBatch") return "Plant Batch";
    if (kind === "plant") return "Plant";
    return "Not in system yet";
  }
  function kindColor(kind) {
    if (kind === "mother") return "#7C9473";
    if (kind === "plantBatch") return "#8FAF8B";
    if (kind === "plant") return "#C08FD8";
    return "#B9603F";
  }

  return (
    <div>
      <TagScanner open={scanOpen} onScan={handleScan} onClose={() => setScanOpen(false)} title="Scan a Metrc Tag" />

      <div style={styles.form}>
        <div style={styles.formHeader}><Sprout size={18} color="#7C9473" /><span>Cultivation</span></div>
        <div style={{ fontSize: 12, color: "#8C9483", marginBottom: 14 }}>Scan any plant, batch, or mother tag to pull it up. (Early preview — clone-cutting, room moves, and harvest logging are on the way.)</div>
        <button style={{ ...styles.submitBtn, fontSize: 16, padding: "14px" }} onClick={() => setScanOpen(true)}>
          <Camera size={18} /> Scan Tag
        </button>
      </div>

      <div style={styles.summaryRow}>
        <SummaryCard label="Mothers" value={String(mothers.length)} accent="#7C9473" />
        <SummaryCard label="Plant Batches" value={String(plantBatches.length)} accent="#8FAF8B" />
        <SummaryCard label="Plants (flowering)" value={String(plants.length)} accent="#C08FD8" />
        <SummaryCard label="Drying Harvests" value={String(harvests.length)} accent="#C9A24B" />
      </div>

      {scanHistory.length > 0 && (
        <>
          <div style={styles.sectionLabel}>SCANNED THIS SESSION</div>
          <div style={styles.historyList}>
            {scanHistory.map((s, i) => (
              <div key={i} style={styles.scanResultCard}>
                <ScanLine size={16} color={kindColor(s.kind)} />
                <div style={{ flex: 1 }}>
                  <div style={{ fontFamily: "monospace", fontSize: 13, color: "#EDE8D8" }}>{s.tag}</div>
                  <div style={{ fontSize: 12, color: kindColor(s.kind), fontWeight: 600 }}>
                    {kindLabel(s.kind)}
                    {s.record && s.record.strain ? ` · ${s.record.strain}` : ""}
                    {s.record && s.record.room ? ` · ${s.record.room}` : ""}
                  </div>
                </div>
              </div>
            ))}
          </div>
          <div style={{ fontSize: 11.5, color: "#6B7264", marginTop: 8 }}>Right now scans just look up the tag. Since the sandbox is empty, everything will read "Not in system yet" until we add the flows that create mothers, batches, and plants — that's next.</div>
        </>
      )}
    </div>
  );
}

// Phase 1: Vaults & BINs (MERC/Lemonnade SOP). Receive bucked flower as a tared BIN tied to a METRC
// UID, see BINs grouped by custody vault, and move a BIN vault-to-vault. Operational only — no money
// yet (that's Phases 4–6, blocked on MERC decisions).
function VaultsTab({ data, currentUser, facility, unit, onPersist }) {
  const bins = data.bins || [];
  const [view, setView] = useState("board"); // board | receive
  // Receive form
  const [binNumber, setBinNumber] = useState("");
  const [metrcUid, setMetrcUid] = useState("");
  const [manifestNumber, setManifestNumber] = useState("");
  const [strain, setStrain] = useState("");
  const [tare, setTare] = useState("");
  const [gross, setGross] = useState("");
  const [err, setErr] = useState("");
  const [justMade, setJustMade] = useState(null);

  const toG = (lbStr) => (parseFloat(lbStr) || 0) * (unit === "lb" ? G_PER_LB : 1);
  const net = Math.max(0, toG(gross) - toG(tare));

  function receiveBin() {
    setErr("");
    if (!binNumber.trim()) return setErr("Enter a BIN # (any label you want to track it by).");
    if (!metrcUid.trim()) return setErr("Enter the METRC UID from the manifest.");
    if (toG(gross) <= 0) return setErr("Enter the gross (weighed) weight.");
    if (toG(gross) < toG(tare)) return setErr("Gross weight can't be less than the tare weight.");
    if (bins.some((b) => b.binNumber.toLowerCase() === binNumber.trim().toLowerCase())) {
      return setErr(`BIN # "${binNumber.trim()}" already exists. Use a different number.`);
    }
    const bin = makeBin({
      binNumber, metrcUid, manifestNumber, strain: strain.trim(),
      tareGrams: toG(tare), grossGrams: toG(gross),
      facility, staff: currentUser.displayName, vault: "processing",
    });
    onPersist({ ...data, bins: [...bins, bin] });
    setJustMade(bin);
    setBinNumber(""); setMetrcUid(""); setManifestNumber(""); setStrain(""); setTare(""); setGross("");
  }

  function moveBin(bin, toVault) {
    const transfer = makeVaultTransfer({ from: bin.vault, to: toVault, grams: bin.netGrams, staff: currentUser.displayName });
    const updated = bins.map((b) => b.id === bin.id ? { ...b, vault: toVault, transfers: [...(b.transfers || []), transfer] } : b);
    onPersist({ ...data, bins: updated });
  }

  const facilityBins = bins.filter((b) => facility === "both" || b.facility === facility);
  const byVault = {};
  VAULTS.forEach((v) => (byVault[v] = []));
  facilityBins.forEach((b) => { (byVault[b.vault] || (byVault[b.vault] = [])).push(b); });

  const nextVaultOf = (v) => { const i = VAULTS.indexOf(v); return i >= 0 && i < VAULTS.length - 1 ? VAULTS[i + 1] : null; };
  const prevVaultOf = (v) => { const i = VAULTS.indexOf(v); return i > 0 ? VAULTS[i - 1] : null; };

  return (
    <div>
      <div style={styles.form}>
        <div style={styles.formHeader}><ShieldCheck size={18} color="#7C9473" /><span>Vaults & BINs</span></div>
        <div style={{ fontSize: 12, color: "#8C9483", marginBottom: 12 }}>Receive bucked flower as a tared BIN, track it through each custody vault, and move it along the flow. (Early preview — the invoice & settlement side comes later.)</div>
        <div style={styles.groupToggle}>
          <button onClick={() => setView("board")} style={{ ...styles.groupBtn, background: view === "board" ? "#C9A24B" : "transparent", color: view === "board" ? "#12160F" : "#B9BFA9" }}>Vault Board</button>
          <button onClick={() => setView("receive")} style={{ ...styles.groupBtn, background: view === "receive" ? "#C9A24B" : "transparent", color: view === "receive" ? "#12160F" : "#B9BFA9" }}>Receive BIN</button>
        </div>
      </div>

      {view === "receive" && (
        <div style={styles.form}>
          <FieldLabel>BIN # (your internal label, created now)</FieldLabel>
          <input style={styles.input} value={binNumber} onChange={(e) => setBinNumber(e.target.value)} placeholder="e.g. BIN-014" />
          <FieldLabel>METRC UID (from the manifest)</FieldLabel>
          <input style={{ ...styles.input, fontFamily: "monospace" }} value={metrcUid} onChange={(e) => setMetrcUid(e.target.value.toUpperCase())} placeholder="1A40602…" />
          <FieldLabel>Manifest # (optional)</FieldLabel>
          <input style={styles.input} value={manifestNumber} onChange={(e) => setManifestNumber(e.target.value)} placeholder="METRC manifest number" />
          <FieldLabel>Strain (optional)</FieldLabel>
          <StrainPicker strains={data.strains} value={strain} onChange={setStrain} />
          <div style={styles.twoCol}>
            <div><FieldLabel>Tare — empty bin ({unit})</FieldLabel><input style={styles.input} type="number" step="0.01" value={tare} onChange={(e) => setTare(e.target.value)} placeholder="0" /></div>
            <div><FieldLabel>Gross — bin + flower ({unit})</FieldLabel><input style={styles.input} type="number" step="0.01" value={gross} onChange={(e) => setGross(e.target.value)} placeholder="0" /></div>
          </div>
          <div style={{ ...styles.readoutRow, marginTop: 6 }}>
            <span style={{ color: "#8C9483", fontSize: 13 }}>Net flower (logged against tare)</span>
            <span style={{ ...styles.readout, color: "#C9A24B" }}>{fmtWeight(net, unit)}</span>
          </div>
          {err && <div style={styles.errorText}>{err}</div>}
          <button style={{ ...styles.submitBtn, marginTop: 10 }} onClick={receiveBin}><Plus size={16} /> Receive BIN into Processing Vault</button>
          {justMade && (
            <div style={{ ...styles.noticeBox, marginTop: 12, borderColor: "#7C9473" }}>
              ✓ BIN {justMade.binNumber} received — {fmtWeight(justMade.netGrams, unit)} net, now in Processing Vault.
            </div>
          )}
        </div>
      )}

      {view === "board" && (
        VAULTS.map((v) => (
          <div key={v} style={{ marginBottom: 18 }}>
            <div style={{ display: "flex", alignItems: "center", gap: 8, margin: "6px 0 8px" }}>
              <span style={{ width: 10, height: 10, borderRadius: "50%", background: VAULT_META[v].color, display: "inline-block" }} />
              <span style={{ fontFamily: "'Oswald', sans-serif", letterSpacing: "0.04em", color: "#EDE8D8", fontSize: 15 }}>{VAULT_META[v].label.toUpperCase()}</span>
              <span style={{ fontSize: 12, color: "#6B7264" }}>({byVault[v].length})</span>
            </div>
            {byVault[v].length === 0 ? (
              <div style={{ fontSize: 12.5, color: "#5E6557", paddingLeft: 18 }}>Empty</div>
            ) : (
              byVault[v].map((b) => (
                <div key={b.id} style={styles.binCard}>
                  <div style={{ flex: 1 }}>
                    <div style={{ fontWeight: 600, color: "#EDE8D8" }}>{b.binNumber}{b.strain ? ` · ${b.strain}` : ""}</div>
                    <div style={{ fontSize: 11.5, color: "#8C9483" }}>
                      <span style={{ fontFamily: "monospace" }}>…{normalizeTag(b.metrcUid).slice(-6)}</span> · {fmtWeight(b.netGrams, unit)} net · tare {fmtWeight(b.tareGrams, unit)}
                    </div>
                  </div>
                  <div style={{ display: "flex", gap: 6 }}>
                    {prevVaultOf(b.vault) && (
                      <button title={`Back to ${VAULT_META[prevVaultOf(b.vault)].short}`} style={styles.binMoveBtn} onClick={() => moveBin(b, prevVaultOf(b.vault))}>←</button>
                    )}
                    {nextVaultOf(b.vault) && (
                      <button style={{ ...styles.binMoveBtn, borderColor: VAULT_META[nextVaultOf(b.vault)].color, color: VAULT_META[nextVaultOf(b.vault)].color }} onClick={() => moveBin(b, nextVaultOf(b.vault))}>
                        → {VAULT_META[nextVaultOf(b.vault)].short}
                      </button>
                    )}
                  </div>
                </div>
              ))
            )}
          </div>
        ))
      )}
    </div>
  );
}

function ExecDashboard({ batches, orders, priceList, priceOverrides, unit, cashOnHand, onUpdateCashOnHand }) {
  const totalOnHand = batches.reduce((s, b) => s + b.remainingGrams, 0);
  // Only SHIPPED orders count as revenue. Pending (held) and samples are excluded until shipped.
  const shippedOrders = useMemo(() => orders.filter((o) => o.status === "shipped"), [orders]);
  const totalRevenue = shippedOrders.reduce((s, o) => s + o.subtotal, 0);
  const unpaidTotal = shippedOrders.filter((o) => o.paymentStatus !== "paid").reduce((s, o) => s + o.subtotal, 0);

  const thisMonthKey = todayISO().slice(0, 7);
  const monthOrders = shippedOrders.filter((o) => o.date.slice(0, 7) === thisMonthKey);
  const revenueThisMonth = monthOrders.reduce((s, o) => s + o.subtotal, 0);

  // Revenue split by facility, from the line items (each carries its own source), so a mixed order
  // still attributes each line to the right facility. Shipped-only.
  const revBySource = useMemo(() => {
    const mtd = { daddyspipes: 0, merc: 0 };
    const all = { daddyspipes: 0, merc: 0 };
    shippedOrders.forEach((o) => {
      const inMonth = o.date.slice(0, 7) === thisMonthKey;
      o.lineItems.forEach((li) => {
        all[li.source] = (all[li.source] || 0) + li.lineTotal;
        if (inMonth) mtd[li.source] = (mtd[li.source] || 0) + li.lineTotal;
      });
    });
    return { mtd, all };
  }, [shippedOrders, thisMonthKey]);

  const cash = cashOnHand || { daddyspipes: 0, merc: 0 };
  const cashTotal = (cash.daddyspipes || 0) + (cash.merc || 0);
  const [editingCash, setEditingCash] = useState(false);
  const [cashForm, setCashForm] = useState({ daddyspipes: "", merc: "" });
  function startEditCash() {
    setCashForm({ daddyspipes: String(cash.daddyspipes || 0), merc: String(cash.merc || 0) });
    setEditingCash(true);
  }
  function saveCash() {
    onUpdateCashOnHand({ daddyspipes: parseFloat(cashForm.daddyspipes) || 0, merc: parseFloat(cashForm.merc) || 0 });
    setEditingCash(false);
  }

  const oneWeekAgo = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString().slice(0, 10);
  const ordersThisWeek = shippedOrders.filter((o) => o.date >= oneWeekAgo).length;

  const inventoryValue = useMemo(() => batches.reduce((s, b) => s + (b.remainingGrams / G_PER_LB) * getEffectivePrice(b, priceList, priceOverrides), 0), [batches, priceList, priceOverrides]);
  const valueBySource = useMemo(() => {
    const totals = { daddyspipes: 0, merc: 0 };
    batches.forEach((b) => { totals[b.source] += (b.remainingGrams / G_PER_LB) * getEffectivePrice(b, priceList, priceOverrides); });
    return totals;
  }, [batches, priceList, priceOverrides]);
  const unpricedWeight = useMemo(() => batches.filter((b) => getEffectivePrice(b, priceList, priceOverrides) === 0).reduce((s, b) => s + b.remainingGrams, 0), [batches, priceList, priceOverrides]);

  // Top strains carry both revenue and lbs sold, so we can show an average $/lb per strain —
  // for the current month and all-time. lbs come from grams / 454.
  function buildTopStrains(orderSet) {
    const map = new Map();
    orderSet.forEach((o) => o.lineItems.forEach((li) => {
      const key = li.displayStrain || li.strain;
      if (!map.has(key)) map.set(key, { strain: key, total: 0, lbs: 0 });
      const e = map.get(key);
      e.total += li.lineTotal;
      e.lbs += li.grams / G_PER_LB;
    }));
    return Array.from(map.values())
      .map((e) => ({ ...e, avgPerLb: e.lbs > 0 ? e.total / e.lbs : 0 }))
      .sort((a, b) => b.total - a.total).slice(0, 5);
  }
  const topStrainsMonth = useMemo(() => buildTopStrains(monthOrders), [monthOrders, thisMonthKey]);
  const topStrainsAll = useMemo(() => buildTopStrains(shippedOrders), [shippedOrders]);

  const lowStockGroups = useMemo(() => groupBatches(batches, (b) => b.strain).filter((g) => g.total < LOW_STOCK_G), [batches]);

  function TopStrainList({ list }) {
    if (list.length === 0) return <div style={styles.emptyState}>No sales yet.</div>;
    return (
      <div style={styles.strainGrid}>
        {list.map((s, i) => (
          <div key={i} style={styles.orderRow}>
            <div style={styles.strainName}>{i + 1}. {s.strain}</div>
            <div style={{ textAlign: "right" }}>
              <div style={styles.readout}>{fmtMoney(s.total)}</div>
              <div style={{ fontSize: 11, color: "#8C9483" }}>{fmtMoney(s.avgPerLb)}/lb avg</div>
            </div>
          </div>
        ))}
      </div>
    );
  }

  return (
    <div>
      <div style={styles.sectionLabel}>EXECUTIVE OVERVIEW</div>
      <div style={styles.summaryRow}>
        <SummaryCard label="Total On Hand" value={fmtWeight(totalOnHand, unit)} accent="#EDE8D8" />
        <SummaryCard label="Total Revenue (all time)" value={fmtMoney(totalRevenue)} accent="#C9A24B" />
        <SummaryCard label="Revenue This Month" value={fmtMoney(revenueThisMonth)} accent="#8FAF8B" />
        <SummaryCard label="Unpaid Balance" value={fmtMoney(unpaidTotal)} accent="#B9603F" />
        <SummaryCard label="Orders This Week" value={String(ordersThisWeek)} accent="#EDE8D8" />
        <SummaryCard label="Strains Low on Stock" value={String(lowStockGroups.length)} accent="#B9603F" />
      </div>

      <div style={styles.sectionLabel}>REVENUE THIS MONTH BY FACILITY</div>
      <div style={styles.summaryRow}>
        <SummaryCard label={`${SOURCE_META.daddyspipes.label} — This Month`} value={fmtMoney(revBySource.mtd.daddyspipes)} accent={SOURCE_META.daddyspipes.color} />
        <SummaryCard label={`${SOURCE_META.merc.label} — This Month`} value={fmtMoney(revBySource.mtd.merc)} accent={SOURCE_META.merc.color} />
        <SummaryCard label={`${SOURCE_META.daddyspipes.label} — All Time`} value={fmtMoney(revBySource.all.daddyspipes)} accent={SOURCE_META.daddyspipes.color} />
        <SummaryCard label={`${SOURCE_META.merc.label} — All Time`} value={fmtMoney(revBySource.all.merc)} accent={SOURCE_META.merc.color} />
      </div>

      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        <div style={styles.sectionLabel}>CASH ON HAND</div>
        {!editingCash && <button type="button" style={styles.iconLabelBtn} onClick={startEditCash}><Edit3 size={12} /> Update</button>}
      </div>
      {editingCash ? (
        <div style={styles.form}>
          <div style={styles.twoCol}>
            <div><FieldLabel>{SOURCE_META.daddyspipes.label}</FieldLabel><input style={styles.input} type="number" step="0.01" value={cashForm.daddyspipes} onChange={(e) => setCashForm({ ...cashForm, daddyspipes: e.target.value })} /></div>
            <div><FieldLabel>{SOURCE_META.merc.label}</FieldLabel><input style={styles.input} type="number" step="0.01" value={cashForm.merc} onChange={(e) => setCashForm({ ...cashForm, merc: e.target.value })} /></div>
          </div>
          <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
            <button type="button" style={styles.submitBtn} onClick={saveCash}>Save</button>
            <button type="button" style={styles.backBtn} onClick={() => setEditingCash(false)}>Cancel</button>
          </div>
        </div>
      ) : (
        <div style={styles.summaryRow}>
          <SummaryCard label="Total Cash on Hand" value={fmtMoney(cashTotal)} accent="#C9A24B" />
          <SummaryCard label={`${SOURCE_META.daddyspipes.label} Cash`} value={fmtMoney(cash.daddyspipes || 0)} accent={SOURCE_META.daddyspipes.color} />
          <SummaryCard label={`${SOURCE_META.merc.label} Cash`} value={fmtMoney(cash.merc || 0)} accent={SOURCE_META.merc.color} />
        </div>
      )}

      <div style={styles.sectionLabel}>INVENTORY VALUATION</div>
      <div style={styles.summaryRow}>
        <SummaryCard label="Total Inventory Value" value={fmtMoney(inventoryValue)} accent="#C9A24B" />
        <SummaryCard label={`${SOURCE_META.daddyspipes.label} Value`} value={fmtMoney(valueBySource.daddyspipes)} accent={SOURCE_META.daddyspipes.color} />
        <SummaryCard label={`${SOURCE_META.merc.label} Value`} value={fmtMoney(valueBySource.merc)} accent={SOURCE_META.merc.color} />
      </div>
      {unpricedWeight > 0 && (
        <div style={styles.noticeBox}>{fmtWeight(unpricedWeight, unit)} on hand doesn't have a price set anywhere (standard, strain, or batch) — it's counted as $0 above. Set prices under Admin → Pricing for a more accurate valuation.</div>
      )}

      <div style={styles.sectionLabel}>TOP STRAINS — THIS MONTH</div>
      <TopStrainList list={topStrainsMonth} />

      <div style={styles.sectionLabel}>TOP STRAINS — ALL TIME</div>
      <TopStrainList list={topStrainsAll} />

      {lowStockGroups.length > 0 && (
        <>
          <div style={styles.sectionLabel}>LOW STOCK</div>
          <div style={styles.strainGrid}>
            {lowStockGroups.map((g) => (
              <div key={g.key} style={styles.orderRow}>
                <div style={styles.strainName}><AlertTriangle size={13} color="#B9603F" style={{ marginRight: 6, verticalAlign: "middle" }} />{g.key}</div>
                <span style={styles.readout}>{fmtWeight(g.total, unit)}</span>
              </div>
            ))}
          </div>
        </>
      )}
    </div>
  );
}


function AdminTab({ data, currentProfile, isAdmin, isManager, batches, visibleSources, onUpdateRooms, onUpdateStrains, onUpdateDistributionEmail, onUpdateShipper, onUpdatePaymentInfo, onUpdatePriceList, onUpdatePriceOverrides, onUpdatePhotos, onUpdateMenuPassword, onUpdateCustomers, onUpdateGradeDescriptions, onUpdateCustomGrades, onRestoreBackup, onUpdateNextOrderNumber, onUpdateLicenses }) {
  const sections = [
    ...(isAdmin ? [{ k: "team", label: "Team" }] : []),
    { k: "catalog", label: "Rooms & Strains" },
    { k: "customers", label: "Customers" },
    { k: "pricing", label: "Pricing" },
    { k: "grades", label: "Grades" },
    ...(isAdmin || isManager ? [{ k: "settings", label: "Settings" }] : []),
    ...(isAdmin ? [{ k: "backups", label: "Backups" }] : []),
  ];
  const [section, setSection] = useState(sections[0].k);
  return (
    <div>
      {!isAdmin && (
        <div style={styles.noticeBox}>
          {isManager ? "Managers have full access here except adding or removing team accounts, and restoring backups — those stay with admins." : "You've been granted extra access to this area by an admin."}
        </div>
      )}
      <div style={styles.groupToggle}>
        {sections.map((opt) => (
          <button key={opt.k} onClick={() => setSection(opt.k)} style={{ ...styles.groupBtn, background: section === opt.k ? "#C9A24B" : "transparent", color: section === opt.k ? "#12160F" : "#B9BFA9" }}>{opt.label}</button>
        ))}
      </div>
      <div style={{ marginTop: 16 }}>
        {section === "team" && isAdmin && <TeamManager currentProfile={currentProfile} />}
        {section === "catalog" && <CatalogManager rooms={data.rooms} strains={data.strains} photos={data.photos} onUpdateRooms={onUpdateRooms} onUpdateStrains={onUpdateStrains} onUpdatePhotos={onUpdatePhotos} visibleSources={visibleSources} />}
        {section === "customers" && <CustomerManager customers={data.customers} onUpdateCustomers={onUpdateCustomers} />}
        {section === "pricing" && <PricingManager priceList={data.priceList} priceOverrides={data.priceOverrides} strains={data.strains} batches={batches} customGrades={data.customGrades} onUpdatePriceList={onUpdatePriceList} onUpdatePriceOverrides={onUpdatePriceOverrides} visibleSources={visibleSources} />}
        {section === "grades" && <GradeManager gradeDescriptions={data.gradeDescriptions} customGrades={data.customGrades} onUpdateGradeDescriptions={onUpdateGradeDescriptions} onUpdateCustomGrades={onUpdateCustomGrades} />}
        {section === "settings" && <SettingsManager distributionEmail={data.distributionEmail} shipper={data.shipper} paymentInfo={data.paymentInfo} menuPassword={data.menuPassword} nextOrderNumber={data.nextOrderNumber || data.orders.length + 1} licenses={data.licenses} onUpdateDistributionEmail={onUpdateDistributionEmail} onUpdateShipper={onUpdateShipper} onUpdatePaymentInfo={onUpdatePaymentInfo} onUpdateMenuPassword={onUpdateMenuPassword} onUpdateNextOrderNumber={onUpdateNextOrderNumber} onUpdateLicenses={onUpdateLicenses} />}
        {section === "backups" && isAdmin && <BackupManager backups={data.backups} fullData={data} onRestore={onRestoreBackup} />}
      </div>
    </div>
  );
}


const ROLE_LABELS = { admin: "Admin", manager: "Manager", sales: "Sales Rep", trimmer: "Trimming Manager" };

function TeamManager({ currentProfile }) {
  const [profiles, setProfiles] = useState(null);
  const [savingId, setSavingId] = useState(null);

  useEffect(() => { loadProfiles(); }, []);
  async function loadProfiles() {
    const { data } = await supabase.from("profiles").select("*").order("created_at", { ascending: true });
    setProfiles(data || []);
  }

  async function updateProfile(id, patch) {
    setSavingId(id);
    await supabase.from("profiles").update(patch).eq("id", id);
    await loadProfiles();
    setSavingId(null);
  }

  return (
    <div>
      <div style={styles.noticeBox}>
        There's no way to create another person's login from inside the app (that requires server-side admin access Supabase doesn't expose to the browser). Instead, share this site's URL and have them sign up with their own email + password — they'll show up here as a Sales Rep, and you can promote them or grant extra permissions below.
      </div>
      {profiles === null && <div style={styles.emptyState}>Loading team…</div>}
      <div style={styles.strainGrid}>
        {(profiles || []).map((u) => (
          <div key={u.id} style={{ ...styles.orderRow, alignItems: "flex-start" }}>
            <div style={{ flex: 1 }}>
              <div style={styles.strainName}>{u.display_name} {u.id === currentProfile.id && "(you)"}</div>
              <div style={styles.historyMeta}>{u.email}</div>
              <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8, marginTop: 8 }}>
                {["admin", "manager", "sales", "trimmer"].map((r) => (
                  <button key={r} disabled={savingId === u.id} onClick={() => updateProfile(u.id, { role: r })}
                    style={{ ...styles.sourceChoiceBtn, borderColor: u.role === r ? "#C9A24B" : "#2A3324", background: u.role === r ? "rgba(201,162,75,0.10)" : "transparent", color: u.role === r ? "#C9A24B" : "#B9BFA9" }}>
                    {ROLE_LABELS[r]}
                  </button>
                ))}
              </div>
              {u.role === "sales" && (
                <div style={{ marginTop: 10 }}>
                  {PERMISSION_DEFS.map((p) => (
                    <label key={p.key} style={styles.bestCheckbox}>
                      <input type="checkbox" checked={!!(u.permissions && u.permissions[p.key])} disabled={savingId === u.id}
                        onChange={(e) => updateProfile(u.id, { permissions: { ...u.permissions, [p.key]: e.target.checked } })} />
                      {p.label}
                    </label>
                  ))}
                </div>
              )}
              {u.role !== "admin" && (
                <div style={{ marginTop: 10 }}>
                  <div style={{ fontSize: 11, color: "#7C8571", marginBottom: 6 }}>Facility Access</div>
                  <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
                    {["both", "daddyspipes", "merc"].map((f) => {
                      const current = (u.permissions && u.permissions.facilityAccess) || "both";
                      return (
                        <button key={f} disabled={savingId === u.id} onClick={() => updateProfile(u.id, { permissions: { ...u.permissions, facilityAccess: f } })}
                          style={{ ...styles.sourceChoiceBtn, borderColor: current === f ? "#C9A24B" : "#2A3324", background: current === f ? "rgba(201,162,75,0.10)" : "transparent", color: current === f ? "#C9A24B" : "#B9BFA9" }}>
                          {f === "both" ? "Both Facilities" : `${SOURCE_META[f].label} Only`}
                        </button>
                      );
                    })}
                  </div>
                  <div style={{ fontSize: 11, color: "#6B7264", marginTop: 6 }}>Limiting someone to one facility hides the other facility's inventory, orders, and numbers everywhere in the app — only Admins can see both.</div>
                </div>
              )}
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}

function CatalogManager({ rooms, strains, photos, onUpdateRooms, onUpdateStrains, onUpdatePhotos, visibleSources }) {
  const [newRoom, setNewRoom] = useState({ daddyspipes: "", merc: "" });
  const [newStrain, setNewStrain] = useState("");
  const [uploadingFor, setUploadingFor] = useState(null);

  function addRoom(source) {
    const v = newRoom[source].trim();
    if (!v || rooms[source].includes(v)) return;
    onUpdateRooms({ ...rooms, [source]: [...rooms[source], v] });
    setNewRoom({ ...newRoom, [source]: "" });
  }
  function removeRoom(source, r) { onUpdateRooms({ ...rooms, [source]: rooms[source].filter((x) => x !== r) }); }
  function addStrain() {
    const v = newStrain.trim();
    if (!v || strains.some((s) => s.toLowerCase() === v.toLowerCase())) return;
    onUpdateStrains([...strains, v].sort());
    setNewStrain("");
  }
  function removeStrain(s) {
    onUpdateStrains(strains.filter((x) => x !== s));
    if (photos.byStrain[s]) { const next = { ...photos.byStrain }; delete next[s]; onUpdatePhotos({ ...photos, byStrain: next }); }
  }
  async function handleStrainPhoto(strain, file) {
    if (!file) return;
    setUploadingFor("strain:" + strain);
    try {
      const dataUri = await compressImage(file);
      onUpdatePhotos({ ...photos, byStrain: { ...photos.byStrain, [strain]: dataUri } });
    } catch {} finally { setUploadingFor(null); }
  }
  function removeStrainPhoto(strain) { const next = { ...photos.byStrain }; delete next[strain]; onUpdatePhotos({ ...photos, byStrain: next }); }
  async function handleSourcePhoto(source, file) {
    if (!file) return;
    setUploadingFor("source:" + source);
    try {
      const dataUri = await compressImage(file);
      onUpdatePhotos({ ...photos, bySource: { ...photos.bySource, [source]: dataUri } });
    } catch {} finally { setUploadingFor(null); }
  }
  function removeSourcePhoto(source) { onUpdatePhotos({ ...photos, bySource: { ...photos.bySource, [source]: "" } }); }

  return (
    <div>
      {["daddyspipes", "merc"].filter((s) => !visibleSources || visibleSources.includes(s)).map((source) => (
        <div key={source} style={{ ...styles.form, marginBottom: 16 }}>
          <div style={styles.formHeader}><Warehouse size={18} color={SOURCE_META[source].color} /><span>{SOURCE_META[source].label} Rooms</span></div>
          <div style={styles.strainSourceTags}>
            {rooms[source].map((r) => (
              <span key={r} style={styles.catalogChip}>{r}<button style={styles.chipRemove} onClick={() => removeRoom(source, r)}><X size={11} /></button></span>
            ))}
          </div>
          <div style={{ display: "flex", gap: 8, marginTop: 12 }}>
            <input style={styles.input} value={newRoom[source]} onChange={(e) => setNewRoom({ ...newRoom, [source]: e.target.value })} placeholder="Add a room…" />
            <button type="button" style={{ ...styles.submitBtn, marginTop: 0, whiteSpace: "nowrap" }} onClick={() => addRoom(source)}>Add</button>
          </div>
          <div style={{ borderTop: "1px solid #232B1D", marginTop: 14, paddingTop: 12, display: "flex", alignItems: "center", gap: 10 }}>
            <div style={styles.strainPhotoThumb}>
              {photos.bySource[source] ? <img src={photos.bySource[source]} style={{ width: "100%", height: "100%", objectFit: "cover" }} /> : <ImageIcon size={16} color="#4B5240" />}
            </div>
            <div>
              <div style={{ fontSize: 12, color: "#8C9483", marginBottom: 2 }}>Default photo for {SOURCE_META[source].label} (used on the menu when a strain or batch doesn't have its own)</div>
              <label style={styles.smallLinkLabel}>
                {uploadingFor === "source:" + source ? "Uploading…" : photos.bySource[source] ? "Replace photo" : "Add photo"}
                <input type="file" accept="image/*" style={{ display: "none" }} onChange={(e) => handleSourcePhoto(source, e.target.files[0])} />
              </label>
              {photos.bySource[source] && <button type="button" style={{ ...styles.smallLinkLabel, color: "#B9603F", marginLeft: 10 }} onClick={() => removeSourcePhoto(source)}>Remove</button>}
            </div>
          </div>
        </div>
      ))}
      <div style={styles.form}>
        <div style={styles.formHeader}><Tag size={18} color="#C9A24B" /><span>Strain List &amp; Photos</span></div>
        <div style={{ fontSize: 12, color: "#8C9483", marginBottom: 10 }}>Strains show up as a dropdown for staff, with the option to type a new name if it's not listed. A strain photo here shows on the menu unless a specific batch has its own photo (add those from the Inventory tab).</div>
        {strains.length === 0 && <div style={{ fontSize: 12.5, color: "#8C9483" }}>No saved strains yet — they'll appear here as staff log them in.</div>}
        <div style={styles.strainPhotoGrid}>
          {strains.map((s) => (
            <div key={s} style={styles.strainPhotoCard}>
              <div style={styles.strainPhotoThumb}>
                {photos.byStrain[s] ? <img src={photos.byStrain[s]} style={{ width: "100%", height: "100%", objectFit: "cover" }} /> : <ImageIcon size={16} color="#4B5240" />}
              </div>
              <div style={{ flex: 1, minWidth: 0 }}>
                <div style={{ fontSize: 12.5, color: "#D8D3C2", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{s}</div>
                <label style={styles.smallLinkLabel}>
                  {uploadingFor === "strain:" + s ? "Uploading…" : photos.byStrain[s] ? "Replace photo" : "Add photo"}
                  <input type="file" accept="image/*" style={{ display: "none" }} onChange={(e) => handleStrainPhoto(s, e.target.files[0])} />
                </label>
                {photos.byStrain[s] && <button type="button" style={{ ...styles.smallLinkLabel, color: "#B9603F", marginLeft: 10 }} onClick={() => removeStrainPhoto(s)}>Remove photo</button>}
              </div>
              <button style={styles.chipRemove} onClick={() => removeStrain(s)}><X size={11} /></button>
            </div>
          ))}
        </div>
        <div style={{ display: "flex", gap: 8, marginTop: 12 }}>
          <input style={styles.input} value={newStrain} onChange={(e) => setNewStrain(e.target.value)} placeholder="Add a strain…" />
          <button type="button" style={{ ...styles.submitBtn, marginTop: 0, whiteSpace: "nowrap" }} onClick={addStrain}>Add</button>
        </div>
      </div>
      <div style={styles.noticeBox}>Want a photo on one specific batch (like an especially nice harvest)? Add that from the Inventory tab — expand any strain card and tap the small photo icon next to the batch.</div>
    </div>
  );
}


function CustomerManager({ customers, onUpdateCustomers }) {
  const [name, setName] = useState("");
  const [license, setLicense] = useState("");
  const [address, setAddress] = useState("");
  const [editingId, setEditingId] = useState(null);
  const [editValues, setEditValues] = useState({});

  function addCustomer() {
    if (!name.trim()) return;
    onUpdateCustomers([...customers, { id: uid(), name: name.trim(), license: license.trim(), address: address.trim(), createdAt: Date.now() }]);
    setName(""); setLicense(""); setAddress("");
  }
  function startEdit(c) { setEditingId(c.id); setEditValues({ name: c.name, license: c.license || "", address: c.address || "" }); }
  function saveEdit(id) {
    onUpdateCustomers(customers.map((c) => (c.id === id ? { ...c, ...editValues } : c)));
    setEditingId(null);
  }
  function removeCustomer(id) { onUpdateCustomers(customers.filter((c) => c.id !== id)); }

  return (
    <div>
      <div style={styles.form}>
        <div style={styles.formHeader}><Users size={18} color="#C9A24B" /><span>Add Customer</span></div>
        <FieldLabel>Business name</FieldLabel>
        <input style={styles.input} value={name} onChange={(e) => setName(e.target.value)} />
        <FieldLabel>State License #</FieldLabel>
        <input style={styles.input} value={license} onChange={(e) => setLicense(e.target.value)} />
        <FieldLabel>Address (optional)</FieldLabel>
        <input style={styles.input} value={address} onChange={(e) => setAddress(e.target.value)} />
        <button style={styles.submitBtn} onClick={addCustomer}><Plus size={16} /> Add Customer</button>
      </div>

      <div style={styles.sectionLabel}>SAVED CUSTOMERS</div>
      {customers.length === 0 && <div style={styles.emptyState}>No customers saved yet — they're also added automatically the first time you use their name on an order.</div>}
      <div style={styles.strainGrid}>
        {customers.map((c) => (
          <div key={c.id} style={{ ...styles.orderRow, alignItems: "flex-start" }}>
            {editingId === c.id ? (
              <div style={{ flex: 1 }}>
                <input style={styles.input} value={editValues.name} onChange={(e) => setEditValues({ ...editValues, name: e.target.value })} placeholder="Name" />
                <input style={{ ...styles.input, marginTop: 8 }} value={editValues.license} onChange={(e) => setEditValues({ ...editValues, license: e.target.value })} placeholder="License #" />
                <input style={{ ...styles.input, marginTop: 8 }} value={editValues.address} onChange={(e) => setEditValues({ ...editValues, address: e.target.value })} placeholder="Address" />
                <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
                  <button style={styles.submitBtn} onClick={() => saveEdit(c.id)}>Save</button>
                  <button style={styles.backBtn} onClick={() => setEditingId(null)}>Cancel</button>
                </div>
              </div>
            ) : (
              <>
                <div>
                  <div style={styles.strainName}>{c.name}</div>
                  <div style={styles.historyMeta}>{c.license ? `License: ${c.license}` : "No license on file"}{c.address ? ` · ${c.address}` : ""}</div>
                </div>
                <div style={{ display: "flex", gap: 8 }}>
                  <button style={styles.iconLabelBtn} onClick={() => startEdit(c)}>Edit</button>
                  <button style={styles.iconBtnDanger} onClick={() => { if (confirm(`Remove ${c.name}?`)) removeCustomer(c.id); }}><Trash2 size={14} /></button>
                </div>
              </>
            )}
          </div>
        ))}
      </div>
    </div>
  );
}

function PricingManager({ priceList, priceOverrides, strains, batches, customGrades, onUpdatePriceList, onUpdatePriceOverrides, visibleSources }) {
  const grades = allGradeCodes(customGrades);
  const emptyGradeMap = () => Object.fromEntries(grades.map((g) => [g, ""]));

  const [localStandard, setLocalStandard] = useState(priceList.standard);
  const [strainPick, setStrainPick] = useState("");
  const [strainGrades, setStrainGrades] = useState(emptyGradeMap());

  const [batchStrain, setBatchStrain] = useState("");
  const [batchGrade, setBatchGrade] = useState("");
  const [batchPick, setBatchPick] = useState("");
  const [batchPrice, setBatchPrice] = useState("");

  function setStandardPrice(source, grade, value) {
    setLocalStandard({ ...localStandard, [source]: { ...localStandard[source], [grade]: value } });
  }
  function saveStandard() {
    const cleaned = {};
    ["daddyspipes", "merc"].forEach((source) => {
      cleaned[source] = {};
      grades.forEach((g) => { cleaned[source][g] = parseFloat((localStandard[source] || {})[g]) || 0; });
    });
    onUpdatePriceList({ ...priceList, standard: cleaned });
  }

  function pickStrainForEdit(strain) {
    setStrainPick(strain);
    const existing = priceList.byStrain[strain];
    setStrainGrades(existing ? Object.fromEntries(grades.map((g) => [g, existing[g] || ""])) : emptyGradeMap());
  }
  function saveStrainPrices() {
    if (!strainPick) return;
    const cleaned = {};
    grades.forEach((g) => { cleaned[g] = parseFloat(strainGrades[g]) || 0; });
    onUpdatePriceList({ ...priceList, byStrain: { ...priceList.byStrain, [strainPick]: cleaned } });
    setStrainPick(""); setStrainGrades(emptyGradeMap());
  }
  function removeStrainPrices(strain) {
    const next = { ...priceList.byStrain };
    delete next[strain];
    onUpdatePriceList({ ...priceList, byStrain: next });
  }

  const batchesForPick = useMemo(() => {
    if (!batchStrain || !batchGrade) return [];
    return batches.filter((b) => b.strain === batchStrain && b.grade === batchGrade);
  }, [batches, batchStrain, batchGrade]);

  function addBatchOverride() {
    if (!batchPick || !batchPrice) return;
    onUpdatePriceOverrides({ ...priceOverrides, byBatch: { ...priceOverrides.byBatch, [batchPick]: parseFloat(batchPrice) || 0 } });
    setBatchStrain(""); setBatchGrade(""); setBatchPick(""); setBatchPrice("");
  }
  function removeBatchOverride(batchId) {
    const next = { ...priceOverrides.byBatch };
    delete next[batchId];
    onUpdatePriceOverrides({ ...priceOverrides, byBatch: next });
  }
  function batchLabel(id) {
    const b = batches.find((x) => x.id === id);
    return b ? `${b.strain} — Grade ${b.grade} — ${SOURCE_META[b.source].label} ${b.room}${b.lot ? ` — Batch ${b.lot}` : ""}` : id;
  }

  return (
    <div>
      <div style={{ ...styles.form, marginBottom: 16 }}>
        <div style={styles.formHeader}><DollarSign size={18} color="#C9A24B" /><span>Standard Prices — by Source &amp; Grade ($/lb)</span></div>
        <div style={{ fontSize: 12.5, color: "#8C9483", marginBottom: 14 }}>The base default, by location grown. Used whenever a strain or batch below doesn't have its own price set.</div>
        {["daddyspipes", "merc"].filter((s) => !visibleSources || visibleSources.includes(s)).map((source) => (
          <div key={source} style={{ marginBottom: 18 }}>
            <div style={{ fontSize: 13, fontWeight: 600, color: SOURCE_META[source].color, marginBottom: 8 }}>{SOURCE_META[source].label}</div>
            <div style={{ display: "grid", gridTemplateColumns: `repeat(${grades.length}, 1fr)`, gap: 8 }}>
              {grades.map((g) => (
                <div key={g}>
                  <FieldLabel>Grade {g}</FieldLabel>
                  <input style={styles.input} type="number" step="0.01" min="0" value={(localStandard[source] || {})[g] || ""} onChange={(e) => setStandardPrice(source, g, e.target.value)} placeholder="0.00" />
                </div>
              ))}
            </div>
          </div>
        ))}
        <button style={styles.submitBtn} onClick={saveStandard}>Save Standard Prices</button>
      </div>

      <div style={{ ...styles.form, marginBottom: 16 }}>
        <div style={styles.formHeader}><Tag size={18} color="#C9A24B" /><span>Prices by Strain &amp; Grade</span></div>
        <div style={{ fontSize: 12.5, color: "#8C9483", marginBottom: 10 }}>Set prices for a specific strain across all its grades — regardless of source. Beats the standard price, loses only to a batch-specific override below.</div>
        <FieldLabel>Strain</FieldLabel>
        <select style={styles.input} value={strainPick} onChange={(e) => pickStrainForEdit(e.target.value)}>
          <option value="">Select a strain…</option>
          {strains.map((s) => <option key={s} value={s}>{s}</option>)}
        </select>
        {strainPick && (
          <>
            <div style={{ display: "grid", gridTemplateColumns: `repeat(${grades.length}, 1fr)`, gap: 8, marginTop: 10 }}>
              {grades.map((g) => (
                <div key={g}>
                  <FieldLabel>Grade {g}</FieldLabel>
                  <input style={styles.input} type="number" step="0.01" min="0" value={strainGrades[g] || ""} onChange={(e) => setStrainGrades({ ...strainGrades, [g]: e.target.value })} placeholder="0.00" />
                </div>
              ))}
            </div>
            <button type="button" style={{ ...styles.submitBtn, background: "#7C9473" }} onClick={saveStrainPrices}>Save Prices for {strainPick}</button>
          </>
        )}
        {Object.keys(priceList.byStrain).length > 0 && (
          <div style={styles.lineItemList}>
            {Object.entries(priceList.byStrain).map(([strain, gradePrices]) => (
              <div key={strain} style={styles.lineItemRow}>
                <div>
                  <div style={styles.lineItemStrain}>{strain}</div>
                  <div style={styles.lineItemMeta}>{grades.map((g) => `${g}: ${fmtMoney(gradePrices[g] || 0)}`).join(" · ")}</div>
                </div>
                <div style={styles.lineItemRight}>
                  <button type="button" onClick={() => pickStrainForEdit(strain)} style={styles.iconLabelBtn}>Edit</button>
                  <button type="button" onClick={() => removeStrainPrices(strain)} style={styles.iconBtn}><X size={14} /></button>
                </div>
              </div>
            ))}
          </div>
        )}
      </div>

      <div style={styles.form}>
        <div style={styles.formHeader}><ClipboardList size={18} color="#C9A24B" /><span>Batch Price Override</span></div>
        <div style={{ fontSize: 12.5, color: "#8C9483", marginBottom: 10 }}>The most specific option — price one exact batch differently (e.g. an unusually good harvest). Pick the strain and grade first to narrow it down. This wins over every other price.</div>
        <div style={styles.twoCol}>
          <div>
            <FieldLabel>Strain</FieldLabel>
            <select style={styles.input} value={batchStrain} onChange={(e) => { setBatchStrain(e.target.value); setBatchGrade(""); setBatchPick(""); }}>
              <option value="">Select a strain…</option>
              {strains.map((s) => <option key={s} value={s}>{s}</option>)}
            </select>
          </div>
          <div>
            <FieldLabel>Grade</FieldLabel>
            <select style={styles.input} value={batchGrade} onChange={(e) => { setBatchGrade(e.target.value); setBatchPick(""); }} disabled={!batchStrain}>
              <option value="">Select a grade…</option>
              {grades.map((g) => <option key={g} value={g}>{g}</option>)}
            </select>
          </div>
        </div>
        {batchStrain && batchGrade && (
          <>
            <FieldLabel>Batch</FieldLabel>
            <select style={styles.input} value={batchPick} onChange={(e) => setBatchPick(e.target.value)}>
              <option value="">Select a batch…</option>
              {batchesForPick.map((b) => <option key={b.id} value={b.id}>{SOURCE_META[b.source].label} {b.room}{b.lot ? ` — Batch ${b.lot}` : ""}</option>)}
            </select>
            {batchesForPick.length === 0 && <div style={{ fontSize: 12, color: "#8C9483", marginTop: 6 }}>No batches on hand match that strain and grade.</div>}
          </>
        )}
        {batchPick && (
          <>
            <FieldLabel>Price per lb ($)</FieldLabel>
            <input style={styles.input} type="number" step="0.01" min="0" value={batchPrice} onChange={(e) => setBatchPrice(e.target.value)} placeholder="0.00" />
            <button type="button" style={{ ...styles.submitBtn, background: "#7C9473" }} onClick={addBatchOverride}><Plus size={16} /> Set Override</button>
          </>
        )}
        {Object.keys(priceOverrides.byBatch).length > 0 && (
          <div style={styles.lineItemList}>
            {Object.entries(priceOverrides.byBatch).map(([batchId, price]) => (
              <div key={batchId} style={styles.lineItemRow}>
                <span style={styles.lineItemStrain}>{batchLabel(batchId)}</span>
                <div style={styles.lineItemRight}>
                  <span style={styles.lineItemTotal}>{fmtMoney(price)}/lb</span>
                  <button type="button" onClick={() => removeBatchOverride(batchId)} style={styles.iconBtn}><X size={14} /></button>
                </div>
              </div>
            ))}
          </div>
        )}
      </div>
    </div>
  );
}

function GradeManager({ gradeDescriptions, customGrades, onUpdateGradeDescriptions, onUpdateCustomGrades }) {
  const [drafts, setDrafts] = useState({});
  const [newCode, setNewCode] = useState("");
  const [newLabel, setNewLabel] = useState("");
  const [newDesc, setNewDesc] = useState("");
  const [newColor, setNewColor] = useState("#8FAF8B");

  function draftFor(code) {
    return drafts[code] !== undefined ? drafts[code] : gradeDescriptionFor(code, gradeDescriptions, customGrades);
  }
  function saveBuiltIn(code) {
    onUpdateGradeDescriptions({ ...gradeDescriptions, [code]: draftFor(code) });
  }
  function addCustomGrade() {
    const code = newCode.trim().toUpperCase();
    if (!code) return;
    if (GRADE_ORDER.includes(code) || customGrades.some((g) => g.code === code)) return;
    onUpdateCustomGrades([...customGrades, { code, label: newLabel.trim() || code, description: newDesc.trim(), color: newColor }]);
    setNewCode(""); setNewLabel(""); setNewDesc(""); setNewColor("#8FAF8B");
  }
  function removeCustomGrade(code) { onUpdateCustomGrades(customGrades.filter((g) => g.code !== code)); }

  return (
    <div>
      <div style={{ ...styles.form, marginBottom: 16 }}>
        <div style={styles.formHeader}><Edit3 size={18} color="#C9A24B" /><span>Built-in Grade Descriptions</span></div>
        <div style={{ fontSize: 12.5, color: "#8C9483", marginBottom: 12 }}>Shown as a helper wherever grades are picked (Intake, Trim Log, Pricing).</div>
        {GRADE_ORDER.map((code) => (
          <div key={code} style={{ marginBottom: 12 }}>
            <FieldLabel><GradeBadge grade={code} isBest={false} /> Grade {code}</FieldLabel>
            <div style={{ display: "flex", gap: 8 }}>
              <input style={styles.input} value={draftFor(code)} onChange={(e) => setDrafts({ ...drafts, [code]: e.target.value })} />
              <button type="button" style={{ ...styles.submitBtn, marginTop: 0, whiteSpace: "nowrap" }} onClick={() => saveBuiltIn(code)}>Save</button>
            </div>
          </div>
        ))}
      </div>
      <div style={styles.form}>
        <div style={styles.formHeader}><Plus size={18} color="#C9A24B" /><span>Custom Grades</span></div>
        <div style={{ fontSize: 12.5, color: "#8C9483", marginBottom: 10 }}>Add a grade code beyond A–D–T (e.g. a pre-roll or shake sub-grade). It'll show up wherever grades are picked and priced.</div>
        {customGrades.length > 0 && (
          <div style={styles.strainSourceTags}>
            {customGrades.map((g) => (
              <span key={g.code} style={{ ...styles.catalogChip, borderColor: g.color }}>
                <GradeBadge grade={g.code} isBest={false} /> {g.label}
                <button style={styles.chipRemove} onClick={() => removeCustomGrade(g.code)}><X size={11} /></button>
              </span>
            ))}
          </div>
        )}
        <div style={styles.twoCol}>
          <div><FieldLabel>Code (1–2 letters)</FieldLabel><input style={styles.input} value={newCode} onChange={(e) => setNewCode(e.target.value)} maxLength={2} placeholder="e.g. P" /></div>
          <div><FieldLabel>Label</FieldLabel><input style={styles.input} value={newLabel} onChange={(e) => setNewLabel(e.target.value)} placeholder="e.g. Pre-Roll" /></div>
        </div>
        <FieldLabel>Description</FieldLabel>
        <input style={styles.input} value={newDesc} onChange={(e) => setNewDesc(e.target.value)} placeholder="What this grade means" />
        <FieldLabel>Badge color</FieldLabel>
        <input style={{ ...styles.input, height: 38, padding: 4 }} type="color" value={newColor} onChange={(e) => setNewColor(e.target.value)} />
        <button style={styles.submitBtn} onClick={addCustomGrade}><Plus size={16} /> Add Grade</button>
      </div>
    </div>
  );
}

function downloadJSON(filename, obj) {
  const blob = new Blob([JSON.stringify(obj, null, 2)], { type: "application/json" });
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url; a.download = filename.endsWith(".json") ? filename : `${filename}.json`;
  document.body.appendChild(a); a.click(); document.body.removeChild(a);
  setTimeout(() => URL.revokeObjectURL(url), 2000);
}

function BackupManager({ backups, fullData, onRestore }) {
  const dates = Object.keys(backups).sort((a, b) => (a < b ? 1 : -1));
  return (
    <div>
      <div style={{ ...styles.form, marginBottom: 16 }}>
        <div style={styles.formHeader}><Download size={18} color="#C9A24B" /><span>Download a Backup Now</span></div>
        <div style={{ fontSize: 12.5, color: "#8C9483", marginBottom: 14 }}>
          The automatic daily snapshot below lives in the same database as everything else, which means it isn't a true independent safety net. This downloads a real copy to your own computer right now — worth doing regularly, especially before any big change.
        </div>
        <button style={styles.submitBtn} onClick={() => downloadJSON(`stem-backup-${todayISO()}`, snapshotForBackup(fullData))}><Download size={16} /> Download Backup File</button>
      </div>
      <div style={styles.form}>
        <div style={styles.formHeader}><RotateCcw size={18} color="#C9A24B" /><span>Daily Backups (in Supabase)</span></div>
        <div style={{ fontSize: 12.5, color: "#8C9483", marginBottom: 14 }}>
          A snapshot saves automatically the first time anyone opens The Stem each day — inventory, orders, customers, rooms, strains, pricing, and grade settings. Team accounts and photos aren't included. Restoring replaces current data with that day's snapshot and can't be undone — worth being sure before you click it.
        </div>
        {dates.length === 0 && <div style={styles.emptyState}>No backups yet — one will be created automatically the next time The Stem is opened on a new day.</div>}
        <div style={styles.strainGrid}>
          {dates.map((d) => (
            <div key={d} style={styles.orderRow}>
              <div style={styles.strainName}>{d}</div>
              <div style={{ display: "flex", gap: 8 }}>
                <button style={styles.iconLabelBtn} onClick={() => downloadJSON(`stem-backup-${d}`, backups[d])}><Download size={13} /> Download</button>
                <button
                  style={styles.iconLabelBtn}
                  onClick={() => { if (confirm(`Restore everything to how it was on ${d}? This overwrites current inventory, orders, customers, and settings, and can't be undone.`)) onRestore(d); }}
                >
                  <RotateCcw size={13} /> Restore
                </button>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}

function SettingsManager({ distributionEmail, shipper, paymentInfo, menuPassword, nextOrderNumber, licenses, onUpdateDistributionEmail, onUpdateShipper, onUpdatePaymentInfo, onUpdateMenuPassword, onUpdateNextOrderNumber, onUpdateLicenses }) {
  const [email, setEmail] = useState(distributionEmail || "");
  const [ship, setShip] = useState(shipper || { name: "", address: "", license: "", contact: "" });
  const [payment, setPayment] = useState(paymentInfo || { instructions: "", terms: "" });
  const [password, setPassword] = useState(menuPassword || "");
  const [orderNum, setOrderNum] = useState(String(nextOrderNumber));
  const [lic, setLic] = useState(licenses || { daddyspipes: "", merc: "" });
  return (
    <div>
      <div style={{ ...styles.form, marginBottom: 16 }}>
        <div style={styles.formHeader}><Settings size={18} color="#C9A24B" /><span>Distribution Email</span></div>
        <div style={{ fontSize: 12.5, color: "#8C9483", marginBottom: 10 }}>When a sales rep hits "Email to Distribution" on an invoice, their mail app will pre-fill this address as the recipient.</div>
        <FieldLabel>Email address</FieldLabel>
        <input style={styles.input} type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="distribution@yourcompany.com" />
        <button style={styles.submitBtn} onClick={() => onUpdateDistributionEmail(email.trim())}>Save</button>
      </div>
      <div style={{ ...styles.form, marginBottom: 16 }}>
        <div style={styles.formHeader}><ShieldCheck size={18} color="#C9A24B" /><span>Facility State License Numbers</span></div>
        <div style={{ fontSize: 12.5, color: "#8C9483", marginBottom: 10 }}>Used for compliance on Trim and Bucked Flower logs — separate from the shipper license shown on invoices below.</div>
        <FieldLabel>Daddy's Pipes License #</FieldLabel>
        <input style={styles.input} value={lic.daddyspipes} onChange={(e) => setLic({ ...lic, daddyspipes: e.target.value })} />
        <FieldLabel>Merc License #</FieldLabel>
        <input style={styles.input} value={lic.merc} onChange={(e) => setLic({ ...lic, merc: e.target.value })} />
        <button style={styles.submitBtn} onClick={() => onUpdateLicenses({ daddyspipes: lic.daddyspipes.trim(), merc: lic.merc.trim() })}>Save</button>
      </div>
      <div style={{ ...styles.form, marginBottom: 16 }}>
        <div style={styles.formHeader}><FileText size={18} color="#C9A24B" /><span>Shipper Info (shown on invoices)</span></div>
        <FieldLabel>Business name</FieldLabel>
        <input style={styles.input} value={ship.name} onChange={(e) => setShip({ ...ship, name: e.target.value })} />
        <FieldLabel>Address</FieldLabel>
        <input style={styles.input} value={ship.address} onChange={(e) => setShip({ ...ship, address: e.target.value })} />
        <FieldLabel>METRC License #</FieldLabel>
        <input style={styles.input} value={ship.license} onChange={(e) => setShip({ ...ship, license: e.target.value })} />
        <FieldLabel>Phone / Email</FieldLabel>
        <input style={styles.input} value={ship.contact} onChange={(e) => setShip({ ...ship, contact: e.target.value })} />
        <button style={styles.submitBtn} onClick={() => onUpdateShipper({ ...ship, name: ship.name.trim(), address: ship.address.trim(), license: ship.license.trim(), contact: ship.contact.trim() })}>Save</button>
      </div>
      <div style={{ ...styles.form, marginBottom: 16 }}>
        <div style={styles.formHeader}><DollarSign size={18} color="#C9A24B" /><span>Payment Info (shown on invoices)</span></div>
        <FieldLabel>Payment Terms</FieldLabel>
        <input style={styles.input} value={payment.terms} onChange={(e) => setPayment({ ...payment, terms: e.target.value })} placeholder="e.g. Net 30, Due on receipt" />
        <FieldLabel>ACH / Payment Instructions</FieldLabel>
        <textarea style={{ ...styles.input, minHeight: 90 }} value={payment.instructions} onChange={(e) => setPayment({ ...payment, instructions: e.target.value })} placeholder="Bank name, routing #, account #, account name, or any other remittance instructions" />
        <button style={styles.submitBtn} onClick={() => onUpdatePaymentInfo({ terms: payment.terms.trim(), instructions: payment.instructions.trim() })}>Save</button>
      </div>
      <div style={{ ...styles.form, marginBottom: 16 }}>
        <div style={styles.formHeader}><ClipboardList size={18} color="#C9A24B" /><span>Order Numbering</span></div>
        <div style={{ fontSize: 12.5, color: "#8C9483", marginBottom: 10 }}>
          The next order gets this number (shown as ORD-####). Set it to continue from wherever your numbering already was, or to jump ahead if you need to.
        </div>
        <FieldLabel>Next order number</FieldLabel>
        <input style={styles.input} type="number" min="1" step="1" value={orderNum} onChange={(e) => setOrderNum(e.target.value)} />
        <button style={styles.submitBtn} onClick={() => { const n = parseInt(orderNum, 10); if (n > 0) onUpdateNextOrderNumber(n); }}>Save</button>
      </div>
      <div style={styles.form}>
        <div style={styles.formHeader}><LayoutGrid size={18} color="#C9A24B" /><span>Menu Password</span></div>
        <div style={{ fontSize: 12.5, color: "#8C9483", marginBottom: 10 }}>
          Right now the Menu is a print/download tool your team uses — it isn't published to a public web address yet, so nothing here is reachable outside your logged-in team. Setting a password now gets it ready for that upgrade later, when we wire up a public link.
        </div>
        <FieldLabel>Menu password</FieldLabel>
        <input style={styles.input} value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Set a password for the future public menu" />
        <button style={styles.submitBtn} onClick={() => onUpdateMenuPassword(password.trim())}>Save</button>
      </div>
    </div>
  );
}

// ---------- HISTORY ----------
function HistoryView({ batches, shipments, buckedLots, trimEntries, gradeEntries, wasteEntries, unit, canDelete, canEdit, rooms, customGrades, gradeDescriptions, onDeleteBatch, onDeleteShipment, onEditBatch, onEditAddition, onEditShipment, onEditBuckedLot, onEditBuckedDelivery, onDeleteBuckedLot, onEditGrade, onDeleteGrade, onEditWaste, onDeleteWaste }) {
  const [query, setQuery] = useState("");
  const [typeFilter, setTypeFilter] = useState("all");
  const [editingKey, setEditingKey] = useState(null);
  const [editForm, setEditForm] = useState(null);
  const [editError, setEditError] = useState("");
  const grades = allGradeCodes(customGrades);

  const events = useMemo(() => {
    const inEvents = batches.flatMap((b) => {
      const additions = (b.additions && b.additions.length > 0) ? b.additions : [{ grams: b.receivedGrams, date: b.dateReceived, staff: b.staff, notes: b.notes, manifestNumber: b.manifestNumber, packageTag: b.packageTag }];
      return additions.map((a, i) => ({
        id: `${b.id}-${i}`, refId: b.id, additionIndex: i, type: "in", strain: b.strain, source: b.source, room: b.room, lot: b.lot,
        grade: b.grade, isBest: b.isBest, isMachineTrimmed: b.isMachineTrimmed, trimmedAt: b.trimmedAt, batchRemaining: b.remainingGrams,
        grams: a.grams, date: a.date, staff: a.staff, detail: b.lot ? `Batch ${b.lot}` : "", notes: a.notes, manifestNumber: a.manifestNumber, packageTag: a.packageTag, multiAddition: additions.length > 1,
      }));
    });
    const outEvents = shipments.map((s) => ({ id: s.id, refId: s.id, type: "out", strain: s.strain, source: null, room: null, grade: null, isBest: false, grams: s.grams, date: s.date, staff: s.staff, detail: s.destination, notes: s.notes, manifestNumber: null, multiAddition: false }));
    const buckedEvents = (buckedLots || []).flatMap((l) => {
      const deliveries = l.deliveries || [];
      return deliveries.map((d, i) => ({
        id: `${l.id}-d${i}`, refId: l.id, deliveryIndex: i, type: l.type === "harvest" ? "harvest_in" : "bucked_in",
        strain: l.strain, source: l.source, room: l.room, lot: l.lot, grade: null, isBest: false, isMachineTrimmed: false, lotRemaining: l.remainingGrams,
        grams: d.grams, date: d.date, staff: d.staff, notes: d.notes,
        detail: l.lot ? (l.type === "harvest" ? `Harvest ${l.lot}` : `Batch ${l.lot}`) : "",
        manifestNumber: d.manifestNumber, packageTag: d.packageTag, multiAddition: deliveries.length > 1,
      }));
    });
    const trimEvents = (trimEntries || []).map((t) => {
      const lot = (buckedLots || []).find((l) => l.id === t.buckedLotId);
      return {
        id: t.id, refId: t.id, type: "trim",
        strain: lot ? lot.strain : "(deleted batch)", source: lot ? lot.source : null, room: lot ? lot.room : null,
        grade: null, isBest: false, isMachineTrimmed: false, lotRemaining: lot ? lot.remainingGrams : 0,
        grams: trimEntryTotal(t), date: t.date, staff: t.staff, notes: t.notes,
        detail: lot ? `Batch ${lot.lot}` : "", trimmerName: t.trimmerName,
      };
    });
    const gradeEvents = (gradeEntries || []).map((g) => {
      const lot = (buckedLots || []).find((l) => l.id === g.buckedLotId);
      return {
        id: g.id, refId: g.id, type: "grade",
        strain: lot ? lot.strain : "(deleted batch)", source: lot ? lot.source : null, room: lot ? lot.room : null,
        grade: null, isBest: false, isMachineTrimmed: false,
        grams: gradeEntryTotal(g), date: g.date, staff: g.staff, notes: g.notes,
        detail: lot ? `Batch ${lot.lot}` : "", gradeGrams: g.gradeGrams,
      };
    });
    const wasteEvents = (wasteEntries || []).map((w) => {
      const lot = (buckedLots || []).find((l) => l.id === w.buckedLotId);
      return {
        id: w.id, refId: w.id, type: "waste",
        strain: lot ? lot.strain : "(deleted batch)", source: lot ? lot.source : null, room: lot ? lot.room : null,
        grade: null, isBest: false, isMachineTrimmed: false, lotRemaining: lot ? lot.remainingGrams : 0,
        grams: w.grams, date: w.date, staff: w.staff, notes: w.notes,
        detail: lot ? `Batch ${lot.lot}` : "",
      };
    });
    return [...inEvents, ...outEvents, ...buckedEvents, ...trimEvents, ...gradeEvents, ...wasteEvents].sort((a, b) => (a.date < b.date ? 1 : -1));
  }, [batches, shipments, buckedLots, trimEntries, gradeEntries, wasteEntries]);

  const filtered = events.filter((e) => {
    if (typeFilter !== "all" && e.type !== typeFilter) return false;
    const q = query.trim().toLowerCase();
    if (!q) return true;
    return e.strain.toLowerCase().includes(q) || (e.staff || "").toLowerCase().includes(q) || (e.room || "").toLowerCase().includes(q)
      || (e.trimmerName || "").toLowerCase().includes(q) || (e.detail || "").toLowerCase().includes(q);
  });

  function startEditIn(e) {
    setEditingKey(`in-${e.id}`);
    setEditForm({
      source: e.source, room: e.room, strain: e.strain, lot: e.lot || "", grade: e.grade, isBest: e.isBest,
      isMachineTrimmed: !!e.isMachineTrimmed, trimmedAt: e.trimmedAt || "", grams: e.grams, date: e.date,
      manifestNumber: e.manifestNumber || "", packageTag: e.packageTag || "", notes: e.notes || "",
    });
    setEditError("");
  }
  function saveEditIn(e) {
    if (!editForm.strain.trim()) return setEditError("Strain name is required.");
    if (editForm.grams <= 0) return setEditError("Enter a weight greater than zero.");
    const floor = Math.max(0, e.grams - e.batchRemaining);
    if (editForm.grams < floor) return setEditError(`Can't go below ${fmtBoth(floor)} for this entry — that much of the batch has already shipped out.`);
    setEditError("");
    onEditBatch(e.refId, {
      source: editForm.source, room: editForm.room, strain: editForm.strain.trim(), lot: editForm.lot.trim(),
      grade: editForm.grade, isBest: editForm.grade === "A" ? editForm.isBest : false, isMachineTrimmed: editForm.isMachineTrimmed, trimmedAt: editForm.trimmedAt,
    });
    onEditAddition(e.refId, e.additionIndex, {
      grams: editForm.grams, date: editForm.date, notes: editForm.notes.trim(), manifestNumber: editForm.manifestNumber.trim() || undefined, packageTag: editForm.packageTag.trim() || undefined,
    });
    setEditingKey(null);
  }

  function startEditOut(e) {
    setEditingKey(`out-${e.id}`);
    setEditForm({ date: e.date, notes: e.notes || "" });
    setEditError("");
  }
  function saveEditOut(e) {
    setEditError("");
    onEditShipment(e.refId, { date: editForm.date, notes: editForm.notes.trim() });
    setEditingKey(null);
  }

  function startEditBucked(e) {
    setEditingKey(`${e.type}-${e.id}`);
    setEditForm({
      source: e.source, room: e.room, strain: e.strain, lot: e.lot || "", grams: e.grams, date: e.date,
      manifestNumber: e.manifestNumber || "", packageTag: e.packageTag || "", notes: e.notes || "",
    });
    setEditError("");
  }
  function saveEditBucked(e) {
    if (!editForm.strain.trim()) return setEditError("Strain name is required.");
    if (editForm.grams <= 0) return setEditError("Enter a weight greater than zero.");
    const floor = Math.max(0, e.grams - e.lotRemaining);
    if (editForm.grams < floor) return setEditError(`Can't go below ${fmtBoth(floor)} for this entry — that much of the batch has already been trimmed or logged as waste.`);
    setEditError("");
    onEditBuckedLot(e.refId, {
      source: e.type === "bucked_in" ? editForm.source : "daddyspipes", room: editForm.room, strain: editForm.strain.trim(), lot: editForm.lot.trim(),
    });
    onEditBuckedDelivery(e.refId, e.deliveryIndex, {
      grams: editForm.grams, date: editForm.date, notes: editForm.notes.trim(),
      manifestNumber: e.type === "bucked_in" ? (editForm.manifestNumber.trim() || undefined) : undefined,
      packageTag: e.type === "bucked_in" ? (editForm.packageTag.trim() || undefined) : undefined,
    });
    setEditingKey(null);
  }

  function startEditWaste(e) {
    setEditingKey(`waste-${e.id}`);
    setEditForm({ grams: e.grams, date: e.date, notes: e.notes || "" });
    setEditError("");
  }
  function saveEditWaste(e) {
    if (editForm.grams <= 0) return setEditError("Enter a weight greater than zero.");
    const available = e.lotRemaining + e.grams; // this entry's own old weight counted back in
    if (editForm.grams > available) return setEditError(`That's more than what's left of this batch — only ${fmtBoth(available)} available.`);
    setEditError("");
    onEditWaste(e.refId, { grams: editForm.grams, date: editForm.date, notes: editForm.notes.trim() });
    setEditingKey(null);
  }

  function startEditGrade(e) {
    setEditingKey(`grade-${e.id}`);
    const gg = {};
    grades.forEach((g) => { gg[g] = (e.gradeGrams && e.gradeGrams[g]) || 0; });
    setEditForm({ date: e.date, gradeGrams: gg, notes: e.notes || "" });
    setEditError("");
  }
  function saveEditGrade(e) {
    const newTotal = Object.values(editForm.gradeGrams).reduce((s, g) => s + (parseFloat(g) || 0), 0);
    if (newTotal <= 0) return setEditError("Add at least one grade with a weight.");
    setEditError("");
    onEditGrade(e.refId, {
      date: editForm.date,
      gradeGrams: Object.fromEntries(Object.entries(editForm.gradeGrams).map(([g, v]) => [g, parseFloat(v) || 0])),
      notes: editForm.notes.trim(),
    });
    setEditingKey(null);
  }

  return (
    <div>
      <div style={{ ...styles.groupToggle, marginBottom: 12, flexWrap: "wrap" }}>
        <button onClick={() => setTypeFilter("all")} style={{ ...styles.groupBtn, background: typeFilter === "all" ? "#C9A24B" : "transparent", color: typeFilter === "all" ? "#12160F" : "#B9BFA9" }}>All</button>
        {ENTRY_TYPE_ORDER.map((t) => (
          <button key={t} onClick={() => setTypeFilter(t)} style={{ ...styles.groupBtn, background: typeFilter === t ? "#C9A24B" : "transparent", color: typeFilter === t ? "#12160F" : "#B9BFA9" }}>{ENTRY_TYPE_META[t].label}</button>
        ))}
      </div>
      <div style={styles.searchBar}>
        <Search size={15} color="#7C8571" />
        <input style={styles.searchInput} placeholder="Search by strain, batch #, room, staff, or trimmer…" value={query} onChange={(e) => setQuery(e.target.value)} />
      </div>
      {filtered.length === 0 && <div style={styles.emptyState}>No entries yet.</div>}
      <div style={styles.historyList}>
        {filtered.map((e) => {
          const rowKey = `${e.type}-${e.id}`;
          if (editingKey === rowKey && e.type === "in") {
            return (
              <div key={rowKey} style={styles.editBatchForm}>
                <div style={styles.twoCol}>
                  <div><FieldLabel>Strain</FieldLabel><input style={styles.input} value={editForm.strain} onChange={(ev) => setEditForm({ ...editForm, strain: ev.target.value })} /></div>
                  <div><FieldLabel>Batch #</FieldLabel><input style={styles.input} value={editForm.lot} onChange={(ev) => setEditForm({ ...editForm, lot: ev.target.value })} /></div>
                </div>
                <FieldLabel>Source</FieldLabel>
                <div style={styles.sourceChoice}>
                  {["daddyspipes", "merc"].map((s) => (
                    <button type="button" key={s} onClick={() => setEditForm({ ...editForm, source: s, room: (rooms[s] || [])[0] || "" })} style={{ ...styles.sourceChoiceBtn, borderColor: editForm.source === s ? SOURCE_META[s].color : "#2A3324", background: editForm.source === s ? SOURCE_META[s].bg : "transparent", color: editForm.source === s ? SOURCE_META[s].color : "#B9BFA9" }}>{SOURCE_META[s].label}</button>
                  ))}
                </div>
                <FieldLabel>Room</FieldLabel>
                <select style={styles.input} value={editForm.room} onChange={(ev) => setEditForm({ ...editForm, room: ev.target.value })}>
                  {(rooms[editForm.source] || []).map((r) => <option key={r} value={r}>{r}</option>)}
                </select>
                <FieldLabel>Grade</FieldLabel>
                <div style={styles.gradeChoice}>
                  {grades.map((gCode) => {
                    const meta = gradeMetaFor(gCode, customGrades);
                    return <button type="button" key={gCode} title={gradeDescriptionFor(gCode, gradeDescriptions, customGrades)} onClick={() => setEditForm({ ...editForm, grade: gCode })} style={{ ...styles.gradeChoiceBtn, borderColor: editForm.grade === gCode ? meta.color : "#2A3324", color: editForm.grade === gCode ? meta.color : "#B9BFA9", background: editForm.grade === gCode ? "rgba(201,162,75,0.10)" : "transparent" }}>{meta.label}</button>;
                  })}
                </div>
                {editForm.grade === "A" && (
                  <label style={styles.bestCheckbox}>
                    <input type="checkbox" checked={editForm.isBest} onChange={(ev) => setEditForm({ ...editForm, isBest: ev.target.checked })} />
                    <Star size={13} color="#C9A24B" fill={editForm.isBest ? "#C9A24B" : "none"} /> Mark as Best A
                  </label>
                )}
                <label style={styles.bestCheckbox}>
                  <input type="checkbox" checked={editForm.isMachineTrimmed} onChange={(ev) => setEditForm({ ...editForm, isMachineTrimmed: ev.target.checked })} />
                  <Cog size={13} color="#8FAF8B" /> Machine Trimmed
                </label>
                <div style={styles.twoCol}>
                  <div><FieldLabel>Weight (this entry)</FieldLabel><DualWeightInput grams={editForm.grams} onChange={(v) => setEditForm({ ...editForm, grams: v })} /></div>
                  <div><FieldLabel>Date</FieldLabel><input style={styles.input} type="date" value={editForm.date} onChange={(ev) => setEditForm({ ...editForm, date: ev.target.value })} /></div>
                </div>
                <div style={styles.twoCol}>
                  <div><FieldLabel>Manifest #</FieldLabel><input style={styles.input} value={editForm.manifestNumber} onChange={(ev) => setEditForm({ ...editForm, manifestNumber: ev.target.value })} placeholder="Metrc manifest number" /></div>
                  <div><FieldLabel>Package Tag (last 4)</FieldLabel><input style={styles.input} value={editForm.packageTag} onChange={(ev) => setEditForm({ ...editForm, packageTag: ev.target.value })} placeholder="e.g. 4821" maxLength={8} /></div>
                </div>
                <FieldLabel>Notes</FieldLabel>
                <textarea style={{ ...styles.input, minHeight: 50 }} value={editForm.notes} onChange={(ev) => setEditForm({ ...editForm, notes: ev.target.value })} />
                {editError && <div style={styles.errorText}>{editError}</div>}
                <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
                  <button type="button" style={styles.submitBtn} onClick={() => saveEditIn(e)}>Save</button>
                  <button type="button" style={styles.backBtn} onClick={() => setEditingKey(null)}>Cancel</button>
                </div>
                {e.multiAddition && <div style={{ fontSize: 11.5, color: "#7C8571", marginTop: 8 }}>Strain, room, source, and grade apply to the whole batch. Weight, date, manifest #, and notes apply just to this entry.</div>}
              </div>
            );
          }
          if (editingKey === rowKey && e.type === "out") {
            return (
              <div key={rowKey} style={styles.editBatchForm}>
                <FieldLabel>Date</FieldLabel>
                <input style={styles.input} type="date" value={editForm.date} onChange={(ev) => setEditForm({ ...editForm, date: ev.target.value })} />
                <FieldLabel>Notes</FieldLabel>
                <textarea style={{ ...styles.input, minHeight: 50 }} value={editForm.notes} onChange={(ev) => setEditForm({ ...editForm, notes: ev.target.value })} />
                <div style={{ fontSize: 11.5, color: "#7C8571", margin: "6px 0" }}>Weight and destination aren't editable here since they're tied to the order — delete and re-log if those need to change.</div>
                {editError && <div style={styles.errorText}>{editError}</div>}
                <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
                  <button type="button" style={styles.submitBtn} onClick={() => saveEditOut(e)}>Save</button>
                  <button type="button" style={styles.backBtn} onClick={() => setEditingKey(null)}>Cancel</button>
                </div>
              </div>
            );
          }
          if (editingKey === rowKey && (e.type === "bucked_in" || e.type === "harvest_in")) {
            return (
              <div key={rowKey} style={styles.editBatchForm}>
                <div style={styles.twoCol}>
                  <div><FieldLabel>Strain</FieldLabel><input style={styles.input} value={editForm.strain} onChange={(ev) => setEditForm({ ...editForm, strain: ev.target.value })} /></div>
                  <div><FieldLabel>{e.type === "harvest_in" ? "Harvest ID" : "Batch #"}</FieldLabel><input style={styles.input} value={editForm.lot} onChange={(ev) => setEditForm({ ...editForm, lot: ev.target.value })} /></div>
                </div>
                {e.type === "bucked_in" && (
                  <>
                    <FieldLabel>Source</FieldLabel>
                    <div style={styles.sourceChoice}>
                      {["daddyspipes", "merc"].map((s) => (
                        <button type="button" key={s} onClick={() => setEditForm({ ...editForm, source: s, room: (rooms[s] || [])[0] || "" })} style={{ ...styles.sourceChoiceBtn, borderColor: editForm.source === s ? SOURCE_META[s].color : "#2A3324", background: editForm.source === s ? SOURCE_META[s].bg : "transparent", color: editForm.source === s ? SOURCE_META[s].color : "#B9BFA9" }}>{SOURCE_META[s].label}</button>
                      ))}
                    </div>
                  </>
                )}
                <FieldLabel>Room</FieldLabel>
                <select style={styles.input} value={editForm.room} onChange={(ev) => setEditForm({ ...editForm, room: ev.target.value })}>
                  {(rooms[e.type === "harvest_in" ? "daddyspipes" : editForm.source] || []).map((r) => <option key={r} value={r}>{r}</option>)}
                </select>
                <div style={styles.twoCol}>
                  <div><FieldLabel>Weight (this entry)</FieldLabel><DualWeightInput grams={editForm.grams} onChange={(v) => setEditForm({ ...editForm, grams: v })} /></div>
                  <div><FieldLabel>Date</FieldLabel><input style={styles.input} type="date" value={editForm.date} onChange={(ev) => setEditForm({ ...editForm, date: ev.target.value })} /></div>
                </div>
                {e.type === "bucked_in" && (
                  <div style={styles.twoCol}>
                    <div><FieldLabel>Manifest #</FieldLabel><input style={styles.input} value={editForm.manifestNumber} onChange={(ev) => setEditForm({ ...editForm, manifestNumber: ev.target.value })} placeholder="Metrc manifest number" /></div>
                    <div><FieldLabel>Package Tag (last 4)</FieldLabel><input style={styles.input} value={editForm.packageTag} onChange={(ev) => setEditForm({ ...editForm, packageTag: ev.target.value })} placeholder="e.g. 4821" maxLength={8} /></div>
                  </div>
                )}
                <FieldLabel>Notes</FieldLabel>
                <textarea style={{ ...styles.input, minHeight: 50 }} value={editForm.notes} onChange={(ev) => setEditForm({ ...editForm, notes: ev.target.value })} />
                {editError && <div style={styles.errorText}>{editError}</div>}
                <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
                  <button type="button" style={styles.submitBtn} onClick={() => saveEditBucked(e)}>Save</button>
                  <button type="button" style={styles.backBtn} onClick={() => setEditingKey(null)}>Cancel</button>
                </div>
                {e.multiAddition && <div style={{ fontSize: 11.5, color: "#7C8571", marginTop: 8 }}>Strain, room, and source apply to the whole batch. Weight, date, manifest #, tag, and notes apply just to this entry.</div>}
              </div>
            );
          }
          if (editingKey === rowKey && e.type === "waste") {
            return (
              <div key={rowKey} style={styles.editBatchForm}>
                <div style={styles.twoCol}>
                  <div><FieldLabel>Waste weight</FieldLabel><DualWeightInput grams={editForm.grams} onChange={(v) => setEditForm({ ...editForm, grams: v })} /></div>
                  <div><FieldLabel>Date</FieldLabel><input style={styles.input} type="date" value={editForm.date} onChange={(ev) => setEditForm({ ...editForm, date: ev.target.value })} /></div>
                </div>
                <FieldLabel>Notes</FieldLabel>
                <textarea style={{ ...styles.input, minHeight: 50 }} value={editForm.notes} onChange={(ev) => setEditForm({ ...editForm, notes: ev.target.value })} />
                {editError && <div style={styles.errorText}>{editError}</div>}
                <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
                  <button type="button" style={styles.submitBtn} onClick={() => saveEditWaste(e)}>Save</button>
                  <button type="button" style={styles.backBtn} onClick={() => setEditingKey(null)}>Cancel</button>
                </div>
              </div>
            );
          }
          if (editingKey === rowKey && e.type === "grade") {
            return (
              <div key={rowKey} style={styles.editBatchForm}>
                <FieldLabel>Date</FieldLabel>
                <input style={styles.input} type="date" value={editForm.date} onChange={(ev) => setEditForm({ ...editForm, date: ev.target.value })} />
                {grades.map((g) => (
                  <div key={g} style={{ display: "flex", alignItems: "center", gap: 8, marginTop: 8 }}>
                    <div style={{ width: 90 }}><GradeBadge grade={g} isBest={false} /></div>
                    <input style={{ ...styles.input, width: 100 }} type="number" step="1" min="0" value={editForm.gradeGrams[g]} onChange={(ev) => setEditForm({ ...editForm, gradeGrams: { ...editForm.gradeGrams, [g]: ev.target.value } })} />
                    <span style={{ fontSize: 11, color: "#8C9483" }}>grams</span>
                  </div>
                ))}
                <FieldLabel>Notes</FieldLabel>
                <textarea style={{ ...styles.input, minHeight: 50 }} value={editForm.notes} onChange={(ev) => setEditForm({ ...editForm, notes: ev.target.value })} />
                {editError && <div style={styles.errorText}>{editError}</div>}
                <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
                  <button type="button" style={styles.submitBtn} onClick={() => saveEditGrade(e)}>Save</button>
                  <button type="button" style={styles.backBtn} onClick={() => setEditingKey(null)}>Cancel</button>
                </div>
              </div>
            );
          }
          const typeMeta = ENTRY_TYPE_META[e.type];
          const TypeIcon = typeMeta.icon;
          const isEditable = e.type === "in" || e.type === "out" || e.type === "bucked_in" || e.type === "harvest_in" || e.type === "waste" || e.type === "grade";
          return (
            <div key={rowKey} style={styles.historyRow}>
              <div style={{ ...styles.historyIcon, background: typeMeta.bg }}>
                <TypeIcon size={15} color={typeMeta.color} />
              </div>
              <div style={{ flex: 1 }}>
                <div style={styles.historyTop}>
                  <span style={styles.historyStrain}>{e.source && <OriginDot source={e.source} style={{ marginRight: 6 }} />}{e.strain} {e.grade && <GradeBadge grade={e.grade} isBest={e.isBest} />}{e.isMachineTrimmed && <span style={styles.machineTag} title="Machine Trimmed — internal only"><Cog size={10} /> MT</span>}</span>
                  <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
                    <span style={styles.historyWeight}>{ENTRY_TYPE_POSITIVE[e.type] ? "+" : "−"}{fmtWeight(e.grams, unit)}</span>
                    {isEditable && canEdit && (
                      <button style={styles.iconBtn} onClick={() => {
                        if (e.type === "in") startEditIn(e);
                        else if (e.type === "out") startEditOut(e);
                        else if (e.type === "waste") startEditWaste(e);
                        else if (e.type === "grade") startEditGrade(e);
                        else startEditBucked(e);
                      }}><Edit3 size={13} /></button>
                    )}
                    {isEditable && canDelete && (
                      <button style={styles.iconBtnDanger} onClick={() => {
                        if (e.type === "in") {
                          const msg = e.multiAddition
                            ? `Delete the whole batch (${e.strain})? This removes every addition logged to it, not just this one entry.`
                            : `Delete this incoming batch (${e.strain})? Any linked outgoing entries will be removed too.`;
                          if (confirm(msg)) onDeleteBatch(e.refId);
                        } else if (e.type === "out") {
                          if (confirm(`Delete this outgoing entry (${e.strain})? Stock will be restored.`)) onDeleteShipment(e.refId);
                        } else if (e.type === "waste") {
                          if (confirm(`Delete this waste entry? The weight goes back to the batch.`)) onDeleteWaste(e.refId);
                        } else if (e.type === "grade") {
                          if (confirm(`Delete this grade entry?`)) onDeleteGrade(e.refId);
                        } else {
                          const msg = e.multiAddition
                            ? `Delete the whole batch (${e.strain})? This removes every delivery logged to it, along with any trim or waste already logged against it.`
                            : `Delete this batch (${e.strain})? Any trim or waste already logged against it will be removed too.`;
                          if (confirm(msg)) onDeleteBuckedLot(e.refId);
                        }
                      }}><Trash2 size={13} /></button>
                    )}
                  </div>
                </div>
                <div style={styles.historyMeta}>
                  {typeFilter === "all" && `${typeMeta.label} · `}{e.date} · {e.staff || "unattributed"}{e.trimmerName ? ` · Trimmer: ${e.trimmerName}` : ""}{e.room ? ` · ${e.room}` : ""}{e.detail ? ` · ${e.detail}` : ""}{e.source ? ` · ${SOURCE_META[e.source].label}` : ""}{e.manifestNumber ? ` · Manifest ${e.manifestNumber}` : ""}{e.packageTag ? ` · Tag …${e.packageTag}` : ""}{e.multiAddition ? " · one of several additions to this batch" : ""}
                </div>
                {e.type === "grade" && e.gradeGrams && (
                  <div style={styles.strainSourceTags}>
                    {Object.entries(e.gradeGrams).filter(([, g]) => g > 0).map(([g, grams]) => (
                      <span key={g} style={styles.catalogChip}><GradeBadge grade={g} isBest={false} /> {fmtBoth(grams)}</span>
                    ))}
                  </div>
                )}
                {e.notes && <div style={styles.historyNotes}>{e.notes}</div>}
              </div>
            </div>
          );
        })}
      </div>
    </div>
  );
}


const styles = {
  app: { minHeight: "100vh", background: "#12160F", color: "#EDE8D8", fontFamily: "'Inter', sans-serif", paddingBottom: 40 },
  trimmerDropdown: { position: "absolute", top: "calc(100% + 4px)", left: 0, right: 0, background: "#1A1F14", border: "1px solid #2A3324", borderRadius: 8, zIndex: 50, overflow: "hidden", boxShadow: "0 6px 20px rgba(0,0,0,0.4)", maxHeight: 260, overflowY: "auto" },
  trimmerOption: { padding: "10px 12px", fontSize: 14, color: "#EDE8D8", cursor: "pointer" },
  // --- Tag scanner ---
  scanOverlay: { position: "fixed", inset: 0, zIndex: 1000, background: "#0A0D07", display: "flex", flexDirection: "column" },
  scanHeader: { display: "flex", alignItems: "center", justifyContent: "space-between", padding: "16px 16px", fontFamily: "'Oswald', sans-serif", fontSize: 17, letterSpacing: "0.03em", color: "#EDE8D8", borderBottom: "1px solid #232B1D" },
  scanClose: { background: "none", border: "none", color: "#B9BFA9", cursor: "pointer", padding: 4 },
  scanStage: { position: "relative", flex: 1, overflow: "hidden", background: "#000", display: "flex", alignItems: "center", justifyContent: "center" },
  scanVideo: { width: "100%", height: "100%", overflow: "hidden", background: "#000" },
  scanVideoInner: {},
  scanReticle: { position: "absolute", top: "50%", left: "50%", transform: "translate(-50%, -50%)", width: "82%", maxWidth: 420, height: 120, border: "2px solid rgba(201,162,75,0.9)", borderRadius: 12, boxShadow: "0 0 0 9999px rgba(0,0,0,0.45)", display: "flex", alignItems: "center", justifyContent: "center" },
  scanReticleLine: { width: "90%", height: 2, background: "rgba(201,162,75,0.8)" },
  scanHint: { position: "absolute", bottom: 24, left: 0, right: 0, textAlign: "center", color: "#EDE8D8", fontSize: 13.5, textShadow: "0 1px 3px rgba(0,0,0,0.8)" },
  scanControls: { padding: "14px 0 24px", borderTop: "1px solid #232B1D", background: "#12160F" },
  scanManualToggle: { display: "flex", alignItems: "center", gap: 8, margin: "0 auto", background: "none", border: "1px solid #2A3324", borderRadius: 8, color: "#B9BFA9", padding: "10px 16px", fontSize: 14, cursor: "pointer" },
  scanResultCard: { display: "flex", alignItems: "center", gap: 10, background: "#1A1F14", border: "1px solid #2A3324", borderRadius: 10, padding: "12px 14px", margin: "8px 0" },
  binCard: { display: "flex", alignItems: "center", gap: 10, background: "#1A1F14", border: "1px solid #2A3324", borderRadius: 10, padding: "10px 12px", margin: "6px 0 6px 18px" },
  binMoveBtn: { background: "transparent", border: "1px solid #3A4232", borderRadius: 7, color: "#B9BFA9", padding: "6px 10px", fontSize: 12.5, cursor: "pointer", whiteSpace: "nowrap" },
  readoutRow: { display: "flex", alignItems: "center", justifyContent: "space-between", padding: "8px 0" },
  loadingScreen: { minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", background: "#12160F" },
  gateWrap: { maxWidth: 420, margin: "0 auto", padding: "50px 20px 30px", display: "flex", flexDirection: "column", alignItems: "center", textAlign: "center" },
  gateTitle: { fontFamily: "'Oswald', sans-serif", fontWeight: 600, fontSize: 22, letterSpacing: "0.06em", marginTop: 14 },
  gateSubtitle: { fontSize: 13, color: "#7C8571", marginTop: 6, marginBottom: 22 },
  authForm: { width: "100%", background: "#171D12", border: "1px solid #2A3324", borderRadius: 12, padding: 20, textAlign: "left" },
  pinHint: { fontSize: 11, color: "#6B7264", marginTop: 14, lineHeight: 1.5 },
  linkBtn: { background: "none", border: "none", color: "#8FAF8B", fontSize: 12, cursor: "pointer", marginTop: 8, display: "block", textAlign: "left", padding: 0 },
  header: { display: "flex", alignItems: "center", justifyContent: "space-between", padding: "16px 20px", borderBottom: "1px solid #232B1D", flexWrap: "wrap", gap: 12 },
  headerLeft: { display: "flex", alignItems: "center", gap: 10 },
  logoMark: { width: 32, height: 32, borderRadius: 6, background: "#C9A24B", display: "flex", alignItems: "center", justifyContent: "center", flexShrink: 0 },
  title: { fontFamily: "'Oswald', sans-serif", fontWeight: 600, fontSize: 17, letterSpacing: "0.06em", lineHeight: 1.1 },
  versionTag: { fontFamily: "'IBM Plex Mono', monospace", fontSize: 10, fontWeight: 500, color: "#6B7264", letterSpacing: "0.02em" },
  demoBadge: { display: "inline-block", marginLeft: 8, fontSize: 10, fontWeight: 700, letterSpacing: "0.06em", color: "#12160F", background: "#C9A24B", padding: "2px 8px", borderRadius: 4, verticalAlign: "middle" },
  subtitle: { fontSize: 11, color: "#7C8571", marginTop: 2 },
  headerRight: { display: "flex", alignItems: "center", gap: 10 },
  unitToggle: { display: "flex", border: "1px solid #2A3324", borderRadius: 6, overflow: "hidden" },
  unitBtn: { border: "none", padding: "6px 10px", fontSize: 12, fontWeight: 600, cursor: "pointer", fontFamily: "'IBM Plex Mono', monospace" },
  savingTag: { fontSize: 11, color: "#7C8571", fontStyle: "italic" },
  switchRoleBtn: { display: "flex", alignItems: "center", gap: 5, background: "transparent", border: "1px solid #2A3324", color: "#8C9483", borderRadius: 6, padding: "6px 10px", fontSize: 12, cursor: "pointer" },
  tabBar: { display: "flex", gap: 4, padding: "0 20px", borderBottom: "1px solid #232B1D", overflowX: "auto" },
  tabBtn: { display: "flex", alignItems: "center", gap: 6, background: "none", border: "none", padding: "12px 14px", fontSize: 13, fontWeight: 500, cursor: "pointer", whiteSpace: "nowrap" },
  main: { padding: "24px 20px", maxWidth: 780, margin: "0 auto" },
  summaryRow: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(150px, 1fr))", gap: 12, marginBottom: 22 },
  summaryCard: { background: "#1A2015", border: "1px solid #232B1D", borderRadius: 10, padding: "14px 16px" },
  summaryLabel: { fontSize: 11, color: "#7C8571", letterSpacing: "0.05em", textTransform: "uppercase" },
  summaryValue: { fontFamily: "'IBM Plex Mono', monospace", fontSize: 22, fontWeight: 600, marginTop: 6, fontVariantNumeric: "tabular-nums" },
  dashControls: { display: "flex", justifyContent: "space-between", alignItems: "center", flexWrap: "wrap", gap: 10, marginBottom: 4 },
  groupToggle: { display: "flex", flexWrap: "wrap", border: "1px solid #2A3324", borderRadius: 7, overflow: "hidden" },
  groupBtn: { border: "none", padding: "7px 12px", fontSize: 12, fontWeight: 600, cursor: "pointer" },
  groupBtnSm: { border: "none", padding: "6px 12px", fontSize: 12, fontWeight: 500, cursor: "pointer", borderRadius: 6 },
  shipBadge: { display: "inline-flex", alignItems: "center", justifyContent: "center", minWidth: 18, height: 18, padding: "0 5px", marginLeft: 6, borderRadius: 9, background: "#B9603F", color: "#fff", fontSize: 11, fontWeight: 700, verticalAlign: "middle" },
  shipCard: { background: "#161B12", border: "1px solid #2A3324", borderRadius: 10, padding: 14, marginBottom: 10 },
  bestFilterBtn: { display: "flex", alignItems: "center", gap: 5, border: "1px solid", borderRadius: 7, padding: "6px 11px", fontSize: 12, fontWeight: 500, cursor: "pointer", background: "transparent" },
  strainGrid: { display: "flex", flexDirection: "column", gap: 10 },
  strainCard: { background: "#171D12", border: "1px solid #2A3324", borderRadius: 10, overflow: "hidden" },
  strainCardTop: { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "14px 16px", cursor: "pointer" },
  strainName: { fontSize: 15, fontWeight: 600 },
  strainSourceTags: { display: "flex", gap: 6, marginTop: 6, flexWrap: "wrap", alignItems: "center" },
  sourceTag: { fontSize: 10, fontWeight: 600, padding: "2px 7px", borderRadius: 4, letterSpacing: "0.03em" },
  gradeBadge: { fontSize: 10, fontWeight: 700, padding: "1px 6px", borderRadius: 4, border: "1px solid", display: "inline-flex", alignItems: "center", fontFamily: "'IBM Plex Mono', monospace", whiteSpace: "nowrap" },
  lowTag: { fontSize: 10, fontWeight: 600, padding: "2px 7px", borderRadius: 4, background: "rgba(185,96,63,0.16)", color: "#B9603F", display: "inline-flex", alignItems: "center", gap: 3 },
  readout: { fontFamily: "'IBM Plex Mono', monospace", fontSize: 20, fontWeight: 600, color: "#EDE8D8", fontVariantNumeric: "tabular-nums", textShadow: "0 0 12px rgba(201,162,75,0.18)" },
  batchList: { borderTop: "1px solid #232B1D", padding: "8px 16px 12px" },
  batchRow: { display: "grid", gridTemplateColumns: "12px 1fr auto auto auto auto auto", gap: 10, alignItems: "center", padding: "6px 0", fontSize: 12.5, color: "#B9BFA9" },
  batchLot: { color: "#D8D3C2", display: "flex", alignItems: "center", gap: 6, flexWrap: "wrap" },
  batchDate: { color: "#7C8571" },
  batchWeight: { fontFamily: "'IBM Plex Mono', monospace", color: "#EDE8D8" },
  emptyState: { background: "#171D12", border: "1px dashed #2A3324", borderRadius: 10, padding: "24px 18px", textAlign: "center", color: "#8C9483", fontSize: 13.5 },
  noticeBox: { background: "rgba(201,162,75,0.10)", border: "1px solid rgba(201,162,75,0.3)", color: "#D8C08A", borderRadius: 8, padding: "10px 14px", fontSize: 12.5, marginBottom: 14 },
  saveErrorBanner: { display: "flex", alignItems: "center", gap: 8, background: "rgba(185,96,63,0.18)", borderBottom: "1px solid #B9603F", color: "#F0C4B5", padding: "10px 20px", fontSize: 12.5, fontWeight: 600 },
  sectionLabel: { fontSize: 12, color: "#7C8571", letterSpacing: "0.08em", marginBottom: 12, marginTop: 24, fontWeight: 600 },
  form: { display: "flex", flexDirection: "column", gap: 4, background: "#171D12", border: "1px solid #232B1D", borderRadius: 12, padding: 20 },
  formHeader: { display: "flex", alignItems: "center", gap: 8, fontFamily: "'Oswald', sans-serif", fontSize: 16, letterSpacing: "0.03em", marginBottom: 12 },
  fieldLabel: { fontSize: 11.5, color: "#8C9483", marginTop: 12, marginBottom: 5, fontWeight: 500 },
  input: { background: "#12160F", border: "1px solid #2A3324", borderRadius: 7, padding: "9px 11px", color: "#EDE8D8", fontSize: 16, width: "100%" },
  twoCol: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 },
  sourceChoice: { display: "flex", gap: 8 },
  sourceChoiceBtn: { flex: 1, display: "flex", alignItems: "center", justifyContent: "center", gap: 6, border: "1px solid", borderRadius: 8, padding: "9px 10px", fontSize: 13, fontWeight: 500, cursor: "pointer", background: "transparent" },
  gradeChoice: { display: "flex", flexWrap: "wrap", gap: 8 },
  gradeChoiceBtn: { border: "1px solid", borderRadius: 7, padding: "8px 14px", fontSize: 13, fontWeight: 600, cursor: "pointer", flex: "1 1 auto" },
  bestCheckbox: { display: "flex", alignItems: "center", gap: 7, marginTop: 10, fontSize: 12.5, color: "#D8D3C2", cursor: "pointer" },
  errorText: { color: "#D97757", fontSize: 12.5, marginTop: 10 },
  submitBtn: { marginTop: 18, background: "#C9A24B", color: "#12160F", border: "none", borderRadius: 8, padding: "11px 16px", fontSize: 14, fontWeight: 600, display: "flex", alignItems: "center", justifyContent: "center", gap: 6, cursor: "pointer" },
  searchBar: { display: "flex", alignItems: "center", gap: 8, background: "#171D12", border: "1px solid #2A3324", borderRadius: 8, padding: "9px 12px", marginBottom: 16 },
  searchInput: { background: "none", border: "none", color: "#EDE8D8", fontSize: 16, flex: 1 },
  historyList: { display: "flex", flexDirection: "column", gap: 8 },
  historyRow: { display: "flex", gap: 12, background: "#171D12", border: "1px solid #232B1D", borderRadius: 9, padding: "12px 14px" },
  historyIcon: { width: 30, height: 30, borderRadius: 7, display: "flex", alignItems: "center", justifyContent: "center", flexShrink: 0 },
  historyTop: { display: "flex", justifyContent: "space-between", fontSize: 13.5, gap: 8, alignItems: "center" },
  historyStrain: { fontWeight: 600, display: "flex", alignItems: "center", gap: 6 },
  historyWeight: { fontFamily: "'IBM Plex Mono', monospace", fontVariantNumeric: "tabular-nums" },
  historyMeta: { fontSize: 11.5, color: "#7C8571", marginTop: 3 },
  historyNotes: { fontSize: 12, color: "#9BA38C", marginTop: 4, fontStyle: "italic" },
  iconBtn: { background: "none", border: "none", color: "#7C8571", cursor: "pointer", padding: 4, display: "flex" },
  iconBtnDanger: { background: "none", border: "1px solid #3A2A22", color: "#B9603F", cursor: "pointer", padding: "5px 6px", borderRadius: 6, display: "flex" },
  iconLabelBtn: { display: "flex", alignItems: "center", gap: 6, background: "#1A2015", border: "1px solid #2A3324", color: "#EDE8D8", borderRadius: 7, padding: "8px 12px", fontSize: 12.5, cursor: "pointer" },
  backBtn: { background: "none", border: "1px solid #2A3324", color: "#B9BFA9", borderRadius: 7, padding: "8px 12px", fontSize: 12.5, cursor: "pointer" },
  lineItemList: { marginTop: 14, borderTop: "1px solid #232B1D", paddingTop: 10 },
  lineItemRow: { display: "flex", justifyContent: "space-between", alignItems: "center", padding: "8px 0", borderBottom: "1px solid #1E2419" },
  lineItemStrain: { fontSize: 13, fontWeight: 600, display: "flex", alignItems: "center", gap: 6 },
  lineItemMeta: { fontSize: 11.5, color: "#8C9483", marginTop: 2 },
  lineItemRight: { display: "flex", alignItems: "center", gap: 10 },
  lineItemTotal: { fontFamily: "'IBM Plex Mono', monospace", fontSize: 13.5 },
  subtotalRow: { display: "flex", justifyContent: "space-between", paddingTop: 10, fontSize: 14, fontWeight: 600 },
  subtotalValue: { fontFamily: "'IBM Plex Mono', monospace" },
  orderRow: { display: "flex", justifyContent: "space-between", alignItems: "center", background: "#171D12", border: "1px solid #2A3324", borderRadius: 10, padding: "12px 16px", flexWrap: "wrap", gap: 10 },
  viewInvoiceBtn: { background: "transparent", border: "1px solid #2A3324", color: "#B9BFA9", borderRadius: 6, padding: "6px 10px", fontSize: 12, cursor: "pointer" },
  paperOuter: { overflowX: "auto", padding: "4px 0 20px" },
  paper: { background: "#F7F4EA", color: "#1A1A14", padding: "0.5in", border: "1px solid #ddd", boxShadow: "0 6px 24px rgba(0,0,0,0.35)", width: "8.5in", minHeight: "11in", maxWidth: "none", margin: "0 auto", boxSizing: "border-box" },
  paperHeader: { display: "flex", justifyContent: "space-between", alignItems: "flex-start", marginBottom: 18, flexWrap: "wrap", gap: 10 },
  shipperTop: { fontSize: 12.5, color: "#333", lineHeight: 1.6 },
  shipperBizName: { fontWeight: 700, fontSize: 15.5, color: "#1A1A14", marginBottom: 2 },
  billToRow: { display: "flex", justifyContent: "space-between", borderTop: "1px solid #ccc", marginTop: 6, paddingTop: 10, marginBottom: 14, fontSize: 12.5, color: "#333", lineHeight: 1.6 },
  billToLabel: { fontSize: 10, letterSpacing: "0.06em", color: "#888", textTransform: "uppercase" },
  paperTitle: { fontFamily: "'Oswald', sans-serif", fontSize: 22, fontWeight: 700, letterSpacing: "0.04em" },
  paperSub: { fontSize: 12, color: "#555", marginTop: 2 },
  paperMetaBlock: { fontSize: 12.5, color: "#333", textAlign: "right", lineHeight: 1.6 },
  paperTable: { width: "100%", borderCollapse: "collapse", marginTop: 6 },
  th: { textAlign: "left", fontSize: 11, textTransform: "uppercase", letterSpacing: "0.04em", color: "#666", borderBottom: "1px solid #ccc", padding: "6px 8px" },
  td: { fontSize: 13, padding: "8px 8px", borderBottom: "1px solid #e5e2d6" },
  paperTotalRow: { display: "flex", justifyContent: "flex-end", gap: 20, marginTop: 16, fontSize: 15, paddingTop: 12, borderTop: "1px solid #ccc" },
  shipperBlock: { fontSize: 12, color: "#444", lineHeight: 1.6, borderTop: "1px solid #ccc", marginTop: 20, paddingTop: 10 },
  catalogChip: { display: "inline-flex", alignItems: "center", gap: 6, background: "#1A2015", border: "1px solid #2A3324", borderRadius: 6, padding: "4px 8px 4px 10px", fontSize: 12.5, color: "#D8D3C2" },
  quickFillChip: { display: "inline-flex", alignItems: "center", gap: 6, background: "rgba(201,162,75,0.10)", border: "1px solid rgba(201,162,75,0.35)", borderRadius: 999, padding: "6px 12px", fontSize: 12.5, color: "#D8C08A", cursor: "pointer" },
  chipRemove: { background: "none", border: "none", color: "#7C8571", cursor: "pointer", padding: 2, display: "flex" },
  paidBadge: { fontSize: 10.5, fontWeight: 700, padding: "3px 9px", borderRadius: 5, letterSpacing: "0.04em" },
  paidBadgePaid: { background: "rgba(124,148,115,0.18)", color: "#8FAF8B" },
  paidBadgeUnpaid: { background: "rgba(185,96,63,0.18)", color: "#C9855F" },
  strainPhotoGrid: { display: "flex", flexDirection: "column", gap: 8, marginTop: 4 },
  strainPhotoCard: { display: "flex", alignItems: "center", gap: 10, background: "#1A2015", border: "1px solid #2A3324", borderRadius: 8, padding: "8px 10px" },
  strainPhotoThumb: { width: 40, height: 40, borderRadius: 6, background: "#12160F", display: "flex", alignItems: "center", justifyContent: "center", overflow: "hidden", flexShrink: 0 },
  batchPhotoBtn: { display: "flex", alignItems: "center", justifyContent: "center", width: 22, height: 22, borderRadius: 4, background: "#1A2015", border: "1px solid #2A3324", cursor: "pointer", overflow: "hidden" },
  machineTag: { display: "inline-flex", alignItems: "center", gap: 3, fontSize: 10, color: "#8FAF8B", background: "rgba(143,175,139,0.14)", borderRadius: 4, padding: "1px 5px", marginLeft: 6 },
  editBatchForm: { background: "#161B10", border: "1px solid #2A3324", borderRadius: 8, padding: "12px 12px 6px", marginBottom: 4 },
  sourceSection: { marginBottom: 24 },
  sourceSectionHeader: { display: "flex", alignItems: "center", gap: 8, fontSize: 14, fontWeight: 600, color: "#EDE8D8", marginBottom: 10 },
  sourceSectionTotal: { marginLeft: "auto", fontFamily: "'IBM Plex Mono', monospace", fontSize: 13, color: "#C9A24B" },
  matrixTable: { width: "100%", borderCollapse: "collapse", fontSize: 12.5 },
  matrixTh: { textAlign: "right", fontWeight: 500, color: "#7C8571", fontSize: 11, padding: "6px 10px", borderBottom: "1px solid #2A3324", whiteSpace: "nowrap" },
  matrixTd: { textAlign: "right", padding: "9px 10px", color: "#D8D3C2", borderBottom: "1px solid #1E2518", fontFamily: "'IBM Plex Mono', monospace", whiteSpace: "nowrap" },
  matrixTdStrain: { textAlign: "left", padding: "9px 10px", color: "#EDE8D8", borderBottom: "1px solid #1E2518", fontWeight: 600, whiteSpace: "nowrap" },
  matrixRow: { cursor: "pointer" },
  searchResultsDropdown: { position: "absolute", top: "calc(100% + 4px)", left: 0, right: 0, zIndex: 20, background: "#171D12", border: "1px solid #3A4232", borderRadius: 8, maxHeight: 260, overflowY: "auto", boxShadow: "0 8px 24px rgba(0,0,0,0.4)" },
  searchResultRow: { padding: "9px 12px", cursor: "pointer", borderBottom: "1px solid #232B1D" },
  selectedBatchChip: { display: "flex", justifyContent: "space-between", alignItems: "center", background: "#1A2015", border: "1px solid #2A3324", borderRadius: 8, padding: "10px 12px" },
  smallLinkLabel: { fontSize: 11.5, color: "#8FAF8B", cursor: "pointer", display: "inline-block", marginTop: 2 },
  menuGridScreen: { display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(150px, 1fr))", gap: 12 },
  menuCardScreen: { background: "#171D12", border: "1px solid #2A3324", borderRadius: 10, overflow: "hidden" },
  menuCardPhoto: { width: "100%", height: 110, background: "#12160F", display: "flex", alignItems: "center", justifyContent: "center" },
};
